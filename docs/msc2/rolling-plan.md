# MSC 2 — Rolling Plan

> ## STATUS: Phase 11 (desktop/web clients) is in progress; **Phase 12 (client redesign) is now planned** below. Phase 11 shipped a working client wired to the real agent, but its UI diverged from MSC 1's information architecture and design language — Phase 12 rebuilds the presentation layer to MSC 1 fidelity, *refreshed*. Terminal UI moved to Phase 13. Phase 12's design system (S0) and shell (S1) were shaped and locked as reference specimens in `docs/msc2/renderings/`, governed by `docs/msc2/antiAIslop.md` (hard rule #11).
> **Next move:** P12.2 (Overview tab) is DONE — Cameron verified it 2026-08-25. P12.2b–j (Java player-data NBT backend, built with Codex) all landed and are marked DONE. P12.3/P12.3a/P12.3b/P12.3c/P12.3d/P12.3e (Players tab, session-log backend, Bedrock identify/skin fixes, real Bedrock stats/inventory) are all DONE — Cameron verified them. P12.3f (session-log client swap) and P12.3g (live-review polish) are built and **awaiting Cameron's verification**. P12.4 (Worlds tab + world wizards) is built and **awaiting Cameron's verification**. **P12.4a–e (2026-08-26) gave P12.4's five found gaps real backends and are all DONE** — Cameron verified them: P12.4a exposes Chunker's already-real `supported_formats` over HTTP, P12.4b wires the already-built `set_slot_thumbnail`/`save_thumbnail` application code to a route, P12.4c lets `POST /v1/worlds/import` redeem an already-on-disk backup, and P12.4d/e port and wire the one genuinely new feature, real Bedrock world repair. **P12.4f (2026-08-26) connects all five to their frontends and is built, awaiting Cameron's verification** — see its own entry below for a real contract/code drift it found and fixed (repair's response schema) and a pre-existing tooling inconsistency it found and deliberately left for a separate decision (`generate.ts`'s quote style vs. the rest of the repo's). See this file's 2026-08-25 notes below for the fuller history of gaps found and how each was handled. **P12.5 and P12.6 (Packs tab removal, Performance tab) are DONE** — Cameron verified them. **P12.7 (Components tab + plugin browser) is built (2026-08-26), awaiting Cameron's verification** — see its own entry below for a small additive `ScreenApi.upload` interface fix it needed and the real pre-existing backend/contract gaps it found and left alone. **P12.7a (2026-08-26) is DONE** — after seeing the real plugin browser, Cameron flagged blank authors/`0 downloads`/no icons; traced to a real two-layer backend bug (`ModrinthSearchHit` never captured those fields, and the route hardcoded them to empty anyway) and fixed both, plus the client's missing icon render. **P12.7b (2026-08-26) is DONE** — icons still didn't show after P12.7a's rebuild; the real cause was one layer deeper, a `#[serde(rename_all = "camelCase")]` acronym-casing bug turning `icon_url` into wire key `iconUrl` instead of the contract's `iconURL`, silently unread by the client. Fixed with explicit `#[serde(rename = ...)]` overrides on all three affected DTO fields plus regression tests. **P12.7c and P12.7d close the one documented gap P12.7/P12.7a left open** — MSC 1's `ModrinthProjectDetailView` (gallery, full About text, per-version compatibility + install) has no backing route. Split into two steps so Codex and Claude Code could work them independently: **P12.7c (2026-08-26) is DONE** — Codex added the contract + Rust backend (two new `GET /v1/catalog/projects/:projectId[/versions]` routes, an additive `versionId` field on the existing install request). **P12.7d (2026-08-26) is built, awaiting Cameron's verification** — the client detail sheet; found along the way that `ServerDTO` carries no Minecraft-version field, so the compatibility banner/badges thread it through from `GET /v1/components`'s existing `primaryComponent.installedVersion` instead (see its own entry for the full finding). **P12.7e (2026-08-26) is DONE** — installing a plugin through the (now-working) browser surfaced a real, pre-existing Phase 8 gap: installed add-ons never got a `currentVersion`, always reading "Unknown version," even though both values the oracle's fallback chain needs were already computed elsewhere in the codebase and simply never threaded through. **P12.7f (2026-08-26) is built, awaiting Cameron's verification** — his own UX preference, not an oracle port: the plugin row's persistent Toggle + Remove button is replaced with a click-anywhere action menu (Enable/Disable, View, Uninstall), with "View" opening `ProjectDetailSheet` for an already-installed add-on via a new, smaller `ProjectDetailItem` type instead of a contract change. **P12.9 (2026-08-27) is built, awaiting Cameron's verification** — hit the same "frozen contract, no backend" gap P12.3 hit for Players: `GET /v1/files`/`GET /v1/files/read` had DTOs in the contract but zero implementation anywhere. Cameron chose to build the backend inline rather than split into precursor steps (see its own entry for why); the step also adds a `PlatformAdapter.revealInFileManager` seam (new Tauri command, browser fallback) for Show in Finder, and explicitly leaves edit/save out of scope (no write route exists in the contract at all). Per this worktree's parallel-execution instructions, `App.svelte`'s section registry was left untouched (an explicit off-limits shared file) — the `files` entry still needs adding there before the tab is reachable; the exact snippet is in P12.9's own entry. **P12.10 (2026-08-27) is built, awaiting Cameron's verification** — see its own entry below: the real docked console lives in `ConsoleDock.svelte`, not the `Files:` line's legacy `src/lib/sections/console/` folder, and needed small additive `api`-prop wiring through `ApplicationShell.svelte`/`App.svelte` (cleared with Cameron first, since both are shared-foundation files) to reach the agent at all. **P12.4k (2026-08-27) is built, awaiting Cameron's verification** — brings `ServerEditorWorldTab.swift`'s Import ZIP / Replace World / Duplicate Slot into the Worlds tab per its own design reversal above; Import ZIP and Replace World are global header-row actions (they don't act on a selected card), while Duplicate is an inline per-card confirm (the backend names the copy "{name} copy" and takes no name argument, so no name-entry sheet was needed). Replace World's `newLevelName` has no client-readable source for Java (`GET /v1/settings` only exposes `level-name` as a field for Bedrock) — the sheet reads it back from settings when available and otherwise defaults to Minecraft's own "world" folder name, matching the oracle's own "read current value, pass it back unchanged" behavior as closely as the existing contract allows without a contract amendment. **P12.12 (2026-08-27) is built, awaiting Cameron's verification** — the Server Editor sheet (General, Broadcast), reached via a new `Edit…` action in `ManageSheet.svelte`. Found a real, systemic contract characteristic while building it: every route this tab touches except rename/eula/delete (`/v1/config/ram`, all of `/v1/broadcast/*`, `/v1/playit*`, `/v1/duckdns`, `/v1/resourcepacks*`) has no `serverId` parameter at all — each one resolves a single agent-wide "active server" server-side, so editing a card that isn't currently active would silently read/write the *wrong* server. Memory and the whole Broadcast tab are gated on a fresh `GET /v1/status` check and show a plain "Set as Active" affordance (reusing `ManageSheet`'s own `setActive`) instead of mutating blind. Also found several `ServerEditorGeneralTab.swift`/`ServerEditorBroadcastTab.swift` fields with no backing route anywhere in the contract despite the domain model already carrying them (`crates/msc-domain/src/app_config_schema.rs`'s `notes`, `auto_restart_on_crash`, `notification_prefs`) — left out rather than faked: Notes, Automation (auto-restart-on-crash), the four per-server notification toggles, the headless-script generator, Server Directory editing, Broadcast's IP Mode picker/host preview, and Reset Xbox Sign-In. Alt Account Profile's email/gamertag/password (`/v1/broadcast/credentials`) is POST-only in the contract, so those fields render blank every time the tab opens rather than pretending to show a stored value. Settings/JARs/Backups/World are confirmed absent as Editor tabs, matching this step's own design decision above. **P12.14 (2026-08-27) is built, awaiting Cameron's verification** — see its own entry below and the "MSC Settings scope" note just below for why it ships as one flat two-card sheet (Appearance, Open Server Folder) instead of MSC 1's three tabs: almost every other MSC 1 Settings card turned out either architecturally superseded by work already done in Phases 9–11 or genuinely never built, and Cameron chose live which of the never-built ones to keep on the list versus drop. **P12.15 (2026-08-27) is built, awaiting Cameron's verification** — the guided tour gets a real anchored spotlight overlay (`TourOverlay.svelte`), replacing Phase 11's plain centered-card placeholder. Found the oracle's 33-step tour is roughly forty percent unwalkable in the current build (no ported `AddServerWizardView`, no Packs tab); rather than edit the frozen oracle-faithful guide content, a new client-side filter (`applicableTourSteps`) keeps only steps whose anchor exists in this build, preserving the full sequence in the served data for whenever the wizard lands. Also found and fixed a real dead-end the naive port would have shipped: the `create` step is both `hideCard` and `requiresUserAction`, and checking `hideCard` first would leave no way to advance past it. See its own entry for the full accounting, including the EULA-location structural difference it surfaced but didn't try to fix. **P12.18a–i (2026-08-27, PLAN only, no code written) is the step list for a real port of MSC 1's `AddServerWizardView`** — Cameron compared MSC 1's actual 6-step wizard against MSC 2's placeholder (a name field + version dropdown) via screenshots and confirmed the placeholder isn't cutting it. Nine lettered steps (shell + Choose Path, Configure ×2 for Java/Bedrock, Network, World, Add-ons, Confirm + real Create, Import Existing, modpack-drop + tour-anchor wiring) — see the block above P12.17 for the full plan, the two scope questions Cameron already answered (fix the version-picker gap first — turned out already fixed, `GET /v1/versions/create` already exists and works; and cover Bedrock now, not deferred), and the antiAIslop redesign note for the wizard's selection cards. **P12.18a (2026-08-28) is built, awaiting Cameron's verification** — the wizard's outer shell and its real Choose Path step, replacing `ManageSheet.svelte`'s old inline name+version create form. Path cards and the step counter both keep the oracle's information shape but strip all accent color per this block's own antiAIslop note; every step past Choose Path is a placeholder until P12.18b–i land. See P12.18a's own entry below for the tour-anchor consequence this was expected to cause (three anchors on the removed inline form go missing until P12.18i rewires them onto the new wizard — `TourOverlay.svelte` already degrades a missing anchor to a plain dim card rather than erroring, so nothing breaks). **P12.18b (2026-08-28) is built, awaiting Cameron's verification** — the Configure step's Java branch: Server Type (Java/Bedrock) selector, Server Name, Server Software (Category then Flavor cards, ported from `JavaServerFlavor.swift`'s catalog since the contract exposes no flavor-list route), Source (Download latest vs. Choose version, reading the real `GET /v1/versions/create?serverType=java&javaFlavor=`), Crossplay, and Xbox Broadcast — all accumulating into a new `WizardDraft` object in `model.ts` rather than mutating anything yet. Picking Bedrock shows the same "lands in a later step" placeholder P12.18a already established, since P12.18c owns Bedrock's own fields. Found `Toggle.svelte` had no `disabled` prop (every other base form control -- Button, Select, Field, NumberField -- already has one); added it as a small additive fix, default `false`, no change to any existing caller, needed so the Crossplay toggle can go visually and functionally inert when Modded/Vanilla makes it unavailable. Not ported: the oracle's live Geyser/Floodgate and Xbox-broadcast-helper download-status text shown while this sheet is open -- that's MSC 1 pre-fetching local jars before the server exists, and the real create operation (P12.18g) is where this client/agent split actually installs them, so there's nothing to poll yet at Configure time. `npm run check`/`npm run test:unit` confirmed against a clean-checkout baseline: same 5 pre-existing type errors, same 2 pre-existing test failures (`tests/navigation/navigation.test.ts`, `tests/agent-install/agent-install.test.ts`, plus the file-level `tests/e2e/tauri-linux/native-renderer.test.ts` failure), nothing new. **Correction during the same visual-review round:** the Source picker's first version shipped as a native `<select>`, and for NeoForge/Forge -- which list every stable loader build per Minecraft version, not just the newest -- that produced a 50+ row OS-native dropdown covering most of the screen, with many rows showing an identical bare Minecraft version (e.g. several "26.2" rows) since the picker only read `VersionEntryDTO.displayLabel`, never `buildLabel` (the field that actually carries the distinguishing loader build, e.g. "NeoForge 26.2.15" -- confirmed against `neoforge_build_entries`/`forge_parse_maven_metadata` in `server_versions.rs`). Fixed by replacing the native select with a bounded, scrollable custom list (`max-height: 220px`), mirroring `components/VersionPickerSheet.svelte`'s own already-established `.list`/`.row` shape, and appending `buildLabel` to each row's label whenever the entry carries one. **Scope check asked and answered (2026-08-28):** Cameron initially expected build numbers on every flavor; checking `ServerJarProviders.swift`'s own `listVersions()` for all six found that MSC 1 itself only ever surfaces one on Paper (a real build number, already wired in this port via `paper_version_entry_from_builds`, pre-dating this step) and NeoForge/Forge (the loader build, this step's own fix) -- Purpur, Vanilla, and Fabric all hardcode `buildLabel: nil` in the oracle's own picker, even though Purpur's API exposes a real build number elsewhere and Fabric resolves a real loader version at download time; neither is surfaced as a separate list choice there. Asked Cameron directly rather than assuming: he chose to match the oracle exactly, so Purpur/Vanilla/Fabric stay bare version numbers -- no further change from this step. **P12.18c (2026-08-28) is built, awaiting Cameron's verification** — the Configure step's Bedrock branch (Bedrock Version, Max Players, Xbox Broadcast always available), additive to P12.18b's same component. Found this step's own plan text called for a live `GET /v1/versions/create?serverType=bedrock` fetch, but the real route unconditionally returns a compile-time-constant "not supported yet" response for that query (no server exists during Configure, so there's no runtime to verify a distribution against) — confirmed against the route's own test. Rendered that same note as static copy instead of fetching it, and ported the oracle's own free-text Bedrock Version field (default "LATEST") rather than a picker the route can never populate pre-creation. See P12.18c's own entry below for the full finding. **P12.18d (2026-08-28) is built, awaiting Cameron's verification** — the Network step (Port Forwarding vs Tunnel(playit.gg), context-varying port fields), wired in at step 3 of the Fresh path; built generically off `WizardDraft` so P12.18h's Import path can reuse it unchanged. Found this step's own plan text called for linking the already-built router port-forward guide content, but P12.16 (the step that owns that content) hasn't landed and there's no pre-P12.16 location either — the frozen contract's `RouterGuideCatalogDTO` is still an empty placeholder schema. Left the guide link out entirely rather than pointing it at nothing; see P12.18d's own entry for the full finding. **P12.18e (2026-08-28) is built, awaiting Cameron's verification** — the World step (World Source segmented control, New World field set: name/difficulty/game mode/seed), wired in at step 4 of the Fresh path. Found the oracle's third World Source option, an existing world folder, has no backing mechanism anywhere in this contract or codebase — the exact gap `worlds/ReplaceWorldSheet.svelte` already hit and dropped for its own equivalent picker ("a browser file picker has no folder-to-archive equivalent"); this step follows that same precedent and offers only New World / From Backup (.zip), with the backup path staging a real file via the same `api.upload('world-import', …)` call `ImportWorldZipSheet.svelte` already uses. Verified live with Playwright against the existing `tests/e2e/browser/contract-harness.mjs` fake-agent harness, not just read — see P12.18e's own entry for the full walkthrough and findings. **P12.18f (2026-08-28) is built, awaiting Cameron's verification** — the Add-ons step (Fresh/Java, between World and Confirm), shown only when the chosen flavor accepts add-ons. Its first commit found the plan's assumption of reusing `PluginBrowserSheet.svelte`/`ImportModpackSheet.svelte` as-is doesn't hold (both routes they call are hard-scoped to "the active server" with no pre-create mode) and shipped a narrowed version — Browse Modrinth dropped entirely, only modpack-archive staging for mod-kind flavors. **Cameron asked for real MSC 1 parity instead** ("you should also be able to search modrinth or add your own... with msc2"), so the same day's second commit fixes it for real: `GET /v1/catalog/search` gained additive `javaFlavor`/`minecraftVersion` query params so it can search without an active server at all, `PluginBrowserSheet.svelte`/`ProjectDetailSheet.svelte` gained an additive `mode="stage"` (mirroring the oracle's own `ModrinthBrowserView` reuse via `onAddToStaging`), and a new `WizardDraft.pendingAddOns` holds individual picks (catalog or local `.jar`) for P12.18g to install one by one right after the real server exists — `POST /v1/components/install` itself stays untouched, still active-server-scoped, called only post-create. Browse Modrinth and "Add your own" now work for both plugin- and mod-kind flavors; Import Modpack stays mod-kind only. See P12.18f's own entry for the full finding, the contract amendment, and live Playwright evidence for both flavors. **P12.18g (2026-08-28) is built, awaiting Cameron's verification** — the Confirm step and the real `POST /v1/servers/create` call: this is the step where the Fresh path becomes fully functional end to end, creating a real server, redeeming any staged world backup (P12.18e) and pending add-ons (P12.18f) once it exists. Found and fixed a real response-shape bug in the browser test harness along the way (`/v1/worlds/import`'s stub didn't match `WorldMutationResultDTO`). See P12.18g's own entry below for the full finding, the world-backup-redemption sequencing, and live Playwright evidence across Java+plugin, Bedrock, and world-backup-import walks. **P12.18h (2026-08-28) is built, awaiting Cameron's verification** — the Import Existing path's own Upload (real `POST /v1/servers/import` scan) and Review (editable Max Players/EULA/world picker) steps, reusing P12.18d's Network step and P12.18g's Confirm pattern (a `path`-gated branch on the same component) unchanged. Found and added a real, missing platform primitive (`PlatformAdapter.onFileDrop`, Tauri-only real drag-and-drop paths) and a real backend gap this path's own reuse of Network surfaces (`enablePlayit` is silently dropped by `import_raw`, unlike Fresh's create path) — see P12.18h's own entry below for both, plus why this step's live-Playwright verification attempt hit the same pre-existing first-launch-gate sandbox flakiness P12.18g already found and declined to chase. **P12.18j (2026-08-28) is built, awaiting Cameron's verification** — the small mechanical fix for the `enablePlayit` gap P12.18h found: `RawImportOverrides` now threads it into `ConfigServer.playit_enabled` for an imported server, same as Fresh create and the oracle's own `importExistingServer`. Backend-only, no client changes needed. Investigating this surfaced a second, much bigger, pre-existing gap not specific to Import at all — no route anywhere in MSC 2 sets the actual playit.gg secret key, so `/v1/playit/start` always fails `no_secret_key` regardless of path, even for a freshly created server. **P12.20/P12.20a/P12.20b (2026-08-28, PLAN only, no code written)** schedule the real fix: Cameron chose a simple paste-in secret-key field (matching the existing CurseForge API-key pattern) over porting MSC 1's full native sign-in/claim flow, which stays a documented, deliberately deferred enhancement. See P12.20's own entry for the full accounting of what Phase 9 already built versus what's actually missing. **P12.16 (2026-08-28) is built, awaiting Cameron's verification** — Guides/Handbook/How MSC Works rebuilt to S0, split into `HandbookBrowser`/`ConceptGuidePanel`/`ConceptDiagram`/`RouterGuidePanel` behind a `SegmentedControl`; see its own entry for the real gaps found (a stale `--msc-*` presentation layer over an already-real 31-topic/14-guide content corpus, a tour-anchor-filtering inconsistency, dead debug markup, and a pre-existing unrelated `workflows.spec.ts` e2e breakage flagged but not chased). **Same-step follow-up (2026-08-28):** Cameron's visual review found the Handbook's *content*, not just its presentation, was ~90-95% compressed against the oracle across all 31 topics; Codex restored it (schema + verbatim content, commit `7a82f74`) and this session built the frontend to render it (commit `728492e`) — see P12.16's own entry for the full accounting, including a correction to this entry's earlier "no backing field" claim about the router-guide confidence badge, and Router Guide/Concept Guide's own (smaller, differently-shaped) versions of the same gap, deliberately deferred to a later pass. **P12.16a (2026-08-28) is DONE** — Codex restored the full MSC 1 router-guide content into the already-ported-but-unwired composer/matcher/troubleshooting/runtime-resolver engines and wired four real routes (`GET /v1/guides/router-catalog` upgraded in place to the rich shape, plus new `GET /v1/guides/router/search`, `GET /v1/guides/router/{guideId}`, `POST /v1/guides/router/troubleshooting/analyze`); its own `cargo nextest` Verify lines are green. **P12.16b (2026-08-28) is built, awaiting Cameron's verification** — rebuilt `RouterGuidePanel.svelte` from its flat picker/steps/topics stub into the oracle's real 3-screen depth (picker → 7-section guide reader → symptom-driven troubleshooting) against P12.16a's routes, flat/no-color per antiAIslop.md and P12.16's own callout precedent; see its own entry for a correction to its plan text (no separate symptom-copy file was needed — P12.16a already threads that text through the existing catalog route) and its Playwright-walk verification. **P12.16c (2026-08-28) is DONE** — Cameron's real-agent verification of P12.16b hit every guide hanging on "Loading guide…" forever; traced (after ruling out a stale binary, including a `Repair service` cycle that didn't fix it) to a real serde bug in P12.16a's `ResolvedItemDto`: an enum-level `rename_all = "camelCase"` renames variant tags only, not struct-variant fields, so `alternateMenuNames`/`alternateTerms`/`suggestedNextActions` wired as snake_case and the client's `.length` access on the (then-`undefined`) camelCase field threw mid-render — Svelte 5 just leaves the stale loading DOM in place rather than visibly crashing. Fixed with three explicit `#[serde(rename = ...)]` overrides (P12.7b's own precedent for this exact bug class) plus a regression assertion; see its own entry for the full live-debugging trail.
> **P12.3 blocked on missing backend (decided 2026-08-25):** before rebuilding the Players tab, Cameron flagged that MSC 1's Players tab includes a read-only Java player inventory/stats viewer (`PlayerNBTReader.swift` + `PlayerInventoryView.swift`, hosted in `PlayerProfileDetailSheet.swift`) that never made it past the file-inventory audit into an actual phase step — no domain crate, no API route, and P12.3's own `What:` line never mentioned it. Investigation found `GET /v1/players/profiles` is **already frozen in the API contract** (`docs/msc2/api-contract/openapi.json`: `PlayerProfileDTO`/`PlayerStatsDTO`/`InventoryItemDTO`, plus `POST /v1/players/hidden`, `POST /v1/players/skin-override`, `GET /v1/players/{profileId}/skin`) but has **no handler at all** — today `GET /v1/players` only serves Bedrock (`crates/msc-agent/src/routes/bedrock.rs`; a Java server gets `note: "not_bedrock"`, empty list). This is a straight port against an already-frozen contract, not new API design. Cameron chose to block P12.3 and build the backend first (steps P12.2b–P12.2j below) rather than ship Players tab without it. Online Now / Seen This Session / Session Log are unaffected — those are console-derived (already built in P11.11) and stay in P12.3 itself. **Mutation actions, decided 2026-08-25:** of MSC 1's 5 player-data mutation actions (migrate to offline UUID, migrate to manual UUID, copy, duplicate, delete), none were in the frozen contract. Cameron chose **4 of the 5** — delete, migrate-to-offline-UUID, migrate-to-custom-UUID, and duplicate — added as new steps P12.2g (contract amendment) through P12.2j (route wiring), fully specified (exact DTO field names/types, exact error codes, pinned known-answer test vectors for the offline-UUID algorithm) since Cameron is running these with Codex. `copyPlayerData` (overwrite one player's data onto another's) is the one action still **deferred, not dropped** — add it later as its own contract-amendment step when wanted.
> **Phase 11 → 12 sequencing (decided 2026-08-25):** the committed P11.28g–j agent work is done and carries forward as Phase 12's foundation. The two unfinished Phase 11 steps — P11.28k and the P11.29 gate — are **superseded and folded into P12.17**, because they verify the first-launch UI and MSC 1 fidelity that only the redesign delivers; the whole client gate now runs once against the redesigned client. Phase 12 begins now.
> **Java tab decision (decided 2026-08-27):** reopens P12.12's closed scope. Reviewing the oracle showed MSC 1's Java executable path/Detect/Install Java… (`PreferencesJavaSection` in `MSCSettingsSections.swift`, reused by the Setup Wizard) lives in the app-wide `MSCSettingsView` Preferences window, editing one global `AppConfig.javaPath` used by every Java server — never a per-server screen. Cameron chose to surface that same global value in Server Editor instead, as a new third **Java** tab shown only for Java servers, because that's the moment it's actually needed — not a per-server value, just a per-server-relevant place to edit it. Added as **P12.12a** (built) and, after Cameron's visual review found three real gaps/fixes, corrected by **P12.12b** (adds Extra JVM flags, drops the host-wide banner, fixes per-tab sheet resizing) — both **awaiting Cameron's verification**. P12.14's future MSC Settings scope is narrowed accordingly (see its own entry) so the same global state isn't drafted from two independent screens.
> **MSC Settings scope (decided 2026-08-27):** reopens/narrows P12.14's original three-tab scope. Checking every remaining MSC 1 `MSCSettingsView` card against the frozen API contract before building found four cards architecturally superseded by work Phases 9–11 already did (Remote Access → named tokens + Tailscale, D-012; Bedrock Runtime toggle → automatic per-host detection, Phase 10; CurseForge API Key → D-027's manual-download import; Process Management → the agent is itself the OS-managed persistent service) and four more with no route in the contract at all and no architectural replacement (Config Recovery, Storage, Archives, Network Ports). Asked Cameron directly rather than deciding alone: he chose to drop Config Recovery and Archives, keep Storage and Network Ports deferred for a later contract-amendment step, and ship now with just the two cards that are genuinely real — Appearance and Open Server Folder — as one flat sheet with no tab bar. See P12.14's own entry for the full accounting.
> **Fresh-install reset decision (approved 2026-08-28):** MSC 2 will distinguish **Reset this client** from **Reset this host**. Client reset clears only local host records, credentials, preferences, and onboarding state. Host reset is an authenticated, host-owned, operation-backed destructive action with configuration-only and full-delete modes. A full reset on the local host also removes the local agent service so reopening the desktop shows **Install and Continue**; that action reinstalls the service, bootstraps a new local credential, reconnects, and resumes first-time host setup. A reset performed from a different computer never controls that computer's service: remote hosts remain installed, old access is invalidated, and the client reconnects through a fresh pairing code generated on the host. First-time setup leads to first-server creation but never creates a server silently. The reset contract and recovery path are planned below as P12.19a–d; no code has been written for them yet.
> **Sidebar planned (2026-08-28, PLAN only, no code written):** Cameron flagged that the four collapsible sidebar sections (Console Access, How to Connect, Maintenance, Quick Commands — `SidebarView.swift`) never got their own Phase 12 step; `ControlSidebar.svelte` has carried real shape but "Rebuilt in a later Phase 12 step" placeholders for all four since P11.8. Investigation found the backend and client-side pieces are almost entirely already real, just not yet wired into the sidebar: How to Connect reuses Overview's own `/v1/connectivity`+`/v1/config/geyser` fetch and `ConnectionCard.svelte`'s row logic; Console Access (Xbox Broadcast quick control) reuses the routes and mutation logic `BroadcastTab.svelte` (P12.12) already built, plus the modded/vanilla crossplay-exclusion helper the creation wizard (P12.18b) already exports; Maintenance's Directory/Logs are the already-built `revealInFileManager` pattern over paths the client already knows; Quick Commands sends the exact console-command strings `AppViewModel+ServerControls.swift` already defines through the already-built `/v1/command` route. The one oracle button with no real backing, Archives (a JAR-archive browser), is dropped rather than newly built — Cameron already made that exact call when the same feature came up as an MSC Settings card during P12.14 planning. Split into **P12.21** (How to Connect + Maintenance — lower-risk, mostly reads) and **P12.22** (Console Access + Quick Commands — live mutations, needs one live confirmation of whether `/v1/command`'s documented Bedrock command translation actually covers whitelist/save-all); both can run as one BATCH EXECUTE conversation since neither is architecturally risky.
> **Last updated:** 2026-08-28 (P12.18j, P12.21, P12.22 built; P12.20/a/b planned)
> **World settings planned (2026-08-28, PLAN only, no code written):** P12.23–P12.29 below close the per-world ownership model, persistence and activation behavior, centralized safety checks, version/edition/flavor capability handling, shared Create Server + Worlds-tab UI, and the Handbook/help explanation. The final step is the completion gate for native world settings supported by MSC 2's current Minecraft-version floor; arbitrary third-party mod configuration remains outside the core contract and must never be silently presented as applied.
> **Bedrock runtime closeout planned (owner-directed 2026-08-29, PLAN only):** P12.30–P12.33 below finish the missing production wiring and packaging so Bedrock is runnable in theory on native Linux, native Windows, and the Intel-macOS Linux VM sidecar. This phase deliberately stops before Cameron's real Windows/macOS server run and before promoting compatibility-matrix cells to live-supported evidence.

**Previous phases (Setup through Phase 10) and their amendments have moved to `rolling-plan-archive.md`** to keep this file small. That archive is historical only — current status and active work stay here.

---

## How this document works

This is the **working state** of the build. The vision documents say where we're going; the port plan says in what order; this file says **where we actually are right now**.

Phases are fixed and come from `msc2-port-plan.md`. **Steps are written one phase at a time**, as we reach each phase — not up front. Phase 11 is active; P11.1 now awaits Cameron's verification.

Each phase runs the six-move loop in `CLAUDE.md`: Plan → Read → Execute → Verify → Review → Advance.

### Step format

Every step looks like this:

```
### P0.3 — Extract TPS parser fixtures
**Status:** not started | in progress | awaiting verification | DONE
**Files:** fixtures/tps/, tools/extract-fixtures/
**What:** Pull the 27 TPS test cases out of MSC 1's TpsMonitoringTests.swift
         into input/expected JSON pairs.
**Verify:** `ls fixtures/tps/*.json | wc -l` → 27
**Commit:** P0.3: extract TPS parser fixtures        <- the message, not a hash
```

Every step also carries a **Batch:** field, telling an agent whether it may be run unattended:

| Batch value | Meaning |
|---|---|
| `safe` | Mechanical, and its Verify is a script Cameron has already reviewed. Batch freely. |
| `stop-after` | Runnable in a batch, but the batch **ends here** — the result needs looking at before continuing. |
| `solo` | Judgment work or a new checker script. Run it alone. Its output needs a cross-check by the other agent before the phase closes. |

**Status is only moved to DONE by Cameron**, after he runs the Verify command himself. An agent may set it to *awaiting verification* and stop.

**A step whose Verify only counts things is `stop-after` at best.** Counting proves something exists, not that it is right.

---

## Phases

Gates are in `msc2-port-plan.md`. This is the map, not the detail.

| Phase | Name | State |
|---|---|---|
| **Setup** | Repo, docs, agent instructions, CI, editor config | complete |
| **0** | Freeze the baseline and build the harness | complete |
| 1 | Domain types and pure rules | complete |
| 2 | API contract and operation model | complete |
| 3 | Safety substrate | complete |
| 4 | Java lifecycle vertical slice | complete |
| 5 | Configuration and migration | complete |
| **6** | Worlds and backups | complete |
| **7** | Server families and provisioning | complete |
| **8** | Mods, plugins, modpacks | complete |
| **9** | Networking and helpers | complete |
| **10** | Bedrock runtimes | complete |
| **11** | Desktop and web clients | agent layer done (P11.28g–j); UI verification (P11.28k, P11.29) folded into Phase 12 |
| **12** | Client redesign (MSC 1 fidelity, refreshed) | **in progress — P12.2b–j, P12.3/a–e, P12.4a–j, P12.5, P12.6, P12.7a, and P12.7b DONE; P12.3f/g, P12.4, P12.7, P12.9, P12.12, P12.14, P12.15, P12.18a, P12.18b, P12.18c, P12.18d, P12.18e, P12.18f, P12.18g, P12.18h, P12.18j, P12.21, and P12.22 built, awaiting Cameron's verification; P12.18i (modpack-drop + tour anchors) planned 2026-08-27, not yet built; P12.20/a/b (playit.gg secret key) planned 2026-08-28, not yet built** |
| 13 | Terminal UI (deferred from v1) | not started |

---

## Phase 11 — Desktop and web clients

**Gate** (`msc2-port-plan.md` §3): one Svelte frontend is delivered through
both the Tauri desktop shell and the agent-served browser UI, built against
the proven API while preserving MSC 1's information architecture and design
language. `GET /v1/help/{helpId}` resolves the embedded handbook, concept, and
router-guide content, and clients consume that contract rather than carrying
their own divergent teaching text. UI completion never gates headless agent
correctness.

**Working exit criteria:** macOS, Windows, and Linux desktop builds and the
browser load the same generated frontend bundle and expose every Phase
11-owned capability that the connected agent advertises and the calling token
permits. All client state is host-keyed, the current host and active server are
always visible, reconnecting restores bounded console and operation state, and
dangerous actions remain explicit. TypeScript DTOs are generated from
`docs/msc2/api-contract/openapi.json`; no handwritten mirror DTOs are allowed.
The agent serves the same static bundle plus embedded educational content, and
the Desktop/Web column of `client-capability-matrix.csv` is updated with every
surface rather than filled in retrospectively. Browser cookie auth and Tauri
local/remote auth close Phase 11's remaining D-012 scope without weakening
Phase 9's loopback-by-default, explicit-Tailscale-only posture. Linux proof
must launch the real Tauri binary through WebKitGTK under a display server and
interact with it; a Chromium-only browser run is not Linux desktop evidence.
The gate also requires fresh-profile evidence for MSC 1's first-launch
experience: setup sheet, Concept Guide, Handbook handoff, guided tour ordering
and anchors, skip/reopen state, splash playback or fallback, reduced-motion
behavior, and the boundary between agent-owned first-server initiation and
client-owned presentation.

**Extensibility boundary:** navigation and routing are a registry of section
descriptors keyed by stable strings and capability predicates, not a closed
enum, exhaustive switch, or fixed tab array. The route families
`/hosts/:hostId/servers/:serverId/bedrock/*` and
`/hosts/:hostId/servers/:serverId/profiles/*` are reserved without shipping
either screen. A later Bedrock client group registers its sections only when
`GET /v1/capabilities` advertises a usable Bedrock backend; it never infers support
from the host OS or client build. A later player-profiles phase must first port
the ledgered agent workflows (profile loads, Mojang/Floodgate resolution,
manual Bedrock identification, UUID migration/data mutation, hidden profiles,
and skin storage/serving), extend the public contract and capability response,
regenerate TypeScript, then register its section. Phase 11's registry,
host/server-scoped route parameters, lazy section loading, permission filters,
and unknown-capability tolerance are the seam that later phase consumes; none
of that work should require replacing Phase 11 navigation.

**Execution order:** no Phase 11 step is blocked on ownership of `crates/`,
the shared contract, CI, or packaging. P11.1–P11.19 build and prove the common
client foundation and non-Bedrock surfaces. P11.20 consumes the completed
Bedrock capability/runtime-state contract to prove the extension seam without
shipping Bedrock screens.
P11.21–P11.29 may then change shared agent/auth/packaging code and close the
gate. Step-to-step dependencies still apply, and a batch must never continue
after a failed Verify.

**Owner choices confirmed during PLAN (2026-08-22):** general-LAN management
remains unavailable for v1. Browser management stays on loopback or an
explicitly configured Tailscale path, and Tailscale never replaces
authentication or permission checks; Phase 11 does not build a local
certificate authority or ask users to bypass browser certificate warnings.
On macOS and Windows, MSC may download and verify a coordinated desktop,
agent, and compatible sidecar update, but it asks before installation; it does
not silently install automatically. Linux update installation remains owned by
the package manager, with MSC limited to an actionable availability notice.

### Group A — Shared client foundation and non-Bedrock surfaces (in progress)

### P11.1 — Scope the client rebuild from the iOS oracle and capability matrix
**Status:** DONE
**Files:** `docs/msc2/clients/phase11-scope.md`, `docs/msc2/audit/msc2-symbol-ledger.csv`, `tools/phase11/scope-check.py`, `docs/msc2/rolling-plan.md`
**What:** Read all 53 copied iOS client files as the primary behavioral and screen-structure reference, then use the MSC 1 macOS views only for the desktop information architecture and visual language. Map every current OpenAPI/WebSocket operation and matrix row to a Phase 11 screen, shared infrastructure, honest future state, or explicitly out-of-scope agent gap. Record the D-003 same-screen rule, D-013 host scoping, D-023 matrix-update rule, D-026 help ownership, D-021 client resource bounds, and the exact Bedrock/player-profile extensibility handoffs above. Do not assign the player-profile agent rows to Phase 11 merely because their old DTOs remain in the frozen baseline.
**Verify:** `python3 tools/phase11/scope-check.py docs/msc2/clients/phase11-scope.md docs/msc2/client-capability-matrix.csv`
**Commit:** `P11.1: scope desktop and web clients`
**Batch:** solo

### P11.2 — Scaffold one standalone Svelte and Tauri client
**Status:** DONE
**Files:** `clients/desktop-web/package.json`, `clients/desktop-web/package-lock.json`, `clients/desktop-web/src/`, `clients/desktop-web/static/`, `clients/desktop-web/src-tauri/`, `clients/desktop-web/svelte.config.js`, `clients/desktop-web/vite.config.ts`, `clients/desktop-web/tsconfig.json`
**What:** Create one TypeScript Svelte frontend with a static build suitable for both agent serving and a thin Tauri 2 shell. Keep `src-tauri` standalone from the root Cargo workspace, with its own lockfile and no server-management behavior, so the existing headless Rust workspace does not acquire GUI build dependencies. Establish formatting, type-checking, unit-test, production-build, and bundle-identity commands.
**Verify:** `npm --prefix clients/desktop-web run verify:scaffold`
**Commit:** `P11.2: scaffold shared Svelte client`
**Batch:** solo

### P11.3 — Generate TypeScript from the frozen OpenAPI contract
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/api/generated.ts`, `clients/desktop-web/src/lib/api/generate.ts`, `clients/desktop-web/package.json`, `clients/desktop-web/package-lock.json`, `tools/phase11/generated-types-check.py`
**What:** Generate the HTTP request/response type surface directly from the current frozen `docs/msc2/api-contract/openapi.json`, preserving optional/additive fields needed for D-010 skew, including `BedrockRuntimeStateDTO` even though Phase 11 ships no Bedrock screens. Make regeneration deterministic and fail when checked-in output differs from the contract. Handwritten transport helpers may wrap generated types, but no hand-authored DTO mirror is permitted.
**Verify:** `npm --prefix clients/desktop-web run api:check`
**Commit:** `P11.3: generate TypeScript API types`
**Batch:** solo

### P11.4 — Build the contract-backed client test harness
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/testing/`, `clients/desktop-web/tests/contract/`, `clients/desktop-web/tests/fixtures/`, `clients/desktop-web/package.json`, `clients/desktop-web/package-lock.json`
**What:** Add deterministic fake HTTP, WebSocket, upload/download, operation, auth, capability, permission, old-agent/new-agent, and reconnect scenarios using generated DTO shapes. Include unknown optional fields and absent future capability keys so the UI proves additive skew tolerance. This becomes the reviewed test boundary later `safe` screen batches use; it must not invent Bedrock screens or player-profile behavior. The separate P11.20 seam test consumes the real Bedrock capability/runtime-state contract.
**Verify:** `npm --prefix clients/desktop-web run test:contract`
**Commit:** `P11.4: add client contract harness`
**Batch:** solo

### P11.5 — Establish extensible information architecture and routing
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/navigation/`, `clients/desktop-web/src/routes/`, `clients/desktop-web/tests/navigation/`, `docs/msc2/clients/phase11-scope.md`
**What:** Implement the descriptor registry, nested host/server route parameters, permission and capability predicates, lazy component loading, stable deep links, narrow/wide layouts, and unknown-section fallback. Prohibit a closed section enum, exhaustive section switch, fixed tab-count assumptions, and checks such as `hostOs == linux` standing in for capability discovery. Reserve but do not register or render Bedrock and player-profile route families; tests must prove a synthetic future descriptor can be added without editing the shell/router and remains hidden until its named advertised capability is present.
**Verify:** `npm --prefix clients/desktop-web run test:navigation`
**Commit:** `P11.5: add extensible client routing`
**Batch:** solo

### P11.6 — Make all connection and cache state host-scoped
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/hosts/`, `clients/desktop-web/src/lib/stores/`, `clients/desktop-web/tests/hosts/`
**What:** Implement D-013's host registry, minimal host switcher, per-host connection/capability/permission/server/console/operation caches, active-server selection, stale-data isolation, and explicit host identity on every destructive confirmation. Credentials remain behind an injected credential adapter so the browser and Tauri mechanisms can land later without migrating store shapes. No singleton active host, global console buffer, or credential field may leak across hosts.
**Verify:** `npm --prefix clients/desktop-web run test:hosts`
**Commit:** `P11.6: add host-scoped client state`
**Batch:** safe

### P11.7 — Implement the generated HTTP and resilient stream client
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/api/`, `clients/desktop-web/src/lib/streams/`, `clients/desktop-web/src/lib/operations/`, `clients/desktop-web/tests/transport/`
**What:** Build one host-aware transport over generated request/response types, `ErrorDTO`, version headers, capability refresh, bounded staged transfers, and cookie-or-bearer credential adapters. Add console, operation, and notification stream reconnect with bounded history, deduplication, cancellation, terminal-state recovery, and explicit unsupported/old-client states. Keep browser and desktop on the same calls; shell IPC may supply credentials or native services but never an alternative management API.
**Verify:** `npm --prefix clients/desktop-web run test:transport`
**Commit:** `P11.7: build shared API transport`
**Batch:** safe

### P11.8 — Build the responsive MSC design system and application shell
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/components/`, `clients/desktop-web/src/lib/styles/`, `clients/desktop-web/src/routes/+layout.svelte`, `clients/desktop-web/tests/visual/`
**What:** Translate the copied iOS component structure and MSC 1 macOS design language into reusable tokens, cards, tables, forms, dialogs, alerts, empty/loading/error states, keyboard focus, reduced motion, and responsive sidebar/bottom-navigation shells. Preserve desktop's server-list/sidebar and always-available console concepts without baking today's section count into layout. Include the client-owned first-launch/splash seam needed for the setup sheet, Concept Guide, Handbook handoff, guided-tour overlay, and animation/fallback behavior; do not replace that sequence with a generic welcome screen. The shell must visibly name the selected host and server and remain usable at phone, tablet, and desktop widths.
**Verify:** `npm --prefix clients/desktop-web run test:visual-shell`
**Commit:** `P11.8: build shared MSC interface shell`
**Batch:** stop-after

### P11.9 — Build fleet, provisioning, and lifecycle workflows
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/home/`, `clients/desktop-web/src/lib/sections/fleet/`, `clients/desktop-web/tests/screens/fleet.test.ts`, `docs/msc2/client-capability-matrix.csv`
**What:** Implement status, active-server switching, create/import/rename/delete/EULA, Java family/version/runtime selection and install, templates, start/stop/restart, clear confirmations, capability/permission gates, and durable operation progress. Use the iOS create/import flows as the functional reference and desktop macOS views for hierarchy only. Update each delivered Desktop/Web matrix row in this same step; unsupported or agent-Planned routes stay `Planned`, never implied by a disabled decorative control.
**Verify:** `npm --prefix clients/desktop-web run test:screen-fleet && python3 tools/phase6/capability-matrix-check.py docs/msc2/client-capability-matrix.csv`
**Commit:** `P11.9: add fleet and lifecycle screens`
**Batch:** safe

### P11.10 — Build console, commands, operations, notifications, and performance
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/console/`, `clients/desktop-web/src/lib/sections/performance/`, `clients/desktop-web/src/lib/components/operations/`, `clients/desktop-web/src/lib/components/notifications/`, `clients/desktop-web/tests/screens/live.test.ts`, `docs/msc2/client-capability-matrix.csv`
**What:** Implement the bounded live console with history/search/filter/pause/copy/clear-local-view, command history/favorites, operation progress/cancel/recovery, notification feed, performance metrics/charts, help affordances, and reconnect behavior. Use DOM/SVG/CSS rendering with a low-cost fallback rather than assuming Chromium-only WebGL/canvas behavior. Update the matching Desktop/Web matrix cells.
**Verify:** `npm --prefix clients/desktop-web run test:screen-live && python3 tools/phase6/capability-matrix-check.py docs/msc2/client-capability-matrix.csv`
**Commit:** `P11.10: add live server screens`
**Batch:** safe

### P11.11 — Build the online roster without claiming player profiles
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/players-online/`, `clients/desktop-web/tests/screens/players-online.test.ts`, `docs/msc2/client-capability-matrix.csv`
**What:** Render only the generic online roster the connected agent actually advertises. Do not build the Bedrock allowlist/permissions UI in this phase. Keep the registered section identity distinct from the reserved future `profiles` route; do not call the frozen-but-unimplemented profile, skin, hidden-profile, session-history, UUID migration, or player-data mutation routes and do not present their matrix cells as implemented. Prove the online section still works when profile capability fields are unknown, absent, or later added.
**Verify:** `npm --prefix clients/desktop-web run test:screen-players-online && python3 tools/phase6/capability-matrix-check.py docs/msc2/client-capability-matrix.csv`
**Commit:** `P11.11: add online player roster`
**Batch:** safe

### P11.12 — Build worlds, backups, and staged transfer workflows
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/worlds/`, `clients/desktop-web/src/lib/sections/backups/`, `clients/desktop-web/src/lib/transfers/`, `clients/desktop-web/tests/screens/worlds-backups.test.ts`, `docs/msc2/client-capability-matrix.csv`
**What:** Implement slot inventory/activation/create/rename/duplicate/delete/import/export/convert, direct active-world mutations where the API supports them, thumbnails, backup create/config/delete/restore, bounded uploads/downloads, transactional warnings, progress/cancel/recovery, and risk-appropriate confirmations. Update every genuinely delivered Desktop/Web matrix cell and leave unavailable agent paths visible only as truthful capability explanations.
**Verify:** `npm --prefix clients/desktop-web run test:screen-worlds-backups && python3 tools/phase6/capability-matrix-check.py docs/msc2/client-capability-matrix.csv`
**Commit:** `P11.12: add world and backup screens`
**Batch:** safe

### P11.13 — Build add-on, modpack, and component workflows
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/addons/`, `clients/desktop-web/src/lib/sections/components/`, `clients/desktop-web/tests/screens/addons.test.ts`, `docs/msc2/client-capability-matrix.csv`
**What:** Implement installed add-ons, catalog search, install/update/toggle/remove/source actions, system-component state, client export, modpack inspect/import/replace, and D-027 manual browser-download then bounded staged-upload completion. Preserve provider-unavailable, dependency, pack-managed, cancellation, and provenance explanations. Update the matching Desktop/Web matrix rows; never hardcode provider or server-family lists where the contract supplies them.
**Verify:** `npm --prefix clients/desktop-web run test:screen-addons && python3 tools/phase6/capability-matrix-check.py docs/msc2/client-capability-matrix.csv`
**Commit:** `P11.13: add add-on and modpack screens`
**Batch:** safe

### P11.14 — Build settings, health, networking, helpers, and access administration
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/settings/`, `clients/desktop-web/src/lib/sections/health/`, `clients/desktop-web/src/lib/sections/connectivity/`, `clients/desktop-web/src/lib/sections/access/`, `clients/desktop-web/tests/screens/administration.test.ts`, `docs/msc2/client-capability-matrix.csv`
**What:** Render schema-driven settings without a client-side field enum; implement health cards/problems/repairs, RAM/Java/Geyser, connectivity diagnostics, Playit, DuckDNS, Xbox Broadcast, resource packs, and named-token create/update/revoke with one-time-secret handling. Permission and capability filters must remove unavailable actions while keeping explanations. Agent-Planned files/watchdog/profile routes remain Planned rather than receiving fake screens. Update each delivered matrix row.
**Verify:** `npm --prefix clients/desktop-web run test:screen-administration && python3 tools/phase6/capability-matrix-check.py docs/msc2/client-capability-matrix.csv`
**Commit:** `P11.14: add administration screens`
**Batch:** safe

### terra clean up

#### Review findings

- The original P11.5–P11.8 foundations were not connected to the running
  shell: it selected hard-coded sections and a hard-coded host/server instead
  of loading the token's permissions and the host's advertised capabilities.
- Reconnecting streams changed label but never scheduled a retry, and staged
  downloads had no client-side memory ceiling.
- The P11.11 roster requested `/v1/session-log` and presented a join/leave
  history even though that route is deliberately deferred with player profiles.
- The capability matrix overstated deferred Bedrock/world and shared-context
  routes as delivered. It also marked live WebSocket channels implemented while
  the shell still reads the bounded HTTP snapshots; those channels wait for the
  authenticated wiring in P11.21–P11.23. A rejected world mutation also
  changed local UI state, and destructive confirmations did not name their
  exact host and target.
- The focused tests covered helpers but did not protect all of those integration
  boundaries against regression.

#### Items completed in this cleanup

- Connect the shell to `/v1/capabilities` and `/v1/me`, use the descriptor
  registry to filter routes, and retain host/server identity in the route.
- Give streams bounded automatic reconnect attempts and give staged downloads a
  512 MiB client-memory ceiling.
- Remove the deferred session-history request and restore its matrix row to
  `Planned`.
- Correct the affected capability-matrix claims, preserve state after rejected
  world actions, name host plus target in destructive confirmations, and leave
  live streams honestly `Planned` until their authentication boundary exists.
- Add regression coverage for the stream/download and deferred-route boundaries.

### P11.15 — Extract and validate the educational content corpus
**Status:** DONE
**Files:** `content/help/`, `content/guides/`, `fixtures/help-content/`, `fixtures/onboarding/`, `tools/phase11/help-content-check.py`, `docs/msc2/clients/phase11-scope.md`
**What:** Extract MSC 1's 31-topic handbook, concept guide, router catalog/records/steps, troubleshooting content, and onboarding copy into the confirmed Markdown-with-YAML-front-matter and structured guide data formats. Preserve source citations and label content versus executable router rules; do not duplicate prose in Svelte. Record unresolved diagram assets honestly. Include the Concept Guide page order, onboarding step content/order, skip/reopen wording, and the source mapping for every first-launch explanation; visual anchoring and animation remain client-owned. Include coverage for every `helpId` already emitted by settings, health, diagnostics, performance, connectivity, and errors, including the `bedrock.runtime-unavailable` later-audit requirement. Add deterministic onboarding fixtures proving fresh-install, already-seen, skipped, reopened, and unknown/future-topic cases.
**Verify:** `python3 tools/phase11/help-content-check.py --all`
**Commit:** `P11.15: extract educational content`
**Batch:** solo

### P11.16 — Render contract-served help and guides in the shared client
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/help/`, `clients/desktop-web/src/lib/sections/handbook/`, `clients/desktop-web/tests/screens/help.test.ts`, `docs/msc2/client-capability-matrix.csv`
**What:** Build safe Markdown rendering, related-topic navigation, handbook/concept/router-guide readers, contextual `helpId` links, unknown-topic degradation, and client-owned onboarding anchors against the fake contract. Implement and test the fresh-install sequence of setup sheet → Concept Guide → guided tour, including the exact step ordering, user-action pauses, form-card hide/resume behavior, completion state, skip behavior, Handbook handoff, and reopen-from-preferences behavior. Implement the splash animation seam with a bounded fallback and reduced-motion path; preserve the source asset or record an explicit reviewed replacement. Every explanation comes from a response or structured content fixture, never a screen-local copy. The registry must allow future Bedrock and profile topics/sections to appear additively without new shell logic.
**Verify:** `npm --prefix clients/desktop-web run test:screen-help && python3 tools/phase11/help-content-check.py --client`
**Commit:** `P11.16: render shared help content`
**Batch:** safe

### P11.17 — Prove browser parity, accessibility, and responsive layouts
**Status:** DONE
**Files:** `clients/desktop-web/tests/e2e/browser/`, `clients/desktop-web/playwright.config.ts`, `clients/desktop-web/package.json`, `clients/desktop-web/package-lock.json`
**What:** Exercise the production static bundle against the contract harness at narrow and wide widths, keyboard-only navigation, reduced motion, destructive confirmations, host switching, reconnect, upload/download, and deep-link reload. Add a fresh-profile onboarding walkthrough proving setup completion, Concept Guide → tour sequencing, step ordering and anchors, user-action pauses, skip/resume/reopen flags, Handbook handoff, splash playback/fallback, and reduced-motion behavior. Run Chromium plus browser WebKit for fast compatibility feedback, while recording plainly that this is browser evidence and does not replace P11.19's native Linux WebKitGTK proof.
**Verify:** `npm --prefix clients/desktop-web run test:e2e-browser`
**Commit:** `P11.17: prove shared browser workflows`
**Batch:** stop-after

### P11.18 — Add the thin Tauri shell without desktop-only screens
**Status:** DONE
**Files:** `clients/desktop-web/src-tauri/`, `clients/desktop-web/src/lib/platform/`, `clients/desktop-web/tests/tauri/`
**What:** Load the exact production Svelte bundle and expose only narrow native adapters for credentials, file pickers, notifications, menus, window lifecycle, and later agent installation/update. Each native affordance must invoke a shared web workflow with a browser fallback; no route or screen may test `isTauri` to reveal desktop-only management behavior. Keep the standalone Tauri crate outside the root workspace so headless Rust builds do not acquire GUI dependencies.
**Verify:** `npm --prefix clients/desktop-web run test:tauri-boundary`
**Commit:** `P11.18: add thin Tauri shell`
**Batch:** solo

### P11.19 — Exercise the real Linux Tauri renderer through WebKitGTK
**Status:** DONE
**Files:** `clients/desktop-web/tests/e2e/tauri-linux/`, `clients/desktop-web/wdio.conf.ts`, `tools/phase11/linux-webkitgtk-smoke.sh`, `docs/msc2/clients/evidence/`
**What:** On a Debian/Ubuntu desktop runner with `libwebkit2gtk-4.1`, `webkit2gtk-driver`, and Xvfb, launch the built Tauri binary and drive its real window through the native WebDriver path. Verify visible shell, navigation, CSS layout, forms, dialogs, live-console fallback, deep links, one mutating fake workflow, and the fresh-profile onboarding entry path including the reduced-motion/fallback branch; record the WebKitGTK package/version and screenshot evidence. A Vite page opened in Chrome or Playwright's bundled WebKit does not satisfy this step.
**Verify:** `bash tools/phase11/linux-webkitgtk-smoke.sh --native`
**Commit:** `P11.19: prove Linux WebKitGTK rendering`
**Batch:** stop-after

### Group B — Bedrock extension seam (ready after Group A; no Bedrock screens)

### P11.20 — Prove capability-driven Bedrock extension seams
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/api/generated.ts`, `clients/desktop-web/src/lib/navigation/`, `clients/desktop-web/tests/navigation/bedrock-extension.test.ts`, `docs/msc2/client-capability-matrix.csv`, `docs/msc2/clients/phase11-scope.md`
**What:** Recheck generated TypeScript against the current frozen OpenAPI document and consume its finalized `serverTypes.bedrock` plus `BedrockRuntimeStateDTO` advertisement without hand-written Bedrock DTOs or host-OS inference. Use a test-only future section descriptor to prove Bedrock navigation is absent when unsupported, can be registered when capability state permits it, survives unknown backend/reason values additively, and fits existing layouts/routes without restructuring. Ship no Bedrock section, creation flow, settings, player, allowlist, world, backup, console, or runtime screen in Phase 11; keep those matrix cells Planned for the later Bedrock client group.
**Verify:** `npm --prefix clients/desktop-web run api:check && npm --prefix clients/desktop-web run test:bedrock-extension && python3 tools/phase6/capability-matrix-check.py docs/msc2/client-capability-matrix.csv`
**Commit:** `P11.20: preserve Bedrock client extension seam`
**Batch:** solo

### Group C — Shared agent, auth, packaging, and gate work (ready after Group B)

### P11.21 — Close the remaining desktop and browser authentication design
**Status:** DONE
**Files:** `docs/msc2/clients/phase11-auth.md`, `docs/msc2/msc2-decisions.md`, `docs/msc2/api-contract/openapi.json`, `docs/msc2/api-contract/auth-scope-phase2.md`, `docs/msc2/lifecycle/pairing-phase4.md`, `crates/msc-api/tests/phase11_auth_conformance.rs`
**What:** Turn D-012's approved mechanisms and Phase 9 posture into one testable contract: same-machine Tauri bootstrap resistant to arbitrary local-process impersonation, per-host remote desktop pairing and secret-store keys, browser pairing-to-httpOnly-SameSite cookie exchange, session revocation/expiry, exact allowed-origin/CSP rules, CSRF tokens for cookie-authenticated mutations, and bearer exemption. Preserve loopback-by-default management and authenticated explicit-Tailscale access; per the owner-confirmed v1 choice above, keep general-LAN management unavailable and do not build certificate provisioning or a local trust system. Use additive versioned routes and `ErrorDTO`; do not put raw credentials in Svelte-accessible storage or URLs.
**Verify:** `python3 tools/api-contract-check.py --v1-summary && cargo nextest run -p msc-api --test phase11_auth_conformance`
**Commit:** `P11.21: freeze desktop and browser auth`
**Batch:** solo

### P11.22 — Implement browser sessions, origin policy, CSP, and CSRF
**Status:** DONE
**Files:** `crates/msc-agent/src/auth/`, `crates/msc-agent/src/routes/browser_session.rs`, `crates/msc-agent/tests/browser_auth.rs`, `clients/desktop-web/src/lib/auth/browser.ts`, `clients/desktop-web/tests/auth/browser.test.ts`
**What:** Implement the frozen browser pairing/session path on the existing credential registry, with one-use challenges, httpOnly cookies, revocation and restart behavior, exact origin checks, restrictive CSP, CSRF on every cookie-authenticated mutation, rate limits, and audit attribution. Prove bearer clients remain unaffected and a hostile origin/local script cannot turn ambient browser authority into a server mutation.
**Verify:** `cargo nextest run -p msc-agent --test browser_auth && npm --prefix clients/desktop-web run test:auth-browser`
**Commit:** `P11.22: add secure browser sessions`
**Batch:** stop-after

### P11.23 — Implement local and remote Tauri credentials per host
**Status:** DONE
**Files:** `clients/desktop-web/src-tauri/`, `clients/desktop-web/src/lib/auth/desktop.ts`, `clients/desktop-web/tests/auth/desktop/`, `crates/msc-agent/src/auth/`, `crates/msc-agent/tests/desktop_auth.rs`
**What:** Implement the chosen same-machine authorization handshake and remote pairing exchange, storing one credential per agent host ID in the platform credential store through the shell so secrets never enter browser storage. Verify local convenience does not become loopback-open authorization, remote credentials obey permission/expiry/revocation, switching hosts cannot reuse another host's credential, and the web build retains its cookie flow with no divergent screen.
**Verify:** `cargo nextest run -p msc-agent --test desktop_auth && npm --prefix clients/desktop-web run test:auth-desktop`
**Commit:** `P11.23: add per-host desktop credentials`
**Batch:** stop-after

### P11.24 — Serve embedded help content and port router-guide rules
**Status:** DONE
**Files:** `crates/msc-domain/src/router_guides.rs`, `crates/msc-domain/tests/router_guides.rs`, `crates/msc-agent/src/help.rs`, `crates/msc-agent/src/routes/help.rs`, `crates/msc-agent/tests/help_routes.rs`, `content/help/`, `content/guides/`, `fixtures/help-content/`, `docs/msc2/api-contract/openapi.json`, `docs/msc2/client-capability-matrix.csv`
**What:** Embed the validated content corpus, implement `GET /v1/help/{helpId}` plus the additive handbook/concept/router-guide catalog routes required to browse it, and port the ledgered router matcher/fallback/composer/troubleshooting rules into Rust against fixtures. Explicitly include the `bedrock.runtime-unavailable` topic and its structured unavailable reasons. Return raw Markdown/structured steps for every client to render; unknown or version-new topics degrade through `ErrorDTO`. Do not absorb client onboarding anchors or presentation into the agent.
**Verify:** `cargo nextest run -p msc-domain --test router_guides && cargo nextest run -p msc-agent --test help_routes && npm --prefix clients/desktop-web run test:screen-help`
**Commit:** `P11.24: serve shared educational content`
**Batch:** solo

### P11.25 — Serve the same production bundle from the agent
**Status:** DONE
**Files:** `crates/msc-agent/src/web_ui.rs`, `crates/msc-agent/tests/web_ui.rs`, `clients/desktop-web/`, `tools/phase11/bundle-identity-check.py`
**What:** Embed or package the exact Svelte production output the Tauri shell loads, serve hashed assets with correct MIME/cache headers and CSP, support safe client-side deep-link fallback without shadowing `/v1`, and provide an explicit unavailable result when a headless package intentionally omits web assets. Prove byte identity between the browser-served and Tauri-loaded bundles and preserve D-011's no-GUI dependency boundary in the agent.
**Verify:** `python3 tools/phase11/bundle-identity-check.py && cargo nextest run -p msc-agent --test web_ui`
**Commit:** `P11.25: serve shared web bundle`
**Batch:** stop-after

### P11.26 — Install and manage the local agent through the shell
**Status:** DONE
**Files:** `clients/desktop-web/src-tauri/`, `clients/desktop-web/src/lib/setup/`, `clients/desktop-web/tests/agent-install/`, `tools/phase11/agent-install-smoke.sh`, `packaging/`
**What:** Add shell-only native commands behind shared setup screens to detect, install, start, stop, repair, and report the platform service and compatible agent/sidecar package. Closing the window must never stop the service or a server. Browser users see the same status/setup route with a truthful instruction/fallback when native install is unavailable; there is no desktop-only screen. Preserve existing service identity, privilege, headless, and rollback rules.
**Verify:** `bash tools/phase11/agent-install-smoke.sh --synthetic`
**Commit:** `P11.26: manage local agent installation`
**Batch:** stop-after

### P11.27 — Define and prove coordinated desktop, agent, and sidecar updates
**Status:** DONE
**Files:** `docs/msc2/clients/phase11-update.md`, `clients/desktop-web/src-tauri/`, `clients/desktop-web/src/lib/updates/`, `packaging/`, `tools/phase11/update-smoke.sh`
**What:** Implement the owner-confirmed prompted, signed macOS/Windows update policy as a compatibility-aware set: download and verify release identity before staging, ask before installation, keep the running agent until replacement is ready, preserve configuration/secrets/worlds, pair the exact compatible Bedrock sidecar where applicable, roll back a failed update, and allow app/agent version skew only within D-010's advertised window. Never install silently. Linux defers installation to its package manager and receives an actionable notice, not a second self-updater. Never merge MSC updates with server/loader/add-on update controls.
**Verify:** `bash tools/phase11/update-smoke.sh --synthetic --all-platforms`
**Commit:** `P11.27: add coordinated client updates`
**Batch:** solo

### P11.28 — Build and exercise desktop and web candidates on all three platforms
**Status:** DONE
**Files:** `.github/workflows/ci.yml`, `tools/phase11/desktop-web-smoke.sh`, `clients/desktop-web/`, `docs/msc2/clients/evidence/`
**What:** Add production frontend/type tests, agent-served browser smoke, and real Tauri builds to macOS, Windows, and Linux CI while preserving the headless no-GUI job. Exercise the same core workflow in browser and desktop modes; on Linux run P11.19's native WebKitGTK smoke, not a Chromium substitute. Record platform renderer/package versions and explicit unavailable signing/notarization evidence without claiming unperformed release distribution.
**Verify:** `bash tools/phase11/desktop-web-smoke.sh --synthetic --all-surfaces`
**Commit:** `P11.28: prove tri-platform clients`
**Batch:** stop-after

### P11.28a — Port the remaining MSC 1 first-time setup pages
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/SetupIntro.svelte`, `clients/desktop-web/src/lib/help/FirstLaunchGate.svelte`, `clients/desktop-web/tests/screens/help.test.ts`, `clients/desktop-web/tests/e2e/browser/`, `clients/desktop-web/tests/e2e/tauri-linux/`, `crates/msc-agent/src/routes/capabilities.rs`, `crates/msc-agent/src/routes/networking.rs`, `crates/msc-agent/src/routes/versions.rs`, `crates/msc-api/src/dto/`, `docs/msc2/api-contract/openapi.json`, `docs/msc2/client-capability-matrix.csv`, `differences.md`
**What:** Port MSC 1’s Server Setup, Playit.gg, Xbox Broadcast, Tailscale, and You’re All Set pages into the existing MSC 2 first-launch flow. Use real agent probes for the servers root, Java 21+, Bedrock runtime, Xbox helper, and Tailscale; keep optional services skippable; persist the selected server types; and hand off explicitly to first-server creation through the existing Concept Guide.
**Verify:** `npm --prefix clients/desktop-web run test:screen-help && npm --prefix clients/desktop-web run build && npm --prefix clients/desktop-web run api:generate -- --check && cargo check -p msc-api -p msc-agent && cargo nextest run -p msc-api --test dto_conformance`
**Commit:** `P11.28a: port remaining first-time setup pages`
**Batch:** stop-after

### P11.28b — Repair first-time setup controls and host probes
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/SetupIntro.svelte`, `clients/desktop-web/src/lib/platform/`, `clients/desktop-web/src-tauri/src/lib.rs`, `clients/desktop-web/tests/tauri/platform-boundary.test.ts`
**What:** Keep Bedrock selectable when the selected built-in runtime is in `provisioning_required`, open setup links through the operating system’s default browser in Tauri, verify the Xbox helper after download by re-reading the agent’s status and filename, and make Tailscale checks visibly report Checking, installed, not installed, or unavailable.
**Verify:** `npm --prefix clients/desktop-web run test:screen-help && npm --prefix clients/desktop-web run test:tauri-boundary && npm --prefix clients/desktop-web run build && cargo check -p msc-api -p msc-agent && cargo check --manifest-path clients/desktop-web/src-tauri/Cargo.toml`
**Commit:** `P11.28b: repair first-time setup controls`
**Batch:** stop-after

### P11.28c — Add native setup pickers and Java verification
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/SetupIntro.svelte`, `clients/desktop-web/src/lib/platform/`, `clients/desktop-web/tests/screens/help.test.ts`, `clients/desktop-web/tests/tauri/platform-boundary.test.ts`, `crates/msc-agent/src/routes/versions.rs`
**What:** Use the native Tauri dialog for the servers-root folder and Java executable Browse actions, retain a manual path fallback in browser mode, and include manually configured Java outside standard search roots in the agent’s real version probe so Check for Java and Use PATH report truthfully.
**Verify:** `npm --prefix clients/desktop-web run test:screen-help && npm --prefix clients/desktop-web run test:tauri-boundary && npm --prefix clients/desktop-web run build && cargo check -p msc-api -p msc-agent && cargo check --manifest-path clients/desktop-web/src-tauri/Cargo.toml`
**Commit:** `P11.28c: add native setup pickers and Java verification`
**Batch:** stop-after

### P11.28d — Make Xbox helper verification stateful in the test host
**Status:** awaiting verification
**Files:** `clients/desktop-web/tests/e2e/browser/contract-harness.mjs`, `clients/desktop-web/tests/e2e/browser/workflows.spec.ts`, `clients/desktop-web/playwright.config.ts`
**What:** Keep the deterministic browser/Tauri test host’s Xbox helper status consistent with its download endpoint, return the helper filename, and exercise the setup flow through the verified-download message so a successful fake download cannot be reported as missing. This remains test-only and must not become a production host.
**Verify:** `npm --prefix clients/desktop-web run test:e2e-browser`
**Commit:** `P11.28d: make Xbox helper verification stateful in the test host`
**Batch:** stop-after

### P11.28e — Wire Tailscale checks through every setup surface
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/SetupIntro.svelte`, `clients/desktop-web/src/lib/sections/handbook/HelpSection.svelte`, `clients/desktop-web/tests/screens/help.test.ts`
**What:** Pass the agent API into the Handbook’s compact first-launch setup, which was previously rendered without it, and make the Tailscale Check button report an unavailable connection instead of silently returning when no API is present.
**Verify:** `npm --prefix clients/desktop-web run test:screen-help && npm --prefix clients/desktop-web run build`
**Commit:** `P11.28e: wire Tailscale checks through every setup surface`
**Batch:** stop-after

### P11.28f — Reserve port 48001 for MSC2
**Status:** awaiting verification
**Files:** `crates/msc-agent/src/cli/mod.rs`, `crates/msc-domain/src/app_config_schema.rs`, `clients/desktop-web/src-tauri/src/lib.rs`, `packaging/agent-service-layout.json`, platform service tests, `docs/msc2/`, `tools/`, `corpus/server-dirs/README.md`
**What:** Move MSC2’s default management/service port from 48400 to 48001 so it can run beside MSC1 without collisions. Keep MSC1 and historical audit/iOS references on 48400, while updating MSC2’s agent defaults, Tauri installer, service metadata, live-test examples, and assertions.
**Verify:** `cargo fmt --all -- --check && cargo test -p msc-infrastructure --test service_model && cargo test -p msc-platform-macos --test service_plist && cargo test -p msc-platform-linux --test systemd_unit && cargo test -p msc-platform-windows --test service_definition && cargo check -p msc-agent -p msc-domain && cargo check --manifest-path clients/desktop-web/src-tauri/Cargo.toml && bash tools/phase11/agent-install-smoke.sh --synthetic`
**Commit:** `P11.28f: reserve port 48001 for MSC2`
**Batch:** stop-after

### P11.28g — Remove fake hosts from the shipped client
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/navigation/`, `clients/desktop-web/src/lib/platform/`, `clients/desktop-web/tests/`, `clients/desktop-web/tests/e2e/browser/contract-harness.mjs`
**What:** Remove the hard-coded `demo-agent` and fake host switch from the production shell. Keep D-013’s host-keyed architecture for real configured hosts, but make the initial desktop host the actual local agent and keep fake HTTP/contract-host behavior exclusively in test entry points. The shipped app must never present a fake host as a selectable destination.
**Verify:** `npm --prefix clients/desktop-web run check && npm --prefix clients/desktop-web run build && ! rg -n "demo-agent" clients/desktop-web/src`
**Commit:** `P11.28g: remove fake hosts from the shipped client`
**Batch:** stop-after

### P11.28h — Connect the Tauri client to the real local agent
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/api/`, `clients/desktop-web/src/lib/auth/desktop.ts`, `clients/desktop-web/src/lib/platform/`, `clients/desktop-web/tests/auth/desktop/`, `clients/desktop-web/tests/tauri/`
**What:** Wire `DesktopSessionAuth` and its native authorized-request bridge into the real `ApiClient` instead of falling back to cookie authentication in Tauri. Use `http://127.0.0.1:48001` as the local agent origin, obtain/store a host-scoped credential through the approved desktop pairing/bootstrap path, and keep browser sessions on the browser cookie adapter. Do not add a shell-token environment-variable shortcut to the shipped client.
**Verify:** `npm --prefix clients/desktop-web run test:auth-desktop && npm --prefix clients/desktop-web run test:tauri-boundary && npm --prefix clients/desktop-web run build && cargo check --manifest-path clients/desktop-web/src-tauri/Cargo.toml`
**Commit:** `P11.28h: connect Tauri to the real local agent`
**Batch:** stop-after

### P11.28i — Start the installed local agent without terminal commands
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/platform/`, `clients/desktop-web/src/lib/sections/setup/AgentSetupSection.svelte`, `clients/desktop-web/src-tauri/src/lib.rs`, `clients/desktop-web/tests/agent-install/`, `clients/desktop-web/tests/tauri/`, `tools/phase11/`
**What:** On desktop launch, inspect the local service through the existing native service adapter. Start an installed-but-stopped MSC2 agent automatically and wait for its health endpoint on `48001`; when the service is not installed, expose one explicit Install action that uses the existing packaged-agent/service path and elevation boundary. Closing the window must not stop the agent or Minecraft servers, and normal operation must not require a terminal.
**Verify:** `bash tools/phase11/agent-install-smoke.sh --synthetic && npm --prefix clients/desktop-web run test:tauri-boundary && cargo check --manifest-path clients/desktop-web/src-tauri/Cargo.toml`
**Commit:** `P11.28i: start the local agent from the desktop shell`
**Batch:** stop-after

### P11.28j — Show an honest first-run and agent-unavailable state
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/sections/setup/AgentSetupSection.svelte`, `clients/desktop-web/src/lib/help/FirstLaunchGate.svelte`, `clients/desktop-web/src/lib/help/SetupIntro.svelte`, `clients/desktop-web/tests/screens/`, `clients/desktop-web/tests/agent-install/`
**What:** Replace the generic connection failure path with a clear state model for missing, stopped, starting, ready, incompatible, and unavailable agents. Give the operator the next safe action—install, start, repair, or reconnect—without showing the first-time setup as complete before the real local agent is ready. Preserve browser users’ truthful no-native-service fallback.
**Verify:** `npm --prefix clients/desktop-web run test:screen-help && npm --prefix clients/desktop-web run test:tauri-boundary && npm --prefix clients/desktop-web run build`
**Commit:** `P11.28j: show real agent readiness states`
**Batch:** stop-after

### P11.28k — Walk the real client and agent through first-time setup
**Status:** superseded — deferred into P12.17 (decided 2026-08-25). This exercises the first-time-setup UI that Phase 12 rebuilds (P12.13); its real-agent/auth/first-server-handoff proof runs once against the redesigned client at the Phase 12 gate rather than against the superseded UI. The committed P11.28g–j agent wiring it would have exercised carries forward as Phase 12's foundation.
**Files:** `tools/phase11/real-client-agent-smoke.sh`, `clients/desktop-web/tests/e2e/`, `docs/msc2/clients/evidence/`, `differences.md`
**What:** Add a real-agent smoke path that launches or connects to MSC2 on `48001`, opens the actual Tauri client, authenticates through the real desktop credential path, and exercises the first-time setup from the opening screen through the final first-server handoff. Cover the server-root picker, Java check, Bedrock disclosure, optional helper links/checks, helper download state, Back/Next/Skip behavior, and restart/reopen behavior. Record fake-harness-only coverage separately and update `differences.md` only from this real-client evidence.
**Verify:** `bash tools/phase11/real-client-agent-smoke.sh --macos`
**Commit:** `P11.28k: prove real client and agent setup`
**Batch:** stop-after

### P11.29 — Reconcile the capability matrix and run the exact Phase 11 gate
**Status:** superseded — folded into P12.17 (decided 2026-08-25). Its checks (capability matrix, D-003 bundle/screen identity, generated DTO drift, D-013 host isolation, browser/Tauri auth, native Linux WebKitGTK rendering, tri-platform packaging, headless independence, and the onboarding/first-launch preservation contract) depend on the final client UI, so they run once against the *redesigned* client at the Phase 12 gate. Running this gate against the superseded UI is pointless — its own criterion requires preserving MSC 1's design, which only the redesign delivers.
**Files:** `docs/msc2/client-capability-matrix.csv`, `tools/phase11/phase11-check.py`, `docs/msc2/clients/evidence/phase11-ci.md`, `docs/msc2/clients/phase11-scope.md`, `docs/msc2/rolling-plan.md`
**What:** Run only after P11.28a–P11.28k. Check every contract and WebSocket operation against real Desktop/Web implementation evidence, leaving agent-Planned, Bedrock-screen, and player-profile rows explicitly Planned and no cell blank. Prove D-003 bundle/screen identity, generated DTO drift, D-013 host isolation, capability/permission routing, D-026 served-content use, browser/Tauri auth, browser responsive behavior, native Linux WebKitGTK rendering, tri-platform packaging, headless independence, and the exact green CI candidate. Also require the onboarding preservation contract in `phase11-scope.md`: fresh-profile setup → Concept Guide → guided tour → Handbook behavior, step/anchor coverage, skip/reopen persistence, splash playback/fallback, reduced-motion behavior, and honest separation of agent-owned first-server initiation from client-owned presentation. This is Phase 11's only full-workspace run; the other agent decides in REVIEW whether the literal gate holds.
**Verify:** `python3 tools/phase11/phase11-check.py --gate && python3 tools/phase6/capability-matrix-check.py docs/msc2/client-capability-matrix.csv && cargo nextest run --workspace`
**Commit:** `P11.29: check desktop and web gate`
**Batch:** solo

### P11.30 — Allow CORS on the unauthenticated /v1/health probe
**Status:** DONE (committed `0e2cc4f`). Discovered during P12.2 visual verification, not part of any planned step: a dev-mode Tauri window's own `fetch()` to this pre-credential readiness probe is cross-origin (devUrl `:1420` vs agent `:48001`), and the agent sent no CORS header at all, so the probe silently failed and the shell never got past "Agent starting" even though the agent was healthy. This route already runs outside the bearer-auth gate and carries no secrets, so a permissive allowance is safe here.
**Files:** `crates/msc-agent/src/routes/health.rs`
**What:** Set `Access-Control-Allow-Origin: *` on the `GET /v1/health` response only.
**Verify:** `curl -s -D - http://127.0.0.1:48001/v1/health` (with the agent running) shows the header; `cargo nextest run -p msc-agent --test browser_auth --test desktop_auth`.
**Commit:** `P11.30: allow CORS on the unauthenticated /v1/health probe`
**Batch:** solo

### P11.31 — Finish and land the local-agent auto-bootstrap auth
**Status:** DONE. Full account in `toughproblems/local-agent-auth-bootstrap.md` — read that before touching this area again. Discovered during P12.2 visual verification (same as P11.30): the real Tauri app could not connect to the local agent at all, through six stacked, independent bugs. Found `git stash@{0}` ("WIP: local-agent auto-bootstrap auth (pre-P12.1)") — Cameron's own substantial prior implementation of the same-machine bootstrap channel `docs/msc2/clients/phase11-auth.md` describes but this codebase never actually had working — and applied it (clean; only `App.svelte` had a trivial non-conflicting overlap with P12.2) rather than rewriting from scratch. Fixed forward from there: (1) the LaunchDaemon needs macOS admin elevation to start/stop, now done via `osascript ... with administrator privileges`; (2) ad-hoc-signing an already-running dev binary invalidates its own live code identity, fixed by signing-then-re-exec at startup instead of signing in place; (3) **keychain-based storage for the installation key and the store's root key was removed entirely** — both are now plain 0600 files under the agent's own secrets directory, since the agent and its desktop shell always run as the same regular user and keychain's ACL/session semantics kept failing in non-interactive-daemon contexts in ways that were each individually correct-looking and still wrong in practice; (4) a `ClientProof` struct was missing `#[serde(rename_all = "camelCase")]`, so every bootstrap proof submission failed JSON parsing (`hostId` sent, `host_id` expected) before it was ever checked. Real dev-workflow caveat recorded in the toughproblems doc: the installed plist's `MSC2_MACOS_DESKTOP_REQUIREMENT` snapshots one specific run's ad-hoc cdhash, which changes on every `tauri dev` relaunch — click "Repair service" once, right after the app under test opens, and don't restart it again before testing.
**Files:** `crates/msc-platform-macos/src/service.rs`, `crates/msc-platform-macos/src/secret_store.rs`, `crates/msc-platform-macos/Cargo.toml`, `crates/msc-agent/src/auth.rs`, `crates/msc-agent/src/auth/desktop.rs`, new `crates/msc-agent/src/auth/local_bootstrap.rs`, `crates/msc-agent/src/main.rs`, `crates/msc-agent/Cargo.toml`, `clients/desktop-web/src-tauri/src/lib.rs`, `clients/desktop-web/src-tauri/Cargo.toml`, `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/auth/desktop.ts`, `clients/desktop-web/src/lib/platform/{index,browser,tauri,types}.ts`, `clients/desktop-web/src/lib/sections/setup/AgentSetupSection.svelte`, associated tests, new `toughproblems/local-agent-auth-bootstrap.md`
**What:** Make the real Tauri app able to authenticate to its own freshly-installed local agent without any manual pairing step, end to end, on macOS.
**Verify:** `cargo nextest run -p msc-platform-macos && cargo nextest run -p msc-agent --test browser_auth --test desktop_auth`; `npm --prefix clients/desktop-web run test:tauri-boundary && npx vitest run clients/desktop-web/tests/agent-install/agent-install.test.ts && npm --prefix clients/desktop-web run test:auth-desktop`. Cameron's own real-app verification: fresh install → Repair service → Reconnect connects without a manual pairing code.
**Commit:** `P11.31: land the local-agent auto-bootstrap auth`
**Batch:** solo

### P11.31a — Move the desktop app's own stored credential off keychain too
**Status:** awaiting verification. Cameron hit a real login-keychain access prompt ("msc2-desktop-web wants to use your confidential information stored in 'com.ctemple.msc2.desktop'...") during P12.3 verification and correctly recalled that keychain had been removed — P11.31's "removed entirely" only covered two *agent-daemon* secrets (installation key, store root key). A third secret was missed: the Tauri desktop app's own stored pairing credential (`StoredDesktopCredential`), still going through `MacosSecretStore::default_keychain_for_service` (the real login keychain) in `desktop_secret_store()`. Fixed to `MacosSecretStore::system()` — the same self-provisioning, file-rooted store the agent itself uses, sharing `agent_data_directory()`'s `secrets/` directory with the `local-bootstrap.key` file this same process already writes there directly (same user, same reasoning P11.31 already established for the other two secrets). Removed the now-unused `DESKTOP_SECRET_SERVICE` constant. Left `crates/msc-agent/src/auth.rs`'s own `default_keychain_for_service` call untouched — that one backs the foreground-`msc serve`-smoke-harness path the module's own doc comment already carves out as deliberate, not a fourth instance of this gap. Full account appended to `toughproblems/local-agent-auth-bootstrap.md` (§5).
**Files:** `clients/desktop-web/src-tauri/src/lib.rs`, `toughproblems/local-agent-auth-bootstrap.md`
**What:** Stop the Tauri desktop app's own credential store from touching the macOS login keychain, matching the reasoning and pattern P11.31 already applied to the agent's two other secrets.
**Verify:** `cd clients/desktop-web/src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test`. Cameron's own real-app verification: fresh pair/reconnect on macOS no longer shows a Keychain access prompt.
**Commit:** `P11.31a: move the desktop app's own stored credential off keychain too`
**Batch:** solo

---

## Phase 12 — Client redesign (MSC 1 visual + behavioral fidelity)

**Gate** (`msc2-port-plan.md` §3): every MSC 1 screen and sheet has a rebuilt
MSC 2 counterpart that (a) matches MSC 1's shape and behavior — verified
screen-by-screen against MSC 1 by Cameron — and (b) passes the `antiAIslop.md`
checklist. The data/agent layer is unchanged in contract, and no screen exists in
the desktop app that is absent from the web UI (D-003 corollary).

**Why this phase exists.** Phase 11 delivered a working desktop/web client wired
to the real agent, but its UI diverged from MSC 1's information architecture and
design language — the very thing Phase 11 was meant to preserve. Phase 12 course-
corrects: it rebuilds the presentation layer so MSC 2 looks *and behaves* like
MSC 1, **refreshed** into one consistent, deliberately-designed system (MSC 1
grew by accretion and is inconsistent with itself; the refresh unifies it —
cleaner, less bulky, more modern, without becoming a different app).

**Keep / replace line (do not cross it).**
- **KEEP untouched** — the data layer that talks to the agent: `src/lib/api/`,
  `src/lib/platform/`, `src/lib/auth/`, `src/lib/stores/`, `src/lib/streams/`,
  `src/lib/operations/`, `src/lib/hosts/`. Backend/agent/Tauri/Rust changes are
  allowed only when a screen genuinely needs one, and are called out per step.
- **REPLACE** — the presentation: `src/App.svelte`, `src/lib/components/`,
  `src/lib/sections/*`, `src/lib/navigation/`, the shell.

**Required reading before any step:** `docs/msc2/antiAIslop.md` (hard rule #11)
and the locked reference specimens in `docs/msc2/renderings/`
(`status-card`, `buttons-and-type`, `primitives`, `shell`,
`decorated-vs-disciplined`). MSC 1 is the oracle for shape + behavior; its source
is at `~/Documents/Swift Projects/minecraft-server-controller/MSCmacOS/` and its
screenshots at `~/Documents/MSCSS/`. Each step begins by reading that screen's
own MSC 1 view(s) for behavior, not just its screenshot.

**How Phase 12 verification differs (recorded deviation from the normal loop).**
This is a *design* phase. Its `Verify:` is Cameron's **visual review** of the
running screen against MSC 1 plus the anti-slop checklist — not a single runnable
command. Two normal hard rules are relaxed here, deliberately:
- **Verify is a visual review**, not a pass/fail command. Where a cheap structural
  component test exists (`vitest`), the step names it too, but the *gate* is the
  eye, and the loop is: execute → Cameron looks → adjustment requests → repeat.
- **More than one commit per step is expected** — one commit per view/sheet, plus
  adjustment commits from the review loop. The "one commit per step" rule does not
  bind Phase 12.
Every step's Batch is therefore `solo`.

**Sections.** S0 (design system) and S1 (shell) were shaped and locked in advance
as the reference specimens; their steps below turn those locked specimens into
code. The rest apply the locked system to each screen, one at a time.

If a screen needs a UI pattern not covered by the locked S0 primitives, stop and surface it to Cameron as a design decision — extend the system deliberately (and add a renderings/ specimen), never improvise a one-off.

### P12.11 — Manage Servers / Hosts sheet (+ multi-host)
**Status:** DONE
**Files:** `src/lib/sections/fleet/`, sheet components, `src/lib/components/shell/ControlSidebar.svelte`, `src/App.svelte`
**What:** Rebuild the Manage sheet as the multi-host home, per the 2026-08-27 design discussion (recorded below). **Local** is auto-registered on first launch (via `DesktopSessionAuth.bootstrapLocal()`), always listed first, and cannot be removed — only remote hosts can be added or removed, so the UI never has to handle a zero-host state. A remote host is added through a new small form (label + base URL + pairing code) that calls the already-built `DesktopSessionAuth.redeemRemotePairing(baseUrl, pairingCode)` (`src/lib/auth/desktop.ts`) and registers the result in `HostStore` (`src/lib/hosts/registry.ts`) — no contract or backend changes, this is presentation over an already-finished path. With exactly one host, Manage renders pixel-identical to MSC 1's flat `ManageServersView` (server cards, Export/Import, Add Server) plus a small "Add Host…" affordance — no host-grouping chrome for a capability nobody's using. Once a second host exists, add a thin host-group header (label, connection dot, server count, rename/reconnect/remove-host actions) above each host's block of cards; the server cards themselves keep MSC 1's exact card treatment unchanged. Also replace `ControlSidebar.svelte`'s current flat `<select>` picker with the host-aware picker already specified in the locked `docs/msc2/renderings/shell.html` spec (`● Local ▸ Survival`, opening a menu grouped by host, with `Manage…` opening this sheet) — this requires `App.svelte` to stop hardcoding `localAgentHostId` and drive host selection from `HostStore` for real. **Left unresolved by this step, flagged rather than silently decided:** the original text folded "connectivity" (Playit/DuckDNS/resource packs/Xbox broadcast) into this same sheet; those look more like per-server concerns (arguably Server Editor's Broadcast sub-tab, P12.12) than host-level ones, and this wasn't covered in the design discussion — decide its home before or during this step rather than defaulting to "inside Manage" by inertia. Reference MSC 1 `ManageServersView` + `~/Documents/MSCSS/Manage Servers`, adapted for D-013 multi-host. **Resolved (2026-08-27, during P12.12's own planning):** connectivity (Playit/DuckDNS/resource packs/Xbox broadcast) is per-server, not host-level — it stays out of Manage and lives in Server Editor's Broadcast tab; see P12.12's own entry below.
**Verify:** `npm run dev`, open Manage with one host (compare to MSC 1, unchanged shape) and after adding a second (host grouping appears, Local still pinned/undeletable); exercise the sidebar picker's grouped menu and `Manage…` entry; compare to checklist. Structural: `npm run test:screen-fleet`.
**Commit:** `P12.11: rebuild manage servers and hosts`
**Batch:** solo

**Built (2026-08-27), awaiting Cameron's verification.** Manage is now `ManageSheet.svelte`, an actual `Sheet.svelte` modal (opened from the sidebar's `Manage…`), not the old `fleet` route/tab — `FleetSection.svelte` and the `fleet` section entry are retired, its `model.ts` reused as-is. `App.svelte` now owns a `HostStore` instance: Local is registered on mount as a plain registry record (id/label/baseUrl) and is never offered a remove action in the UI; the actual credential exchange for it is unchanged (`createAgentTransport` still calls `DesktopSessionAuth.bootstrapLocal()` internally for Tauri, exactly as before — the registry entry is bookkeeping, not a new auth path). `hostId` now comes from `hostStore.selectedHost` and `switchHost()` re-runs the existing `initializeClient()`/`restoreHostContext()` connection lifecycle unchanged.

**Finding, not a judgment call: multi-host UI is Tauri-only.** `src/lib/platform/index.ts`'s `createAgentTransport` (KEEP-list, unmodified) always targets `window.location.origin` off Tauri, with no per-host `baseUrl` override — a browser tab can only ever reach the one agent that served it. So `isDesktopShell` (resolved once in `App.svelte` via `getPlatform()`, named to avoid the literal string the D-003 platform-boundary test (`tests/tauri/platform-boundary.test.ts`) forbids in screen/route sources) gates Add Host, host-group headers, and cross-host switching everywhere; a browser always renders the exact single-host, no-chrome shape. This isn't new backend work — `redeemRemotePairing`/`HostStore` were already built — just where the UI is allowed to show up.

**Known, deliberate gaps against MSC 1's `ManageServersView`, left rather than papered over:** (1) the delete confirmation offers only "Remove from Controller" — `ServerDeleteRequestDTO` carries just `serverId`, no disk-delete flag, so MSC 1's "Delete from Disk" choice isn't offered. (2) The footer has no "Export…" — there is no `/v1/servers/export` route in the contract at all, so nothing was wired to a route that doesn't exist. (3) "Add Server…" is the existing simple name+version create form, not a port of MSC 1's multi-step `AddServerWizardView` — that wizard is real scope, not attempted here.

**Verify run so far:** `npm run test:screen-fleet` (2 passed), `npx svelte-check` (7 errors, all pre-existing/unrelated — `src/lib/auth/desktop.ts`, `SetupIntro.svelte`, `navigation/route.ts`, `tests/auth/desktop/desktop.test.ts`), `npx prettier --check` clean on every touched file, `npm run build` clean. Also ran the other test files that reference `App.svelte`/`ApplicationShell.svelte`/`ControlSidebar.svelte` (`tests/screens/help.test.ts`, `tests/navigation/navigation.test.ts`, `tests/tauri/platform-boundary.test.ts`, `tests/agent-install/agent-install.test.ts`, `tests/visual/shell.test.ts`) as a non-full-suite regression check: all pass except one pre-existing, unrelated failure already present at `HEAD` before this step (`tests/navigation/navigation.test.ts:141`, `expect(appSource).toContain('client.getCapabilities()')` — the code has always read `selectedClient.getCapabilities()`, a case mismatch that predates this step; not touched). Cameron's own visual pass (`npm run dev`) and the one-host/two-host comparison are still outstanding, per this step's own Verify line.

### P12.11a — Redesign the agent-unreachable screen (+ real `msc service` CLI)
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/setup/AgentSetupSection.svelte`, `clients/desktop-web/src/App.svelte`, `crates/msc-agent/src/cli/service.rs`, `crates/msc-agent/tests/cli_service.rs` (new)
**Context for whoever executes this (written so it's self-contained — no prior conversation needed):** this screen has no MSC 1 equivalent at all; MSC 1 is one unit with no background agent to babysit, so there is no oracle to port against here, only the antiAIslop checklist and the rest of MSC 2's own already-rebuilt Phase 12 screens to match. P12.11 (multi-host Manage sheet) landed first, commit `1a34ef0` — `App.svelte` now has real host state to build on (`hostStore`, `hosts`, `hostId`, `isDesktopShell`, all declared near the top of the `<script>` block), so this step does not need to invent any of that.

**(1) Backend gap, fix inline (do not split into a separate step — same reasoning P12.9 used for its own inline backend fix).** `crates/msc-agent/src/cli/service.rs`, function `run()` (currently lines 62-74): it builds a real `ServiceManagerCommand` via `into_model()` but then always returns `Err(CliError::internal(format!("service management is modeled but not executable yet ({rendering}); platform adapters land in P4.22-P4.24")))` — that message is stale; P4.22-P4.24 landed months ago and are marked DONE in `rolling-plan-archive.md`. The real platform-specific logic already exists, is hardware-proven, and is called successfully today from a completely different entry point: `clients/desktop-web/src-tauri/src/lib.rs`, function `service_manager()` (currently lines 480-501) picks the right concrete manager by `#[cfg(target_os = ...)]` — `msc_platform_macos::service::MacosLaunchdServiceManager`, `msc_platform_windows::service::WindowsServiceManager`, or `msc_platform_linux::service::LinuxSystemdServiceManager` — each of which implements the `ServiceManager` trait (`crates/msc-infrastructure/src/service.rs:167`) and is driven by calling `.execute(ServiceManagerCommand::Status{..} / Start{..} / Stop{..} / Install(..) / Uninstall{..})`, exactly the enum `cli/service.rs::into_model()` already builds. Give the CLI binary the same `#[cfg(target_os = ...)]` selection (it can live in `cli/service.rs` itself or a small shared helper — `msc-agent` already depends on all three platform crates, confirm in `crates/msc-agent/Cargo.toml` and add the dependency if one is missing) and call `.execute(model)` on the result instead of erroring, then render `describe_command`'s existing formatting (or the manager's own returned status/report type — check what `ServiceManager::execute` actually returns in `service.rs` and adapt) as the command's real output. There is no `repair` verb at the `ServiceManager`/CLI level — confirm by reading `crates/msc-infrastructure/src/service.rs:167` (`ServiceManager` trait) and `ServiceManagerCommand` (same file, around line 80) — Tauri's "Repair" (`src-tauri/src/lib.rs:432`, `manage_agent_service`) is `uninstall` + `install` + `start` composed at the Tauri layer, not a primitive; do not invent a `Repair` variant at the CLI/infrastructure layer to match it.

**Known complication, make an explicit call and document it in this step's own write-up rather than leaving it half-solved:** `ServiceCommand::Install` (`cli/service.rs:9`, `ServiceInstallArgs`) requires `--binary-path`, `--working-directory`, `--log-path`, and `--expected-port` — there is no way to give a bare, memorize-and-type `msc service install --service-name msc-agent` command with no other arguments, because nothing in the CLI computes sensible defaults for those paths (Tauri's own equivalent, `agent_install_request()` in `src-tauri/src/lib.rs:503`, only works because it locates paths *relative to the installed desktop app bundle* — `packaged_agent_path()` at `src-tauri/src/lib.rs:631` walks up from `std::env::current_exe()` assuming a `.app`/installer layout that a headless CLI install has no equivalent of). Recommended resolution (part 2 below depends on this): only ever put `status`, `start`, and `stop` in the frontend's copy-paste panel — each needs nothing but the fixed `--service-name msc-agent` — and for the "not installed at all" / "needs a full reinstall" states, keep the copy pointing at reinstalling via the platform's headless package/installer rather than asking a user to hand-type an `install` command whose argument values they have no way to know. Do not fabricate placeholder default values for those four flags just to make an `install` one-liner look complete.

**(2) Frontend redesign.** Rebuild `AgentSetupSection.svelte` on the same S0 primitives every other already-rebuilt Phase 12 screen uses — `Button`, `Card`, `Badge`, `StatusDot`, `Field`, all under `clients/desktop-web/src/lib/components/base/` — replacing the pre-redesign components it currently imports: `ActionButton`, `StatePanel`, `SurfaceCard` (all in `clients/desktop-web/src/lib/components/`, top-level, *not* the `base/` ones — do not confuse the two families) and `ScreenHeader`/`StatusBadge` (in `clients/desktop-web/src/lib/sections/shared/` and top-level `components/` respectively). Read `docs/msc2/antiAIslop.md` before touching markup, same as every other Phase 12 screen. Head it with the host's name — `"‹Host name› agent"` — matching the breadcrumb pattern already visible elsewhere in this shell; the host's `label` is available wherever `hostLabel` is threaded (see prop-wiring below).

**Wiring this screen needs that it does not have today:** `AgentSetupSection` is instantiated in `App.svelte`'s `<svelte:component this={activeComponent} ... />` block (search for `onAgentRetry={() => void initializeClient()}` to find it) and currently receives only `api`, `hostId` (a bare string id), `serverId`, `permissions`, `readiness`, `onAgentRetry`. It needs two more props threaded through from state `App.svelte` already has: the connected host's base URL (available via `hostStore.getState(hostId).host.baseUrl` — `hostStore` is already a module-level instance in `App.svelte`, added by P12.11) and whether this is the desktop shell (`isDesktopShell`, already computed in `App.svelte` via `getPlatform()`, also added by P12.11 — reuse it directly rather than calling `getPlatform()` again inside the section). Add both as new props, e.g. `hostBaseUrl` and `isDesktopShell`, passed alongside the existing ones.

**Branch the screen's body three ways**, using those two new props:
- **`isDesktopShell` true (Tauri):** keep today's real Install/Start/Stop/Repair buttons and their existing `agentServiceStatus()`/`manageAgentService()` calls (`clients/desktop-web/src/lib/platform`) — restyle only, no behavior change.
- **`isDesktopShell` false and `hostBaseUrl` is loopback:** detect loopback by parsing `hostBaseUrl` with `new URL(...)` and checking `hostname === '127.0.0.1' || hostname === 'localhost' || hostname === '::1'` — this is an unspoofable same-machine signal (a browser can only load a page from loopback if it's running on that literal machine), not a heuristic that can be wrong. Replace the current dead-end "This browser cannot install a local background service… Install the headless package" message (see `clients/desktop-web/src/lib/platform/browser.ts`'s `agentServiceStatus`/`manageAgentService` stub text — that stub itself is in the KEEP-untouched `src/lib/platform/` and must not change) with a copy-pasteable terminal-command panel offering exactly `msc service status --service-name msc-agent`, `msc service start --service-name msc-agent`, and `msc service stop --service-name msc-agent`, each with its own Copy button — no `install`/`repair` commands here, per the complication noted above; if the status genuinely is "not installed," say so and point at the headless package documentation instead of a copy box.
- **`isDesktopShell` false and `hostBaseUrl` is not loopback (a different machine entirely):** show distinct copy — this client is not running on the machine that hosts this agent, so nothing here is actionable from this browser at all — with no service-control content, buttons, or commands of any kind.

**Verify:** Backend: `cargo nextest run -p msc-agent --test cli_service` — a new test file asserting `msc service status/start/stop/install/uninstall` actually execute (using `msc_infrastructure::service::FakeServiceManager`, `crates/msc-infrastructure/src/service.rs:172`, already used the same way by `src-tauri/src/lib.rs:863`'s own tests — inject it in place of the real per-platform manager for the test) instead of returning the old "not executable yet" error; also add/keep a `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` pass on the touched crate only. Frontend: `npm run dev` in `clients/desktop-web/`; stop the local agent and confirm the Tauri-branch buttons still work unchanged; simulate the loopback-browser branch (open the built web client at `localhost`/`127.0.0.1` with the agent stopped) and confirm the copyable command panel appears and that the printed commands actually work when pasted into a real terminal against this machine's own agent; simulate the non-loopback branch (point the client at a different host's address) and confirm the "not this machine" copy with no command content. No MSC 1 comparison applies — compare against `docs/msc2/antiAIslop.md`'s checklist and the rest of the already-rebuilt Phase 12 screens for visual consistency instead.
**Commit:** `P12.11a: redesign the agent-unreachable screen and wire the service CLI`
**Batch:** solo

### P12.11b — Make the agent screen reachable from the host picker
**Status:** DONE
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/components/ApplicationShell.svelte`, `clients/desktop-web/src/lib/components/shell/ControlSidebar.svelte`, `clients/desktop-web/tests/visual/shell.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Keep MSC 1's fixed seven server-detail tabs exactly as they are; the agent is host-scoped, not an eighth server tab. Instead, add one explicit `Agent…` action to the existing host/server picker beside `Manage…`. In `App.svelte`, give that action one callback that selects the already-registered `agent-setup` section for the active host, then thread it through `ApplicationShell.svelte` to `ControlSidebar.svelte`. The picker must expose it in both the one-host and multi-host menus, so browser and Tauri load the same reachable screen (D-003) and the selected host's label/base URL continue to drive P12.11a's three truthful states. Do not add backend calls, platform branching, a new primary-tab entry, or a second navigation model. Extend the existing source-level shell test to prove the callback is threaded and the picker contains `Agent…`; retain its anti-slop guard. Manually confirm `Agent…` takes an otherwise healthy running agent to the redesigned page in both the browser and Tauri window, then use the existing picker/server controls to return to a normal server tab.
**Verify:** `cd clients/desktop-web && npm run test:visual-shell && npx tauri dev` — in the running Tauri app, open the host/server picker, choose `Agent…`, and confirm the redesigned page opens while the agent is healthy; repeat against the browser UI at the same Vite origin and run the anti-slop checklist.
**Commit:** `P12.11b: make the agent screen reachable from the host picker`
**Batch:** solo

### P12.11c — Use macOS elevation for the agent Stop control
**Status:** DONE
**Files:** `clients/desktop-web/src-tauri/src/lib.rs`, `crates/msc-platform-macos/src/service.rs`, `docs/msc2/rolling-plan.md`
**What:** Correct the P12.11a verification finding: the desktop app was sending `launchctl stop <label>` from its unprivileged GUI process, which targets the GUI launchd context and fails with exit 3 for MSC's system LaunchDaemon. Add the matching elevated macOS stop helper beside `start_elevated`, wait until the shared status model reports `Stopped`, and route Tauri's existing Stop action through it. Keep the shared `ServiceManager` contract and non-macOS behavior unchanged. Include a focused unit check that protects the bare-label command shape (the privileged process resolves that label in the system domain).
**Verify:** `cargo nextest run -p msc-platform-macos --lib && cd clients/desktop-web && npx tauri dev` — open **Agent…**, click **Stop agent**, approve the macOS prompt, and confirm the page reports stopped; then click **Start agent** and confirm it returns to running.
**Commit:** `P12.11c: elevate macOS agent stop control`
**Batch:** solo

### P12.11d — Scope the agent screen to its selected host
**Status:** DONE
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/sections/setup/AgentSetupSection.svelte`, `clients/desktop-web/tests/agent-install/agent-install.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Correct the P12.11a/P12.11b host-scoping finding. A Tauri desktop app must expose native Install/Start/Stop/Repair controls and local service status only when the selected host is its own Local agent; selecting a paired remote host must never operate or report the desktop computer's service under that remote host's name. On a browser, register the one reachable host using the page's actual origin, not the desktop loopback constant: loopback pages retain the copyable Terminal commands, while pages reached over a host address show the explicit “run service controls on ‹host›” non-actionable state. Keep browser multi-host unavailable as already required by D-003/D-013's current transport boundary; the Tauri picker remains the multi-host switcher. Add regression assertions covering the actual-origin, local-host, loopback, and remote-host gates. No remote service-control API is being invented: a remote host's service remains controlled on that host.
**Verify:** `cd clients/desktop-web && npx vitest run tests/agent-install/agent-install.test.ts -t "keeps service controls scoped" && npm run build && npx tauri dev` — in Tauri, add/select a remote host, open **Agent…**, and confirm it names that host but offers no service buttons or local PID/status; switch back to Local agent and confirm controls/status remain. In a browser at `localhost`, confirm the Terminal commands appear; open the same client through a non-loopback host address and confirm the remote-host message has no commands or buttons. (The broader `npm run test:agent-install` currently includes a pre-existing stale assertion for copy removed before this corrective step; it is not used as this step's verifier.)
**Commit:** `P12.11d: scope the agent screen to its selected host`
**Batch:** solo

### P12.11e — Restore the Agent screen after host scoping
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/setup/AgentSetupSection.svelte`, `clients/desktop-web/tests/agent-install/agent-install.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Correct the P12.11d regression found during Cameron verification. Its new reactive local-service refresh reads the host ID passed by `App.svelte`, but `AgentSetupSection` did not declare that prop, so opening **Agent…** throws at component initialization. Declare the existing `hostId` input with a safe default and extend the host-scope regression assertion to require it. Do not change routing, service behavior, or the selected-host policy from P12.11d.
**Verify:** `cd clients/desktop-web && npx vitest run tests/agent-install/agent-install.test.ts -t "keeps service controls scoped" && npm run build && npx tauri dev` — open **Agent…** from the host/server picker and confirm the page opens; repeat after selecting a remote host and then Local agent, confirming the P12.11d control boundary still holds.
**Commit:** `P12.11e: restore the agent screen after host scoping`
**Batch:** solo

### P12.11f — Open the local agent in a browser from desktop chrome
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src-tauri/src/lib.rs`, `clients/desktop-web/src/lib/components/ApplicationShell.svelte`, `clients/desktop-web/src/lib/components/shell/TopBar.svelte`, `clients/desktop-web/src/lib/components/shell/ShellIcon.svelte`, `clients/desktop-web/tests/visual/shell.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Add one neutral external-link action beside the existing sidebar and console controls in the desktop top bar. It opens the local agent-served browser UI at `http://127.0.0.1:48001` through the existing platform `openExternal` boundary. Extend the existing desktop external-link policy from HTTPS-only to allow that one loopback HTTP case (`127.0.0.1`, `localhost`, or `::1`) while continuing to reject every other HTTP and non-web URL; cover the policy with a unit test. Render the action only for Tauri; browser users are already in that surface. The action always means this desktop computer’s Local agent and must not suggest that MSC can open a browser on a paired remote host. Extend the existing shell source checks to cover the callback, desktop gate, label, and icon.
**Verify:** `cargo test --manifest-path clients/desktop-web/src-tauri/Cargo.toml external_url_policy_allows_https_and_local_agent_loopback_only && cd clients/desktop-web && npm run test:visual-shell && npm run build && npx tauri dev` — in the desktop window, click the new button between the console and Help controls; confirm the default browser opens `http://127.0.0.1:48001` and the desktop window stays open. Confirm the button does not render in the browser UI.
**Commit:** `P12.11f: open the local agent in a browser from desktop chrome`
**Batch:** solo

### P12.11g — Refresh the agent-served browser bundle
**Status:** awaiting verification
**Files:** `crates/msc-agent/web-ui/`, `docs/msc2/rolling-plan.md`
**What:** Correct the browser verification finding: the agent's embedded `web-ui/` snapshot had fallen behind the current Vite `dist/`, so desktop and browser delivery no longer shared one frontend. Rebuild the finished desktop-web client, replace the packaged agent bundle with that exact output using the existing packaging script, and verify every packaged file is byte-identical to `dist/`. Rebuild the development agent binary from the refreshed embedded bundle and place it at the already-installed desktop resource path; Cameron then uses the existing **Repair service** action once to install/start that exact resource binary. No browser-only fork or second frontend is introduced.
**Verify:** `cd clients/desktop-web && npm run bundle:package-agent && cd ../.. && python3 tools/phase11/bundle-identity-check.py && cargo nextest run -p msc-agent --test web_ui` — then in Tauri click **Repair service**, wait for the agent to return to running, use the top-bar browser button, and confirm `http://127.0.0.1:48001` renders the same current Svelte shell rather than the old disconnected page.
**Commit:** `P12.11g: refresh the agent-served browser bundle`
**Batch:** solo

### P12.11h — Authorize the browser opened by desktop
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/auth/browser-handoff.ts` (new), `clients/desktop-web/src/lib/platform/` (`types.ts`, `tauri.ts`, `browser.ts`), `clients/desktop-web/src-tauri/src/lib.rs`, `clients/desktop-web/tests/auth/browser-handoff.test.ts` (new), `clients/desktop-web/tests/visual/shell.test.ts`, `crates/msc-agent/web-ui/`, `docs/msc2/rolling-plan.md`
**What:** Correct the P12.11f/g browser verification finding. The button successfully opens the agent-served shared bundle, but it opens it anonymously; it cannot transfer the desktop bearer token into a browser. Reuse P11.21's existing browser-pairing/session contract instead: the Tauri backend uses its stored local credential to create a full-permission, one-use browser pairing; it opens only the local loopback origin with that code in the URL fragment (never sent in HTTP); the shared browser startup immediately exchanges the fragment for the existing revocable httpOnly cookie and removes the fragment from browser history before normal API loading. The bearer token and pairing code never enter Svelte state. Repackage the changed shared frontend into the agent bundle. No MSC 1 equivalent exists: this is MSC 2's D-012 browser-cookie boundary, and it must retain D-003's one shared frontend.
**Verify:** `cd clients/desktop-web && npm run test:auth-browser && npx vitest run tests/auth/browser-handoff.test.ts && npm run test:visual-shell && npm run bundle:package-agent && cd ../.. && python3 tools/phase11/bundle-identity-check.py && cargo test --manifest-path clients/desktop-web/src-tauri/Cargo.toml local_browser_handoff_keeps_the_pairing_out_of_the_http_url && cargo nextest run -p msc-agent --test web_ui` — then run `npx tauri dev`, click the top-bar browser button, and confirm the new browser tab connects to the Local agent and reaches the normal Home screen without a pairing code in its address bar; hard-refresh the tab to confirm its cookie session persists.
**Commit:** `P12.11h: authorize the desktop browser handoff`
**Batch:** solo

### P12.11i — Retry stale local credentials in the browser handoff
**Status:** awaiting verification
**Files:** `clients/desktop-web/src-tauri/src/lib.rs`, `docs/msc2/rolling-plan.md`
**What:** Correct the P12.11h verification finding: a stale desktop credential makes the first browser-button click silently fail. The shared authorized-request helper correctly deletes a bearer record after a `401 Unauthorized`, but the handoff then stopped instead of using the now-cleared state to bootstrap a replacement. Keep the existing local bootstrap and browser-cookie contract unchanged; retry exactly once after a `401` by re-running the local bootstrap and browser-pairing creation. Do not retry permission denials or any other agent error. No MSC 1 equivalent exists: this is narrow recovery within MSC 2's D-012 local-desktop credential boundary.
**Verify:** `cargo fmt --manifest-path clients/desktop-web/src-tauri/Cargo.toml -- --check && cargo clippy --manifest-path clients/desktop-web/src-tauri/Cargo.toml --all-targets -- -D warnings && cargo test --manifest-path clients/desktop-web/src-tauri/Cargo.toml local_browser_handoff && npx tauri dev` — with a stale local credential (or after the agent rejects one), click the top-bar browser button once and confirm it opens an authenticated browser tab; confirm a non-admin/forbidden pairing response is reported rather than retried.
**Commit:** `P12.11i: retry stale local credentials in browser handoff`
**Batch:** solo

### P12.11j — Show browser-handoff failures in the Agent screen
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/sections/setup/AgentSetupSection.svelte`, `clients/desktop-web/tests/visual/shell.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Correct the P12.11h/i verification finding that the desktop browser icon can appear to do nothing: its native handoff error was retained only in App's shell message, which is not visible while the main shared shell is active. Preserve the existing secure handoff and stale-credential retry. On a handoff failure only, navigate to the already-reachable Agent screen and render the exact error using that screen's existing alert treatment; a successful click leaves the user where they are and opens the browser as before. No MSC 1 equivalent exists: this is truthful recovery copy for MSC 2's D-012 handoff boundary, not a second status pattern.
**Verify:** `cd clients/desktop-web && npm run test:visual-shell && npm run build && npx tauri dev` — click the browser icon with the agent reachable and confirm a browser tab opens; temporarily make the local handoff fail and confirm the desktop switches to **Background agent** with a readable error rather than silently doing nothing; restore the agent and confirm a successful click does not navigate away.
**Commit:** `P12.11j: show browser handoff failures in agent screen`
**Batch:** solo

### P12.11k — Survive Tauri hot reload in the browser handoff
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/platform/` (`index.ts`, `tauri.ts`), `clients/desktop-web/tests/tauri/platform-boundary.test.ts`, `clients/desktop-web/tests/visual/shell.test.ts`, `crates/msc-agent/web-ui/`, `docs/msc2/rolling-plan.md`
**What:** Correct the P12.11j verification evidence: the Agent screen reports `openLocalAgentBrowser is not a function`. The Tauri adapter received the native handoff dependency but did not expose it on the platform interface, so even a fresh desktop shell could not call it. Add that delegation. At the shared platform boundary, also detect a development hot-reload's already-cached older adapter shape and dynamically invoke the registered native handoff command; a fresh shell uses the adapter normally, and a browser never gains a desktop action. Repackage the shared frontend so Tauri and agent delivery remain byte-identical. No MSC 1 equivalent exists: this is MSC 2 development-reload resilience at the D-003 shared-client boundary.
**Verify:** `cd clients/desktop-web && npm run test:tauri-boundary && npm run test:visual-shell && npm run bundle:package-agent && cd ../.. && python3 tools/phase11/bundle-identity-check.py && cargo nextest run -p msc-agent --test web_ui && npx tauri dev` — while `npx tauri dev` is already running, make the browser-handoff caller hot-reload and click the browser icon once; confirm it opens an authenticated tab rather than showing the stale-adapter error. Restart the desktop shell and repeat, confirming the normal adapter path also opens the tab.
**Commit:** `P12.11k: survive Tauri hot reload in browser handoff`
**Batch:** solo

### P12.11l — Return the pairing contract's created response
**Status:** awaiting verification
**Files:** `crates/msc-agent/src/routes/browser_session.rs`, `docs/msc2/rolling-plan.md`
**What:** Correct the P12.11k verification evidence: the desktop reaches `POST /v1/auth/pairings`, but rejects the agent's successful HTTP 200 response because the frozen OpenAPI contract requires HTTP 201 Created. The route already records a created audit event and the desktop correctly requires 201 before opening a one-use browser session; make the route send the matching 201 response and add a focused route-level regression test. Do not weaken the desktop handoff to accept a contract-incorrect success status. No MSC 1 equivalent exists: this is an MSC 2 D-012 contract enforcement repair.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-agent --bin msc browser_pairing_creation_returns_created && cargo build -p msc-agent` — replace the installed development agent resource with the rebuilt binary, restart the local service, run `npx tauri dev`, click the browser icon once, and confirm an authenticated browser tab opens with no pairing code retained in its address bar.
**Commit:** `P12.11l: return the pairing contract's created response`
**Batch:** solo

### P12.11m — Pin the desktop to its packaged agent and restore browser navigation
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/navigation/serverSelection.ts` (new), `clients/desktop-web/tests/navigation/browser-context.test.ts` (new), `clients/desktop-web/src-tauri/src/lib.rs`, `crates/msc-agent/web-ui/`, `docs/msc2/rolling-plan.md`
**What:** Correct the combined P12.11l verification evidence instead of adding another status-code workaround. The installed service currently points directly at the desktop bundle's mutable agent resource, so replacing that file during development can leave launchd running the previous process while the desktop assumes it is current. Stage the packaged agent under a SHA-256 content-addressed path in MSC 2's application-data directory, install the service from that immutable path, and compare every local service report with the current package digest. A missing/mismatched service definition must report `unavailable` with a Repair instruction; local desktop bootstrap, browser handoff, and authorized loopback requests must fail closed before contacting a mismatched process. Apply the same definition check through the shared macOS/Windows/Linux service model rather than a launchd-only workaround. Also fix the browser's post-pairing navigation race in `App.svelte`: choose a server ID that actually exists (active server, retained selection, then first server), resolve the initial route from the freshly assigned capabilities/permissions instead of waiting for Svelte's reactive flush, and remove the internal `Acknowledge shell` placeholder so a successful browser handoff opens the real Overview/tabs. Repackage the shared bundle so the agent-served browser uses the same corrected frontend as Tauri. No MSC 1 equivalent exists for service generation pinning or browser pairing; the visible shell remains the locked Phase 12 design.
**Verify:** `cargo fmt --manifest-path clients/desktop-web/src-tauri/Cargo.toml -- --check && cargo clippy --manifest-path clients/desktop-web/src-tauri/Cargo.toml --all-targets -- -D warnings && cargo test --manifest-path clients/desktop-web/src-tauri/Cargo.toml packaged_agent && cd clients/desktop-web && npx vitest run tests/navigation/browser-context.test.ts && npm run bundle:package-agent && cd ../.. && python3 tools/phase11/bundle-identity-check.py && cargo nextest run -p msc-agent --test web_ui` — then run `cd clients/desktop-web && npx tauri dev`; the old service definition must be refused before any API request, **Repair service** must install/restart the packaged generation, and one browser-icon click must open an authenticated tab with a real selected server and working Overview/Players/Worlds/etc. tabs, with no `Acknowledge shell` page.
**Commit:** `P12.11m: make local browser handoff deterministic`
**Batch:** solo

### P12.11n — Keep agent verification out of the request hot path
**Status:** awaiting verification
**Files:** `clients/desktop-web/src-tauri/Cargo.toml`, `clients/desktop-web/src-tauri/src/lib.rs`, `docs/msc2/rolling-plan.md`
**What:** Correct the P12.11m live verification regression. A process sample of the frozen Tauri shell showed concurrent `desktop_authorized_request` calls spending all available CPU inside `verify_staged_agent`: P12.11m re-read and SHA-256-hashed both 60 MB binaries before every local API request. Preserve the fail-closed service-definition comparison, but cache the successfully staged-and-verified content-addressed path for the lifetime of this desktop process. The packaged resource is immutable for a running release, and `tauri dev` restarts the Rust process when its package changes, so one verification per process preserves the generation guarantee while removing hashing from the request hot path. Optimize the `sha2` dependency in the development profile as well: a live stack sample after caching proved the one required initial verification still occupied an unoptimized development build for roughly a minute, while release builds already optimize it. Add a focused regression test proving repeated path resolution performs one staging operation.
**Verify:** `cargo fmt --manifest-path clients/desktop-web/src-tauri/Cargo.toml -- --check && cargo clippy --manifest-path clients/desktop-web/src-tauri/Cargo.toml --all-targets -- -D warnings && cargo test --manifest-path clients/desktop-web/src-tauri/Cargo.toml packaged_agent` — then run `cd clients/desktop-web && npx tauri dev`; confirm the splash plays, the shell becomes responsive, CPU returns to idle, the real server/tabs load, and the browser button still opens an authenticated browser tab.
**Commit:** `P12.11n: cache packaged agent verification`
**Batch:** solo

### P12.11o — Authorize browser mutations after desktop handoff
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/platform/index.ts`, `clients/desktop-web/tests/auth/browser.test.ts`, `crates/msc-agent/web-ui/`, `docs/msc2/rolling-plan.md`
**What:** Correct the next browser-handoff verification finding. The one-use pairing exchange successfully creates the httpOnly browser session, and `BrowserSessionAuth.credentialAdapter()` already implements the frozen D-012 CSRF flow, but `createAgentTransport()` discards that adapter and wires the browser client to `cookieCredentialAdapter()`, which sends no `X-MSC-CSRF` header. As a result every browser mutation is rejected with `csrf_invalid`: first-run servers-root/Java saves, Xbox-helper download, Start/Stop, settings, and all other POST/PUT/DELETE actions. Construct the browser transport with a `BrowserSessionAuth` bound to the selected agent origin and use its credential adapter; keep safe GET requests cookie-only and keep Tauri's native credential bridge unchanged. Add a regression assertion at the production transport boundary and repackage the shared bundle. **Design finding recorded, not expanded into this repair:** `FirstLaunchGate` currently mixes host-owned setup with per-browser `localStorage`; the correct follow-up is agent-persisted host setup state plus a separate per-client welcome/tour, with native path pickers shown only in Tauri and browser users operating on explicitly labeled host paths/defaults.
**Verify:** `cd clients/desktop-web && npx vitest run tests/auth/browser.test.ts && npm run bundle:package-agent && cd ../.. && python3 tools/phase11/bundle-identity-check.py && cargo nextest run -p msc-agent --test web_ui` — then open from the desktop browser button, proceed past **Server Setup** using the agent-reported defaults, and confirm later browser mutations (including Start/Stop) succeed without `csrf_invalid`.
**Commit:** `P12.11o: authorize browser mutations`
**Batch:** solo

### P12.11p — Separate host setup from client onboarding
**Status:** awaiting verification
**Files:** `crates/msc-domain/src/app_config_schema.rs`, `crates/msc-domain/tests/app_config_schema.rs`, `crates/msc-api/src/dto/versions.rs`, `crates/msc-agent/src/main.rs`, `crates/msc-agent/src/routes/versions.rs`, `docs/msc2/api-contract/openapi.json`, `clients/desktop-web/src/lib/api/generated.ts`, `clients/desktop-web/src/lib/help/FirstLaunchGate.svelte`, `clients/desktop-web/src/lib/help/SetupIntro.svelte`, `clients/desktop-web/src/lib/sections/handbook/HelpSection.svelte`, `clients/desktop-web/tests/screens/help.test.ts`, `clients/desktop-web/tests/e2e/browser/contract-harness.mjs`, `clients/desktop-web/tests/e2e/browser/workflows.spec.ts`, `clients/desktop-web/tests/e2e/tauri-linux/native-renderer.test.ts`, `crates/msc-agent/web-ui/`, `docs/msc2/rolling-plan.md`
**What:** Implement the host/client ownership split approved after P12.11o. Reuse `AppConfig.initial_setup_done`, already ported from MSC 1 `AppConfig.swift:537,623,687,749-750,854` and set by `AppViewModel+ServerSettings.swift:28`, instead of adding another persisted flag. Treat any config containing a registered server as setup-complete even when an older MSC 2 config explicitly persisted `initial_setup_done: false`, so an already-configured host migrates without replaying setup. Add `HostSetupStateDTO { complete: boolean }`, `GET /v1/config/host-setup` (permission `none`), and bodyless `POST /v1/config/host-setup/complete` (permission `settings`); the POST persists `initial_setup_done = true`, returns the same DTO, and reports save failures as HTTP 500 `set_failed`. `FirstLaunchGate` and the Handbook's first-launch reader must obtain setup completion from that agent route, while Concept Guide and tour completion remain per-client `localStorage` values and are never written into host config. Finishing `SetupIntro` must persist host completion before dismissing, with an inline retryable error if the save fails. On the server-path page, continue using the agent's existing `GET /v1/config/servers-root`, `GET /v1/config/java-runtime`, and `GET /v1/java-runtimes` defaults; label browser-entered values explicitly as paths on the agent host, and render the native `Browse…` controls only when `PlatformKind` is `tauri`. Repackage the shared frontend so Tauri and the agent-served browser remain byte-identical. This intentionally sharpens MSC 1's single-process ownership: host prerequisites remain durable with the host, while each browser/desktop client gets its own welcome and tour.

**Picked up (2026-08-27) after the executing agent (Codex) ran out of budget mid-step**, everything above this paragraph is Codex's own write-up and code, verified rather than rewritten: the backend (`HostSetupStateDto`, the two routes, the `initial_setup_done` migration OR-fix) and the frontend (`FirstLaunchGate`/`HelpSection`/`SetupIntro` reading and persisting host-setup state through the new route, `platformKind`-gated `Browse…`) all matched the plan and passed their own targeted checks unchanged. `npm run api:generate -- --check` (in place of `npm run api:check`, per Codex's own note about that checker's pre-existing `Record<string, never>` blind spot) and `npx vitest run tests/screens/help.test.ts` both passed as committed.

**Three real, pre-existing bugs surfaced while finishing this step's own Playwright evidence — not introduced by this step, but living in files this step already touches, and directly blocking the one check that actually exercises the new host-setup flow — so fixed forward rather than worked around:**
1. `contract-harness.mjs` never served `GET /v1/auth/csrf`. P12.11o's `BrowserSessionAuth.headersForMutation()` (`src/lib/auth/browser.ts:50`) fetches that route before every browser POST/PUT/DELETE and throws if the agent doesn't serve it — so in this test harness, *every* browser mutation has been silently failing client-side (never even reaching the network) since P12.11o landed, including this step's own `POST /v1/config/host-setup/complete` and the pre-existing `POST /v1/config/servers-root` save. Added the route, matching the real agent's shape (`crates/msc-agent/src/routes/browser_session.rs`'s `CsrfTokenResponse { csrfToken, expiresAt }`), plus `x-msc-csrf` to the harness's CORS allow-headers list (needed for the cross-origin Tauri-window case `tests/e2e/tauri-linux/native-renderer.test.ts` exercises) and `DELETE` to allow-methods (`BrowserSessionAuth.logout()` needs it).
2. The harness's `/v1/help/catalog` fixture and topic objects used a field named `id` (and `markdown` for body text); the real, frozen contract (`crates/msc-agent/src/help.rs`'s `HelpCatalogEntry`/`HelpTopic`, both `#[serde(rename_all = "camelCase")]` on `help_id`/`body`) and the frontend's own `HelpTopic`/`HelpCatalog` types (`src/lib/help/types.ts`) both use `helpId`/`body`. Both fixture topics therefore had `item.helpId === undefined` — a real Svelte `each_key_duplicate` runtime error in `HelpSection.svelte`'s topic-list `{#each catalog.topics as item (item.helpId)}` (harmless there, since real key-diffing doesn't blast the page), but load-bearing for `HelpSection.svelte`'s own `topicId = ... ?? catalog.topics[0]?.helpId ?? ''` fallback, which silently resolved to `''`, 404'd against `GET /v1/help/`, and left the reader permanently on "That topic is not available on this agent" instead of the real Overview topic — exactly the state the tour-completion handoff lands on. Renamed both fixture fields to match the real DTO shape.
3. `tests/e2e/browser/workflows.spec.ts`'s `walks a fresh profile...` test asserted `gate.getByRole('heading', { name: 'playit.gg' })` (and the same pattern for Xbox Broadcast/Tailscale/You're All Set) with no `level` given; every one of those setup pages also renders an h3 subsection whose text contains (or, for "You're All Set", exactly repeats) the page's own h2 title, so Playwright's default substring match resolves to multiple elements once the assertion is actually reached (a strict-mode violation, not a "not found"). Added `level: 2` to each of these page-title assertions, since `SetupIntro.svelte`'s own page-title heading (`src/lib/help/SetupIntro.svelte:379`) is always an `<h2>` regardless of which sub-card headings share its text.

With all three fixed, `npx playwright test tests/e2e/browser/workflows.spec.ts --project=chromium -g "walks a fresh profile"` passes end to end (setup wizard through every optional page, Concept Guide, guided tour, real Overview handoff, tour restart) — this is the test that actually proves this step's host-setup behavior, and it had never once passed in this repository's history before today, masked by bug 1 (from P12.11o onward) and, before that, by the harness's `/v1/status` first-call-always-503 behavior described below.

**Fourth pre-existing bug, narrowly worked around rather than fully fixed:** `contract-harness.mjs`'s `/v1/status` handler (`if (count === 1) return json(response, {...}, 503)`) deliberately 503s the *very first* `/v1/status` call any client makes, to simulate a reconnect-pending state for the separate `keeps the local host identity and presents reconnect fallback` test. Because the counter is keyed only by User-Agent (shared across every test in the file, not reset per test), whichever test happens to make the first-ever call pays for it with a spurious "Agent unavailable" screen — confirmed present and already broken on bare `main` HEAD before this step's changes (reproduced by hand against commit `5ccea89`), independent of host setup. That made `walks a fresh profile...` fail whenever run alone or first, which is exactly how this step's own Verify line needs to run it. Rather than touch the shared counter's design (risking the reconnect-fallback test it exists for), extended the `/__test/host-setup` reset this test already calls at its own first line to also mark that request's own User-Agent as already past the first call (`statusRequests.set(client, 1)`), so a fresh-profile walk — which has nothing to do with reconnect simulation — no longer eats it. This does not touch any other test's behavior (confirmed: `keeps the local host identity and presents reconnect fallback` and `names destructive targets and completes bounded upload and download workflows` still fail exactly as before, unrelated to host setup, when run first/alone — they don't call `/__test/host-setup` and are untouched by this step). Those two, plus `renders the production bundle` when it runs first, still need the counter's underlying design fixed (e.g. reset per test some other way, or keyed off something test-scoped) in a future corrective step; flagged, not fixed, here.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-domain -p msc-api -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-domain --test app_config_schema && cargo nextest run -p msc-agent --bin msc host_setup && cd clients/desktop-web && npm run api:generate -- --check && npx vitest run tests/screens/help.test.ts && npm run bundle:package-agent && npx playwright test tests/e2e/browser/workflows.spec.ts --project=chromium -g "walks a fresh profile" && cd ../.. && python3 tools/phase11/bundle-identity-check.py && cargo nextest run -p msc-agent --test web_ui` — then restart `npx tauri dev`, use **Repair service** to install the repackaged agent, and confirm: the configured local host opens without Server Setup in both Tauri and a newly opened browser; clearing only browser storage replays the Concept Guide/tour but not Server Setup; an unconfigured empty host receives agent defaults, browser copy says the paths are on the host with no `Browse…` buttons, and Tauri retains both native pickers.
**Commit:** `P12.11p: separate host setup from client onboarding`
**Batch:** solo

### P12.4k — Add Import ZIP / Replace World / Duplicate Slot to the Worlds tab
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/worlds/` (`WorldsSection.svelte`, `model.ts`, new sheets alongside the existing `CreateWorldSheet.svelte`/`RenameWorldSheet.svelte`/`WorldRepairSheet.svelte`/`WorldConversionWizard.svelte`)
**What:** **Design decision (2026-08-27):** Cameron's call, sharper than P12.12's first draft below — world actions should live in exactly one place, not split across Details and Editor. P12.4's own write-up had already carved Import ZIP / Replace World / Duplicate Slot out of the Worlds tab and provisionally assigned them to Server Editor's `World` sub-tab; that assignment is reversed here. Port `ServerEditorWorldTab.swift`'s three actions directly into the already-built Worlds tab instead: Import ZIP (external archive → new slot), Replace World (overwrite the live world from an external source, using the existing `WorldReplaceRequestDTO`/`WorldReplaceActiveRequestDTO` split already wired in `crates/msc-agent/src/routes/worlds.rs` per P12.4's own finding), and Duplicate Slot. This makes the Worlds tab the single, complete home for every world behavior MSC 1 has (Create, Save Current, Activate, Rename, Delete, Convert, Repair, Import, Replace, Duplicate) and removes any World-shaped tab from Server Editor entirely (see P12.12 below). Land this step before or alongside P12.12 so world actions are never briefly homeless.
**Verify:** `npm run dev`, open Worlds, exercise Import ZIP / Replace World / Duplicate Slot on a real server; compare each against MSC 1's `ServerEditorWorldTab` + the World screenshot; confirm nothing else in the tab regressed. Structural: `npm run test:screen-worlds-backups`.
**Commit:** `P12.4k: bring Import/Replace/Duplicate into the Worlds tab`
**Batch:** solo

### P12.4l — Collapse world slot actions into a menu
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/worlds/WorldSlotCard.svelte`, `clients/desktop-web/src/lib/sections/worlds/WorldsSection.svelte`
**What:** Cameron's own follow-up call on P12.4k's landed layout: a persistent 5-button grid per world-slot card (Activate/Convert/Rename/Duplicate/Delete) read as cluttered. Collapse those per-card actions into the same anchored `Menu` overlay `ComponentsSection.svelte`'s addon rows and `ManageSheet.svelte`'s server rows already use — a small "more actions" trigger opens a floating list, the destructive item is styled rather than separately colored, and the menu itself is one shared instance owned by `WorldsSection.svelte` (matching those two screens' own pattern), not one per card. The inline-confirm flow for Activate/Duplicate/Delete (P12.3g's expand-in-place pattern) is unchanged; only the entry point moves from always-visible buttons to the menu. Selecting a card for its Backups panel is unchanged, just recolored to the shared `--msc2-selection` token those rows use for their own selected state.
**Verify:** `npm run dev`, open Worlds, open a card's "World actions" menu and exercise Set as Active/Convert/Rename/Duplicate/Delete; confirm the destructive item styling and single shared overlay match `ComponentsSection`/`ManageSheet`. Structural: `npm run test:screen-worlds-backups`.
**Commit:** `P12.4l: collapse world slot actions into a menu`
**Batch:** solo

### P12.12 — Server Editor sheet (General, Broadcast)
**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/server-editor/` (new), `clients/desktop-web/src/lib/sections/fleet/ManageSheet.svelte` — `App.svelte` turned out not to need any change: `ServerEditorSheet` is a self-contained child of `ManageSheet` (same pattern `ProjectDetailSheet` uses inside `ComponentsSection`), so no shell wiring was required.
**What:** **Design decision (2026-08-27, replacing this step's original "7 sub-tabs" scope):** reading the MSC 1 oracle side by side with the already-built Details tabs (P12.2–P12.10) showed the two view families genuinely duplicate each other in MSC 1 itself — not a porting artifact, real accretion. `ServerEditorSettingsTab.swift` and `DetailsSettingsTabView.swift` embed the literal same `ServerSettingsView` component with two independent Save-Changes drafts; `ServerEditorJarsTab.swift`'s server-jar/mod/Geyser-Floodgate management is the same territory the already-built Components tab (P12.7) now owns live; the Editor's `Backups` tab (global auto-backup policy + manual backup/prune) duplicates the per-slot auto-backup toggle+interval the already-built Worlds tab's `BackupsPanel` (P12.4) already has; and per Cameron's own further call, `World` isn't split between the two view families at all any more — P12.4k moves the remaining Import ZIP/Replace World/Duplicate Slot actions into the Worlds tab, so every world behavior lives in exactly one place. The organizing rule throughout: keep in Details whatever is day-to-day/live-server operation, keep in Editor only what's genuinely setup-time and reached exclusively from Manage (never from inside the live Details workspace, confirmed via `grep -rn "ServerEditorView(" MSCmacOS` — the oracle only ever opens it from `ManageServersView.swift:123`).

Reached via a new `Edit…` action on each server card in `ManageSheet.svelte` (P12.11) — add this trigger if it isn't already there — separate from that sheet's existing simple "Add Server…" create form (P11.9/P12.11 already established that form as its own thing, not this sheet; this step is for editing an existing registered server, not initial creation). Depends on P12.4k landing first (or alongside), so a server being edited never has its world actions missing from both places at once. Build exactly two tabs:
- **General** — `displayName`, `serverDir`, min/max RAM, EULA accept, auto-restart-on-crash, notes, the headless-script generator, **Delete Server** (`ServerEditorGeneralTab.swift`, 1:1 with `ConfigServer`), **plus** the four notification toggles (start/stop/join/leave) carried over from the dropped Settings tab — they're per-server MSC-level config, the same character as auto-restart.
- **Broadcast** — `ServerEditorBroadcastTab.swift`'s full Xbox broadcast config (enable, IP mode, alt-account email/gamertag/password notes, helper JAR download, reset sign-in; Components' Crossplay row keeps its existing quick enable-toggle mirror, unchanged) **plus** Playit tunnel setup/start-stop, DuckDNS setup, and resource-pack config. This resolves P12.11's own flagged-open "connectivity" question: those are per-server network/hosting concerns, not host-level, so they belong here rather than in the Manage sheet — Manage stays host identity + server list only.

**Explicitly not rebuilt as separate tabs, not silently dropped:** `Settings` (Details' Settings tab, P12.8, already covers server.properties fully, reachable even for a stopped/just-created server — no reason for a second independent draft), `JARs` (Details' Components tab, P12.7, already owns live server-jar/mod/Geyser-Floodgate management; initial jar/version choice for a new server stays in the existing Add Server form), `Backups` (Details' Worlds tab's `BackupsPanel`, P12.4, already has per-slot auto-backup + Back Up Now — no separate global-policy tab), `World` (fully absorbed into the Worlds tab by P12.4k — no World-shaped tab exists in Editor at all). `Docker` (`ServerEditorDockerTab.swift`) is excluded entirely for now — D-008 is still **Proposed**, not Approved, so this step builds no Docker surface at all; revisit only once D-008 resolves.
**Amended (2026-08-27):** reopened by **P12.12a** below — Cameron reviewed MSC 1's actual Java placement and decided the Server Editor should gain a third **Java** tab (global Java executable/Detect/Install, shown only for Java servers) rather than leaving it for P12.14's MSC Settings sheet. See P12.12a for the full decision and scope; this step's own General/Broadcast scope above is unchanged.
**Verify:** `npm run dev`, open Edit on an existing server, walk General/Broadcast; compare General against MSC 1's equivalent, Broadcast against MSC 1's Broadcast tab plus its Playit/DuckDNS setup flows (wherever they currently render), and confirm Settings/JARs/Backups/World do *not* reappear as Editor tabs. Run the `antiAIslop.md` checklist.
**Commit:** `P12.12: rebuild the server editor`
**Batch:** solo

### P12.12a — Add a Java tab to the Server Editor
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/server-editor/JavaTab.svelte` (new), `clients/desktop-web/src/lib/sections/server-editor/ServerEditorSheet.svelte`, `clients/desktop-web/src/lib/sections/server-editor/model.ts`, `docs/msc2/rolling-plan.md`
**What:** **Design decision (2026-08-27):** reopens P12.12's closed scope — see the top-of-file "Java tab decision" note for the full reasoning. In MSC 1, the Java executable path, Detect (`JavaRuntimeManager.detectInstalledJavaRuntimes` → `JavaRuntimePickerSheet`), and Install Java… (`JavaInstaller`'s Minecraft-version-framed major picker → Adoptium Temurin download) all live in one place, `PreferencesJavaSection` (`MSCSettingsSections.swift:4-108`) inside the app-wide `MSCSettingsView`, reused once more by the Setup Wizard's Java step. The value it edits (`AppConfig.javaPath`) is genuinely global — one JVM launches every Java server on the host, never a per-server value. Cameron's call: surface the same global value here instead, as a third `SegmentedControl` option in `ServerEditorSheet.svelte` labeled **Java**, rendered only when `currentServer.serverType === 'java'` (Bedrock servers keep today's two-tab General/Broadcast layout unchanged) — because editing a Java server is the moment this setting is actually relevant, not because it becomes per-server data.

Build `JavaTab.svelte` against routes that are already frozen in the contract and already backed by real agent code (no contract or backend work needed this step):
- `GET`/`POST /v1/config/java-runtime` (`JavaConfigResponseDto`/`JavaConfigSetRequestDto`, `executablePath` only) — read/save the manual path field.
- `GET /v1/java-runtimes` (`JavaRuntimesResponseDto` → `JavaRuntimeDto[]` with `name`/`executablePath`/`majorVersion`) — Detect button opening a picker list; port `JavaRuntimePickerSheet`/`JavaRuntimePickerRow`'s shape (name, path, major version, an explicit "no runtimes found — you can still paste a path manually" empty state) rather than inventing a new one.
- `POST /v1/java-runtimes/install` (`JavaRuntimeInstallRequestDto{major}` → `JavaRuntimeInstallResultDto{success,message,operationId}`) — Install Java… button opens a picker over the four Minecraft-framed majors (8/17/21/25, mirroring `msc_domain::java_runtime::MINECRAFT_INSTALL_OPTIONS`'s Minecraft-version ranges and recommended flag — hardcode the same four rows client-side; not worth a new route for a static four-item list), then polls the returned `operationId` with this sheet's own already-existing `pollOperation` helper (`model.ts`) until it succeeds or fails, the same pattern already used elsewhere in this sheet.
- `SetupIntro.svelte`'s `probeJava`/`browseJava` functions are a working reference for this exact detect/browse/save sequence against the same routes — reuse the pattern rather than re-deriving it. Show the native `Browse…` file picker only when `PlatformKind` is `tauri`, matching P12.11p's precedent for host-path fields on a browser client.

Because the value is host-wide, the tab must say so plainly (e.g. "This Java executable runs every Java server on this host") — word it as host config being edited from a convenient place, not as if it only affects the server being edited.

**Explicitly excluded, not silently dropped:** MSC 1's "Extra JVM flags" field (`AppConfig.extraFlags`) has no backing DTO or route anywhere in the contract — `JavaConfigResponseDto`/`JavaConfigSetRequestDto` carry only `executablePath`, even though `crates/msc-domain/src/app_config_schema.rs:891`'s `extra_flags` already exists in the domain model. Same shape as P12.12's own General/Broadcast gaps: left out rather than faked. Add it later as its own small contract-amendment step if wanted.
**Amended (2026-08-27):** corrected by **P12.12b** below after Cameron's visual review of the built tab — adds the "Extra JVM flags" field this step deferred, removes the host-wide banner, and fixes the sheet resizing per tab.
**Verify:** `npm run dev`, open Edit on a Java server and confirm the Java tab appears with working Detect / Install Java… / manual-path save, each checked against a real host; open Edit on a Bedrock server and confirm no Java tab appears. Compare shape and copy against MSC 1's `PreferencesJavaSection` and `JavaInstallerSheet`. Run the `antiAIslop.md` checklist.
**Commit:** `P12.12a: add a java tab to the server editor`
**Batch:** solo

### P12.12b — Fix the Java tab per Cameron's visual review
**Status:** awaiting verification
**Files:** `crates/msc-api/src/dto/versions.rs`, `crates/msc-agent/src/routes/versions.rs`, `crates/msc-agent/src/cli/mod.rs`, `docs/msc2/api-contract/openapi.json`, `clients/desktop-web/src/lib/api/generated.ts`, `clients/desktop-web/src/lib/components/base/Field.svelte`, `clients/desktop-web/src/lib/sections/server-editor/JavaTab.svelte`, `clients/desktop-web/src/lib/sections/server-editor/ServerEditorSheet.svelte`, `docs/msc2/rolling-plan.md`
**What:** Corrects P12.12a against Cameron's screenshots of the built tab. Four changes:
1. **Adds the "Extra JVM flags" field P12.12a explicitly deferred** for lacking a route. Adds `extraFlags: Option<String>` to `JavaConfigResponseDto`/`JavaConfigSetRequestDto` (contract + Rust DTO + regenerated `generated.ts`), and wires `get_java_config`/`set_java_config` to read/write `AppConfig.extra_flags` alongside `java_path`. Found and fixed a real bug while doing it: `set_java_config` previously applied `executable_path` unconditionally, defaulting to `"java"` whenever the field was absent from the request body — harmless while it was the route's only field, but it would have silently reset a configured path the first time a flags-only save landed on this now-shared route. Both fields now apply independently (`if let Some(...)` per field), matching the tab's own per-field Save-button pattern; a regression test (`set_java_config_route_saves_each_field_independently`) covers both directions. `crates/msc-agent/src/cli/mod.rs`'s `msc java set` command updated for the new DTO field (`extra_flags: None`, unchanged CLI behavior).
2. **Removes the host-wide-scope banner** ("This Java executable runs every Java server on this host…") — Cameron's call, not needed.
3. **Executable path stays first, Java Arguments (Extra JVM Flags) is a new zone directly underneath it** — same `Card`/row visual language as the executable-path zone, its own Field + Save.
4. **Stops the sheet resizing between tabs** — `ServerEditorSheet.svelte`'s General/Broadcast/Java content now renders inside a fixed-height (`560px`), independently-scrolling `.tab-panel`, instead of letting the sheet's own outer frame grow or shrink with each tab's content. `Field.svelte` gained an additive `multiline` mode (opt-in textarea rendering) for the flags field; every existing single-line caller is unaffected.

Pre-existing, unrelated gaps noticed and left alone (out of scope for this step): `tools/api-contract-check.py --v1-summary` reports 133 routes against an expected-131 baseline, and `npm run api:check` reports `OnboardingGuideDTO` missing from generated output — both reproduce identically on the pre-P12.12b commit, so neither was caused by this step.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-domain -p msc-api -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-agent --bin msc -E 'test(java_config)' && cd clients/desktop-web && npm run api:check && npm run check` — then `npm run dev`, open Edit on a Java server's Java tab and confirm: no banner, Extra JVM Flags saves and persists independently of the executable path, and switching General/Broadcast/Java no longer changes the sheet's size.
**Commit:** `P12.12b: fix java tab per visual review`
**Batch:** solo

### P12.13 — First-time setup / Prerequisites / Setup wizard
**Status:** awaiting verification
**Files:** `src/lib/sections/setup/`, `src/lib/help/` (first-launch)
**What:** Rebuild the first-launch flow — setup sheet, prerequisites/Java check, Bedrock disclosure, helper links, first-server handoff — to MSC 1's shape and ordering. Reference MSC 1 `SetupWizardView`, `PrerequisitesView`, `FirstStartSheetView` and `~/Documents/MSCSS/First Time Setup`. Preserve the agent-owned vs client-owned initiation boundary from P11 scope.

**Scope note (executed 2026-08-27):** `SetupIntro.svelte` (the setup wizard: welcome/accent → server type → servers root + Java + Bedrock → playit.gg → Xbox Broadcast → Tailscale → done) already existed content-complete from Phase 11, just built on pre-redesign components (`ActionButton`, old `--msc-*` tokens). This step rebuilt it on the S0 base primitives (`Button`, `Card`, `StatusDot`) per `antiAIslop.md` — dropped every colored-icon-in-box, the gradient hero header, and the per-family/per-type accent colors; replaced ad hoc colored callout boxes with `StatusDot`(+label) for real statuses and plain muted text for pure hints; kept the `compact` embed used by the Handbook's reopened first-launch reader (P12.16 scope) untouched in behavior. No script/API-call logic changed — restyle only. `PrerequisitesView`'s standalone dependency-checker sheet has no MSC 2 route and was not built as a separate screen: its Java/Bedrock/Tailscale checks are already folded into the wizard's own pages, which is treated as satisfying it. `FirstStartSheetView`'s post-server-creation "how do people connect" connection-guidance screen also has no MSC 2 equivalent and was not built here — it would need new backend DTOs/endpoints (local/public IP, live playit/Xbox addresses) beyond this step's frontend-only `Files:` scope, so "first-server handoff" was read as the wizard's existing Done-page → Concept Guide → first-server-creation handoff, not a new post-start screen. Flagging this gap for Cameron rather than deciding it silently.
**Verify:** `npm run dev` on a fresh profile, walk setup start → first-server handoff; compare to MSC 1 + checklist.
**Commit:** `P12.13: rebuild first-time setup`
**Batch:** solo

### P12.14 — MSC Settings (app settings)
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/app-settings/AppSettingsSheet.svelte` (new), `clients/desktop-web/src/App.svelte`, `docs/msc2/rolling-plan.md`
**What:** Rebuild the app-wide MSC Settings sheet. Reference MSC 1 `MSCSettingsView` + `~/Documents/MSCSS/MSC settings`. **Java excluded (decided 2026-08-27):** `PreferencesJavaSection` (Java executable path, Detect, Install Java…) moved to Server Editor's new **Java** tab instead (P12.12a) — Cameron's call to surface that global setting in the per-server-editing context rather than draft the same global state from two independent screens. This step covers the rest of MSC 1's Preferences sections (`MSCSettingsSections.swift`'s Process Cleanup, Remote API, Shared Access, Data Folders, Storage, Config Recovery, Ports, Archive, etc.).

**Design decision (2026-08-27, replacing this step's original three-tab scope):** checking every remaining MSC 1 card against the frozen API contract before building found that almost none of them have a real backend, for two different reasons — flagged for Cameron mid-step (same pattern as P12.3's Players-tab block and P12.12's Editor gaps) rather than either faking working controls or silently building new backend outside this step's frontend-only scope. Cameron chose to ship the two genuinely real cards now, no tab bar (not enough content to justify General/Remote/Data), and to record the rest as excluded here rather than dropped silently.

**Superseded, not merely unported** — MSC 2 already replaced what these managed:
- **Remote Access** (expose-on-LAN toggle, `mscremote://pair` QR, shared-access list) — replaced by the real auth system Phase 9/11 already built: named tokens (`/v1/users`, `AccessSection.svelte`) plus Tailscale-only remote access. D-012 explicitly rules out a general LAN-exposure toggle ("general-LAN management remains unavailable for v1").
- **Bedrock Runtime toggle** (`useVMBedrockBackend`) — Phase 10 made Bedrock runtime backend selection automatic per host (`routes/bedrock_runtime.rs`); no route exposes a user-facing toggle any more.
- **CurseForge API Key** — D-027 replaced CurseForge modpack import with manual browser-download + staged-upload (`/v1/modpacks/import`); no API key is used anywhere in the contract.
- **Process Management** (orphaned-Java-process scan/kill, "Relaunch MSC on crash" watchdog) — MSC 2's agent is itself the OS-managed persistent service (`msc-infrastructure`/`msc-platform-macos` `service.rs`), so MSC 1's underlying problem ("the app quit and orphaned its children") doesn't exist in this architecture. The three `/v1/watchdog/*` routes are frozen in the contract but have zero handler anywhere in `msc-agent` — `AccessSection.svelte`'s existing `CapabilityNotice` already documents "watchdog controls" as Agent-Planned.

**Real gaps, never built, not superseded by anything** — Cameron's 2026-08-27 call, asked directly since this changes what ships later: **drop** Config Recovery (rescan folder / restore corrupt backup) and Archives (auto-archive-downloaded-JARs toggle); **keep on the list, deferred** Storage (disk usage breakdown) and Network Ports (global port list + listening probe — note this also overlaps the already-built, still pre-redesign per-server `ConnectivitySection.svelte` port diagnostics) for a future contract-amendment step, same shape as P12.7's deferred `copyPlayerData`. "Testing reset" (Reset MSC to a fresh-install state) is dropped outright — no backend, and reads as a dev-only escape hatch rather than a real product feature. "Open App Support Folder" is dropped outright too — there's no exposed concept of the agent's own data directory in the contract, and "open a folder on this machine" isn't well-defined for a possibly-remote host anyway.

**What's actually built:** one flat sheet (`AppSettingsSheet.svelte`, no tabs), reached from the gear icon in `TopBar.svelte` (wired for the first time — the icon existed but `onSettings` was never passed from `App.svelte`). Two cards:
- **Appearance** — the per-server accent banner color. This was already fully built as dead client-local code (`styles/bannerColor.ts`'s `bannerColorFor`/`setBannerColorFor`, clamped the same way MSC 1's `clampedAwayFromWhite`/`clampedAwayFromBlack` guard rails did) with zero call sites anywhere in the client until this sheet. Edits the *currently selected server's* color (MSC 1 edited the selected server if one existed, else an app-wide default — MSC 2 has no "no server selected" shell state and no built app-wide-default storage, so this sheet always targets the selected server, honestly labeled with its name). Client-local by design, not synced through the agent; the card says so.
- **Data & Folders → Open Server Folder** — `GET /v1/config/servers-root` for the path, `PlatformAdapter.revealInFileManager` (the seam P12.9 built for the Files tab) to open it, with the same "needs the desktop app" browser-fallback notice `FilesSection.svelte` already uses.
**Verify:** `cd clients/desktop-web && npm run check` (0 new errors — 5 pre-existing, unrelated failures remain in `auth/desktop.ts`/`navigation/route.ts`/`tests/auth/desktop/desktop.test.ts`), then `npm run dev`, click the gear icon in the top bar, confirm the sheet opens with no tab bar, exercise Appearance (color swatch + Save, confirm the running-state terrain banner updates) and Open Server Folder (desktop: opens Finder/Explorer; browser: shows the "needs the desktop app" notice). Run the `antiAIslop.md` checklist.
**Commit:** `P12.14: rebuild MSC settings`
**Batch:** solo

### P12.15 — Onboarding tour / contextual help
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/TourOverlay.svelte` (new), `clients/desktop-web/src/lib/help/tourAnchors.ts` (new), `clients/desktop-web/src/lib/help/FirstLaunchGate.svelte`, `clients/desktop-web/src/lib/help/onboarding.ts`, `clients/desktop-web/src/lib/components/base/Button.svelte`, `clients/desktop-web/src/lib/components/base/Field.svelte`, `clients/desktop-web/src/lib/components/base/Sheet.svelte`, `clients/desktop-web/src/lib/components/shell/ControlSidebar.svelte`, `clients/desktop-web/src/lib/components/shell/TabStrip.svelte`, `clients/desktop-web/src/lib/components/ApplicationShell.svelte`, `clients/desktop-web/src/lib/components/shell/ConsoleDock.svelte`, `clients/desktop-web/src/lib/sections/fleet/ManageSheet.svelte`, `clients/desktop-web/src/lib/sections/server-editor/GeneralTab.svelte`
**What:** Rebuild the guided tour with a real anchored spotlight overlay (`TourOverlay.svelte`), replacing the plain centered-card placeholder Phase 11 shipped for the `tour` stage of `FirstLaunchGate.svelte`. Ports `OnboardingOverlayView.swift`'s shape and behavior: a spotlight cutout on the real control with everything else dimmed (a single fixed-position box-shadow-spread element on web, not SwiftUI's four-rect dim layer -- a mechanism simplification, not a behavior change), full-screen welcome/done cards, a progress bar, the `hideCard`/"Got it" pattern, and reduced-motion (`prefers-reduced-motion` disables the spotlight's position transition). Per `antiAIslop.md`: no colored icon-in-circle on the welcome/done cards, no accent-colored glow ring (the spotlight ring is plain white -- accent stays spent only on running-state/active-tab/primary-action/status per the design law), and every button is the shared `Button` component (solid neutral fill, no capsule/gradient).

**Anchors are real DOM elements, not a parallel tour-only layer.** `tourAnchors.ts`'s `onboardingAnchor` Svelte action reports an element's live viewport rect into a shared store on mount/resize/scroll; `Button.svelte`/`Field.svelte` gained an additive `anchorId` prop (opt-in, most callers leave it unset) and `Sheet.svelte` an additive `closeAnchorId`, so wiring an anchor is a one-line prop on the real control rather than a second copy of the UI. Seventeen anchors are wired this step, one per MSC 1 `OnboardingAnchorID` the current build actually has UI for: `ob_manage_servers`/`ob_start_button` (`ControlSidebar.svelte`), `ob_create_server`/`ob_server_name`/`ob_server_source`/`ob_confirm_page`/`ob_manage_done` (`ManageSheet.svelte`'s create form and its Sheet close button), `ob_accept_eula` (`GeneralTab.svelte`), `ob_console_panel` (`ConsoleDock.svelte`), `ob_console_divider_handle` (`ApplicationShell.svelte`), and the seven `ob_details_*_tab` ids (`TabStrip.svelte`).

**A real scope gap, resolved by filtering content client-side rather than editing it.** MSC 1's tour is 33 steps (`content/guides/onboarding.json`, frozen oracle-faithful data from P11.15) but roughly forty percent of them anchor to UI MSC 2 hasn't built: `AddServerWizardView`'s whole multi-page create flow (type/category/flavor/version/crossplay/Xbox-broadcast/settings-review/connectivity/ports/first-world/add-ons) is still the plain name+version form in `ManageSheet.svelte` -- that file's own P11.9-era comment already calls the wizard port "its own scope, not attempted" -- and the Packs tab was permanently dropped (P12.5). Rather than hand-edit the frozen guide content (which the guide's own `presentationBoundary` field designates agent-owned: content/order/branching, not client presentation) or silently invent UI to fill the gaps, `onboarding.ts`'s new `applicableTourSteps` filters the served step list down to steps whose anchor is either absent (welcome/done) or in `tourAnchors.ts`'s `KNOWN_TOUR_ANCHOR_IDS` set. The full 33-step oracle sequence is preserved untouched in the served data; once the wizard is built, its steps become walkable again by adding their anchors to that one set, no content or filtering-logic change needed. The walkable sequence today is 20 steps: welcome → open the server list → Add Server → name it → pick a version → Create → close the list → accept the EULA → Start → watch the console boot → continue into Details or finish → make room for the console → the seven detail tabs → done.

**A real structural difference, not smoothed over:** MSC 1 surfaces Accept EULA as a prominent control on the main screen, blocking Start. MSC 2 has no such banner today -- EULA acceptance lives inside `GeneralTab.svelte`, reached via a server row's Edit… action inside the (by then closed) Manage sheet. The tour's `accept-eula` step spotlights that real control; because the overlay treats "anchor not yet mounted" as a full-dim fallback rather than an error, the coach mark stays a plain instructional card while the user navigates there and automatically resolves into a real spotlight once `GeneralTab` mounts, with no special-case code needed. Whether EULA acceptance deserves a more prominent surface in MSC 2 is a separate, larger UX question, out of scope here.

**A real dead-end, found and fixed before shipping:** the `create` step is both `hideCard` and `requiresUserAction` in the oracle data (MSC 1 resumes it via the wizard's own Continue button calling `OnboardingManager.advance()` directly, a click-interception mechanism this port doesn't implement). Branch-ordering `hideCard` before `requiresUserAction` in `TourOverlay.svelte` would let "Got it" hide the card with no remaining way to advance. Fixed by checking `requiresUserAction` first: any step requiring a real action confirms via the existing "I did that" affirmation (already the established mechanism from Phase 11's placeholder, per `onboarding.ts`'s `nextTourStep(steps, index, userActionCompleted)` contract, which `tests/screens/help.test.ts` already pins) regardless of `hideCard`. `hideCard`-only steps (none exist in today's 20-step walkable sequence, but the content schema and future wizard steps carry the flag) still get the plain "Got it" treatment.

**Deliberately not built, not silently dropped:** MSC 1's second, independent `ContextualHelpFoundation.swift` system (short 2-4 step "?"-triggered mini-guides scattered across `SidebarView`, `DetailsView`, `MSCSettingsView`, `ServerEditorView`, and others) is a different mechanism from the guided tour and is not ported this step -- this step's `Files:` scope and Verify line are both tour-only. Real click-interception auto-advance (the oracle's actual mechanism for `requiresUserAction` steps) is not implemented; this client keeps the already-tested explicit "I did that" confirmation instead, which the oracle's own accessible affordance set treats as an equally valid resumption model, just not the one macOS's real button wiring uses.
**Verify:** `cd clients/desktop-web && npm run check` (0 new errors -- same 5 pre-existing, unrelated failures as P12.14 in `auth/desktop.ts`/`navigation/route.ts`/`tests/auth/desktop/desktop.test.ts`) `&& npm run test:unit` (same 2 pre-existing, unrelated failures as before this step, confirmed via `git stash` against a clean checkout: `tests/navigation/navigation.test.ts`, `tests/agent-install/agent-install.test.ts`, plus the pre-existing `tests/e2e/tauri-linux/native-renderer.test.ts` file-level failure -- none touch this step's files), then `npm run dev`, run the tour start→finish (Manage… → Add Server… → name → version → Create → close the list → Edit… → General → Accept EULA → Start → console → continue into Details or finish → resize handle → all seven tabs → done), check the spotlight tracks each real control, Skip tour marks it complete, and reopening from the Handbook's "Restart this tour" replays it from welcome; compare to MSC 1 + the `antiAIslop.md` checklist.
**Commit:** `P12.15: rebuild the onboarding tour`
**Batch:** solo

### P12.15a — Advance the onboarding tour through Choose Path
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/TourOverlay.svelte`, `clients/desktop-web/src/lib/help/tourAnchors.ts`, `clients/desktop-web/src/lib/help/onboarding.ts`, `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte`, `clients/desktop-web/tests/e2e/browser/contract-harness.mjs`, `clients/desktop-web/tests/e2e/browser/workflows.spec.ts`, `docs/msc2/rolling-plan.md`
**What:** Correct the first three action-driven tour cards. Real anchored controls now emit a completion event after their click, and the tour consumes it for Manage Servers, Create a Server, and Choose Your Path. Choose Path is tour-locked to Start Fresh, disables Import Existing while that card is active, and advances from the wizard's real Continue button. Normal wizard use outside the tour is unchanged; later required-action cards retain their existing behavior for the next slide-by-slide pass.
**Verify:** `cd clients/desktop-web && npm run build && npx vitest run tests/screens/help.test.ts && npx prettier --check src/lib/help/onboarding.ts src/lib/help/TourOverlay.svelte src/lib/sections/fleet/wizard/AddServerWizard.svelte tests/e2e/browser/contract-harness.mjs tests/e2e/browser/workflows.spec.ts` — then run the tour visually: click Manage…, click Add Server…, confirm Start Fresh is selected and Import Existing is disabled, and click Continue to advance.
**Commit:** `P12.15a: advance the onboarding tour through choose path`
**Batch:** solo

### P12.15b — Spotlight Choose Path's Continue action
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/TourOverlay.svelte`, `docs/msc2/rolling-plan.md`
**What:** Correct the Choose Your Path card's visual target. The card still explains the Start Fresh choice, but its spotlight now highlights the wizard's real Continue button instead of the two path cards. The action event and guide data remain unchanged.
**Verify:** `cd clients/desktop-web && npm run build && npx prettier --check src/lib/help/TourOverlay.svelte` — then open the onboarding tour, reach Choose Your Path, and confirm only Continue has the spotlight before clicking it.
**Commit:** `P12.15b: spotlight choose path continue action`
**Batch:** solo

### P12.15c — Anchor Xbox Broadcast to its visible card
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/ConfigureStep.svelte`, `clients/desktop-web/tests/screens/add-server-wizard.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Fix the Xbox Broadcast tour dead-end for Java Paper servers with Crossplay enabled. Move `ob_server_xbox_broadcast` from the Bedrock Version section onto the conditional Xbox Broadcast section itself, so the anchor mounts when that card is visible and the tour can resolve and advance normally.
**Verify:** `cd clients/desktop-web && npx vitest run tests/screens/add-server-wizard.test.ts && npm run build` — then run the tour through Java → Paper → enable Crossplay and confirm the Xbox Broadcast card is spotlighted and the tour can continue.
**Commit:** `P12.15c: anchor xbox broadcast card`
**Batch:** solo

### P12.15d — Pause the tour while reviewing settings
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/TourOverlay.svelte`, `clients/desktop-web/tests/screens/help.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Correct the Review Your Settings card's missing-anchor dead-end. Because `ob_server_settings` has no one-to-one element in the current wizard, this card now presents `Okay` and `Skip tour`; `Okay` removes the onboarding overlay completely so the current sheet is freely usable, and the real wizard Continue action resumes and advances the tour. The served guide text remains unchanged.
**Verify:** `cd clients/desktop-web && npx vitest run tests/screens/help.test.ts && npm run build` — then reach Review Your Settings, confirm the card offers Okay and Skip tour, click Okay, review the unobstructed sheet, and click Continue to resume the tour.
**Commit:** `P12.15d: pause the tour while reviewing settings`
**Batch:** solo

### P12.15e — Filter Xbox Broadcast by server choices
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/onboarding.ts`, `clients/desktop-web/src/lib/help/FirstLaunchGate.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/ConfigureStep.svelte`, `clients/desktop-web/tests/screens/help.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Make the Xbox Broadcast tour step conditional on the live wizard draft. It appears only for Java Standard non-Vanilla servers with Crossplay enabled; it is omitted for Crossplay-off, Bedrock, Vanilla, and every Modded choice. The wizard publishes its choices to the client-owned tour filter without changing the served guide data or normal server creation behavior.
**Verify:** `cd clients/desktop-web && npx vitest run tests/screens/help.test.ts && npm run build` — then visually run the tour through Paper with Crossplay off (no Xbox step), repeat with Crossplay on (Xbox step appears), and confirm Bedrock and Modded paths skip it.
**Commit:** `P12.15e: filter xbox broadcast by server choices`
**Batch:** solo

### P12.15f — Advance Connectivity from its real Continue action
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/TourOverlay.svelte`, `clients/desktop-web/tests/screens/help.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Apply the common action-confirmation correction to the Connectivity “Ready to Continue” card. Clicking the wizard’s highlighted Continue button now advances the tour directly; the card no longer asks for “I did that.”
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/help/TourOverlay.svelte tests/screens/help.test.ts && npx vitest run tests/screens/help.test.ts && npm run build` — then reach Connectivity → Ready to Continue and confirm clicking the real Continue button advances the tour.
**Commit:** `P12.15f: advance connectivity from continue action`
**Batch:** solo

### P12.15g — Complete the onboarding create-server flow
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/help/onboarding.ts`, `clients/desktop-web/src/lib/help/TourOverlay.svelte`, `clients/desktop-web/src/lib/help/FirstLaunchGate.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte`, `clients/desktop-web/tests/screens/help.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Carry the guided tour through World, optional Add-ons, and Confirm/Create. World and Add-ons use an unobstructed Okay-and-resume pause so the user can freely configure a basic server; Add-ons explicitly stays optional. Confirm uses the same pause while the user reviews the summary and display name. The tour waits for the real create operation to succeed, then ends on the wizard's server-created page instead of showing the later EULA/start/console cards. Filter Java-only Configure and Add-ons cards from Bedrock and Vanilla paths based on the live wizard choices. Served onboarding content remains unchanged, and the Server Handbook is out of scope.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/help/onboarding.ts src/lib/help/TourOverlay.svelte src/lib/help/FirstLaunchGate.svelte src/lib/sections/fleet/wizard/AddServerWizard.svelte tests/screens/help.test.ts && npx vitest run tests/screens/help.test.ts && npm run build` — then visually run the tour through World, confirm Add-ons says it is optional and Okay removes the overlay, confirm Continue resumes it, create the server, and verify the success page remains visible with no EULA/start/console tour card.
**Commit:** `P12.15g: complete onboarding create-server flow`
**Batch:** solo

### P12.16 — Guides / Handbook / How MSC Works
**Status:** built, awaiting Cameron's verification
**Files:** `src/lib/sections/handbook/`, `src/lib/help/`
**What:** Rebuild the Server Handbook, Concept Guide ("How MSC Works"), and router/port-forward guides, consuming the `GET /v1/help/{helpId}` content contract (no hardcoded divergent text). Reference MSC 1 `ServerHandbookView`, `ConceptGuideView`, `RouterPortForwardGuideSheet` and `~/Documents/MSCSS/{Guides,Server Handbook,How MSC Works}`.

`HelpSection.svelte` (Files scope) was still on the Phase 11 `ScreenHeader`/`ActionButton`/`screen.css`/`--msc-*` stack — same shape the P11.24 SetupIntro note already flagged — and crammed Handbook + Concept Guide + Router Guides + the first-launch replay affordance into one ad hoc vertical stack. Rebuilt to S0 (`--msc2-*` tokens, `Card`/`Button`/`Badge`/`Field`/`SegmentedControl`/`EmptyState`), split into four new presentation-only components (`HandbookBrowser`, `ConceptGuidePanel`, `ConceptDiagram`, `RouterGuidePanel`, plus `catalogOrder.ts` for the client-owned reading order MSC 1's `HandbookTopic.allCases`/router-picker enum define — no prose lives there). Since MSC 2 has one "Handbook" tab rather than MSC 1's three separate windows, the three guides live behind a `SegmentedControl` in one "Guides" zone, with the onboarding replay affordance kept below as its own zone, matching the shared-component `.zone`/`.section-header` convention already established by `WorldsSection.svelte`/`PerformanceSection.svelte` — no page-level heading, since the tab strip already names the screen (confirmed no other migrated Phase 12 section repeats one).

Real gaps found and handled, not silently worked around:
- The agent already serves a *real* 31-topic handbook corpus and a real 14-guide/5-category/9-topic router catalog (`crates/msc-agent/src/help.rs`, `crates/msc-domain/src/router_guides.rs`) — this was never a stub to build against, just a presentation layer that hadn't caught up. `RouterGuideCatalog` in `help/types.ts` was missing `family`/`category` (present on the wire, unused by the old flat `<details>` list); added additively.
- The reopened-tour card used the *unfiltered* `onboarding.steps` list (`nextTourStep(onboarding.steps, …)`) instead of `applicableTourSteps` like `FirstLaunchGate.svelte` uses for the real first-run walk (P12.15) — a real inconsistency that could spotlight a not-yet-built anchor on replay. Fixed to match.
- Dropped a vestigial `<p data-onboarding-anchor={step.anchor}>Anchor: {step.anchor}</p>` debug element — grepped the codebase; `data-onboarding-anchor` was never read anywhere, the real spotlight mechanism is the `use:onboardingAnchor` action on the actual target element (`tourAnchors.ts`). `tests/screens/help.test.ts` asserted this dead attribute's literal string; updated the test (moved its "unavailable topic" assertion to the new `HandbookBrowser.svelte`, dropped the dead-attribute assertion) rather than reintroducing it.
- The oracle's router-guide picker shows a per-guide "verified recently / community based" confidence badge with no backing field in `RouterGuide` (`family`/`category`/`displayName`/`steps` only) — left out rather than invented, same handling P12.18c gave Bedrock's dead `dockerImage` field.
- `ConceptGuideDiagrams.swift`'s per-page diagrams have no extracted asset (`content/guides/concept-guide.json` marks every page `assetStatus: "unresolved... client-owned rendering required"`) and the oracle's own diagrams are exactly antiAIslop tells #1/#6/#13 (per-page accent color, colored icon tiles, staggered fade-in animation). Rendered flat monochrome node/arrow diagrams instead (`ConceptDiagram.svelte`) — same information shape, no color, no motion.
- `HelpLink.svelte` (in `Files:` scope) was still `--msc-accent`; restyled to `--msc2-text-secondary`/underline, incidentally also improving `MonitoringRail.svelte`/`HealthSection.svelte`'s existing uses of it.

Verified against real content (not just the fixture harness): `crates/msc-agent/src/help.rs`'s embedded corpus and `content/guides/*.json`/`content/help/*.md` directly, confirming category slugs (`concepts`, `java-servers`, `modded-java`, `bedrock-servers`, `connection-access`, `server-management`, `getting-started`), all 31 `handbook.*` topic ids, and the 5 router categories/14 guides/9 troubleshooting topics `catalogOrder.ts` and the panels assume. Also verified live with Playwright against a production build + `tests/e2e/browser/contract-harness.mjs`: Handbook/How MSC Works/Router Guide all render correctly with zero console errors, and the "topic not in this fixture" and "router catalog empty" paths both degrade to the intended `EmptyState` rather than erroring. The existing `tests/e2e/browser/workflows.spec.ts` suite was checked too and found already broken on a clean `main` checkout for reasons entirely outside this step's `Files:` scope — its very first assertion (`getByRole('navigation', {name:'Sections'})`) never appears in the fake-harness DOM at all, and its onboarding-tour walk stalls on a pre-existing `ob_manage_servers` anchor visibility gap in `ControlSidebar.svelte`/Fleet, both upstream of anything Handbook-related and reproducing identically with this step's changes stashed out. Not chased (matches P12.18g/h's precedent of noting pre-existing sandbox gaps rather than fixing them out of scope) — flagging here for whoever next touches App shell/Fleet e2e coverage. `npm run check`/`npm run test:unit` confirmed against the same clean-checkout baseline prior steps established: same 5 pre-existing type errors, same 2 pre-existing test failures, nothing new.

**Content-fidelity follow-up (2026-08-28, same step):** Cameron's visual review of the built screen above found the *content* underneath it, not just the presentation, was the real problem — every one of the 31 `content/help/handbook/*.md` files was compressed to ~20-38 words (analogy + one sentence) against 336-890 real words per topic in the oracle (`MSCmacOS/MSCmacOS Swift/ServerHandbookTopics.swift`), dropping whole structural blocks the oracle always includes (`GuideTopicHeader.subtitle`, `BulletList`, 1-4 `GuideCallout`s, an `InAppBox`, an `AdvancedSection`, a `RamGuideTable`, and phase-grouped `ChecklistStep`s on the 3 Getting-Started topics). Investigating the other two guides found the same systemic under-extraction, at different severity — Router Guide's 14 real per-brand guides (`RouterPortForwardGuidesFoundation.swift`) have real per-step title+body text, admin addresses, notes, and per-guide troubleshooting links, reduced to 3 generic one-line steps each; Concept Guide's compression is milder (body text condensed, the oracle's per-page `note` aside dropped entirely) but pages 1-2 were also restructured — the oracle's real page 1 got merged into the current page 2, and the current page 1 is synthetic framing not sourced from the oracle at all. **Cameron scoped this pass to the Handbook only**; Router Guide and Concept Guide have the same kind of gap and are deliberately left for a later pass, not forgotten.

Split across two agents at Cameron's request: Codex did the schema + content authoring (additive `subtitle: Option<String>` and `sections: Vec<HelpContentBlock>` on `HelpTopic`, a `### <Marker>` heading convention in the markdown body for `Body`/`Bullet List`/`Callout: <style>`/`In This App`/`Advanced Details`/`Checklist: <phase>`/`Table` blocks, and all 31 files transcribed verbatim from the oracle — commit `7a82f74`, 4 new Rust tests, `cargo fmt`/`clippy` clean), verified correct by inspection and by re-running its tests before building against it. This Claude Code session then built the frontend: `HandbookBrowser.svelte` renders `subtitle` and each `sections` block with a flat, neutral, labeled treatment (no color coding across the 4 callout styles — that would spend the accent budget on a content classification, not a defined system status) and one card depth (`Advanced Details` is a native `<details>`, not a nested card); `markdown.ts` gained `**bold**`/`` `code` `` support, both used throughout the restored prose and previously rendered as literal asterisks/backticks (commit `728492e`). Verified by temporarily enriching the Playwright fixture harness with one example of every block type and screenshotting the real render (not committed — reverted after) — see chat history for the screenshots. `npm run check`/`npm run test:unit` still match the established baseline, nothing new.

**Correction to this entry's own earlier note:** the "router-guide confidence badge has no backing field" line above is wrong as stated — the oracle's `RouterGuideReviewMetadata` (`sourceConfidence`/`lastReviewed`/`reviewNotes`) is real, hand-authored data per guide; it's just as under-extracted as everything else Router Guide is missing, not something the oracle itself lacks. Left as a known gap for the later Router Guide pass, not re-litigated here.

**Verify:** `npm run dev`, open each guide, confirm content comes from the help contract; compare to MSC 1 + checklist.
**Commit:** `P12.16: rebuild guides and handbook`
**Batch:** solo

### P12.16a — Router Guide content restoration + real routes (backend)

**Status:** planned, not built
**Files:** `content/guides/router-catalog.json`, `content/guides/router-troubleshooting.json`, `crates/msc-domain/src/router_guides.rs`, `crates/msc-agent/src/help.rs`, `crates/msc-agent/src/routes/help.rs`, `docs/msc2/api-contract/openapi.json`
**What:** Closes the gap P12.16 found and deliberately deferred ("Router Guide... deliberately left for a later pass, not forgotten") — the router content corpus is compressed to 3 generic one-line steps per guide against MSC 1's real per-step title+body text, admin addresses, notes, and troubleshooting links, even though the domain engines that consume that richer shape are already fully ported and characterization-tested (`crates/msc-domain/src/router/{matcher,fallback_tree,composer,runtime_resolver,troubleshooting}.rs`, 3,043 lines) and simply sit unused behind test fixtures today.

Verbatim-transcribe MSC 1's `RouterPortForwardGuideSeedData.v1Guides` (14 guides) and `v1TroubleshootingTopics` (9 topics) from `MSCmacOS/MSCmacOS Swift/RouterPortForwardGuidesFoundation.swift` into the richer shape `composer::Guide`/`GuideStep`/`GuideNote`/`ReviewMetadata` and `matcher::Guide` already define: full per-step `kind`/`title`/`body`/`referencedTokens`/`alternateTerms`, notes, admin addresses, menu path, alternate menu names, provider vs. device display names, review metadata (`sourceConfidence`/`lastReviewed`/`reviewNotes`), search keywords, admin surface, family/category. Wire real HTTP routes on top of the already-built engines instead of the current flat `GET /guides/router-catalog` stub: compose-by-id (`composer::compose_guide_by_id` + `runtime_resolver::resolve` against a selected server's live context), search/match (`matcher::match_query`, with `fallback_tree::resolve` feeding the ambiguous/no-direct-match banner), and troubleshooting analyze (`troubleshooting::analyze` over posted symptom ids). Also emit the canonical `SymptomId → title/description` pairing MSC 1's `RouterPortForwardSymptom` array carries, as reference JSON for P12.16b to consume — `troubleshooting.rs`'s own doc comment excludes this text from the Rust port on purpose ("client-owned static data, not executable behavior"), so the real copy still needs porting somewhere, just not into the engine itself.

**Verify:** `cargo nextest run -p msc-domain --test router_guides`, plus a scoped `cargo nextest run -p msc-agent --test <new route test binary>` for whichever agent-route test file lands with the new routes.
**Commit:** `P12.16a: restore router guide content and wire real routes`
**Batch:** solo

### P12.16b — Router Guide frontend: the real guided flow

**Status:** built, awaiting Cameron's verification
**Files:** `clients/desktop-web/src/lib/sections/handbook/RouterGuidePanel.svelte` (rebuilt), `RouterGuideReader.svelte` (new), `RouterGuideTroubleshooting.svelte` (new), `routerLabels.ts` (new), `clients/desktop-web/src/lib/help/types.ts`, `clients/desktop-web/src/lib/sections/handbook/HelpSection.svelte` (passes `api` through), `clients/desktop-web/tests/e2e/browser/contract-harness.mjs`, `clients/desktop-web/tests/screens/help.test.ts`
**What:** `RouterGuidePanel.svelte` was a flat picker-list → raw `<li>` steps → raw troubleshooting-topic list against the pre-P12.16a stub catalog. Rebuilt into the oracle's real 3-screen depth against P12.16a's real content and routes, split into three components (picker stays in `RouterGuidePanel.svelte`; reader and troubleshooting are new siblings), keeping P12.16's already-Cameron-reviewed architecture call — Router Guide stays one `SegmentedControl` zone inside the single Handbook tab, not a floating modal sheet like the oracle's window:
- **Picker:** single search field (provider/brand/model/product line) hitting `GET /v1/guides/router/search`, an "I don't know my router" escape-hatch card, and the existing three grouped lists (Provider Gateways / Routers / Mesh Systems, `catalogOrder.ts`'s dedicated categories only). Search results branch on the matcher's real output: direct match, ambiguous candidate list, or a "no guide for this family yet" banner with the closest fallback guide.
- **Guide reader:** fetches `GET /v1/guides/router/{guideId}` (the composer's real section order, runtime-resolved) — What you are doing (collapsible intro + confidence badge) → Before you start (prerequisites checklist) → Values you will enter (copy-to-clipboard rows, a manual-lookup callout when the local IP can't be detected, cross-referenced against `unresolvedTokens`) → Where to look (menu breadcrumb + "similar labels may include") → Steps (numbered, checkable, "mark all done," expandable detail, "also called: X, Y") → Notes and quirks (collapsible) → "Still not working?" (the guide's own linked troubleshooting topics, plus a nudge into the full troubleshooting screen). A `no_active_server` 409 (no server is currently active agent-side) renders as its own empty state rather than a generic error, since it's a real, recoverable condition, not a bug.
- **Troubleshooting:** symptom checklist → `POST /v1/guides/router/troubleshooting/analyze` → diagnosis summary, severity-ranked likely causes (expandable), recommended actions, escalation warnings, fallback-guide suggestion — driving the real `troubleshooting::analyze` engine instead of the static topic list it printed before.

**Correction to this step's own plan text above:** it called for "a new client-owned symptom-copy data file," reasoning that the Rust troubleshooting engine intentionally excludes symptom title/description text. That's still true of the *engine*, but P12.16a already threads the real `RouterSymptom` copy through the existing `GET /v1/guides/router-catalog` response (`symptoms: RouterSymptomDTO[]`, alongside `guides`/`troubleshooting`) — so the real MSC 1 symptom text arrives over the wire already, and no separate client-owned data file was needed; `HelpSection.svelte`'s existing catalog fetch now just carries one more field.

Per `antiAIslop.md` and P12.16's own precedent for the Handbook's callouts ("no color coding across the callout styles — that would spend the accent budget on a content classification, not a defined system status"): MSC 1's per-section color-coding (purple intro / blue prerequisites / green values / orange notes) and per-guide confidence color are dropped for one neutral `Card` per section and plain-text confidence (`routerLabels.ts`). Severity in the troubleshooting screen keeps the reserved status ramp (`Badge variant="status"`, high→error/medium→warn/low→ok) because it's a real value the engine computes per analysis, not a content classification.

Verified: `cargo nextest run -p msc-domain --test router_guides` and `cargo nextest run -p msc-agent --test help_routes` (P12.16a's own Verify lines) both green before building against them, confirming the real DTO shapes used here (`RouterGuideDTO`/`ResolvedRouterGuideDTO`/`RouterGuideSearchDTO`/`RouterTroubleshootingAnalyzeResponseDTO`, all read directly from `docs/msc2/api-contract/openapi.json`'s diff and `crates/msc-agent/src/routes/help.rs`). `npm run check` and `npm run test:unit` match the established clean-checkout baseline exactly (5 pre-existing type errors; `tests/navigation/navigation.test.ts` + `tests/e2e/tauri-linux/native-renderer.test.ts` pre-existing failures, confirmed pre-existing again here by re-running `tests/e2e/browser/reset-recovery.spec.ts` against a stashed pre-P12.16b checkout and reproducing its same failure point), nothing new. Also walked the real flow live with Playwright against a production build + the (now extended) `tests/e2e/browser/contract-harness.mjs` fixture — picker search → guide reader (sections, copy-to-clipboard, checkable step) → troubleshooting (symptom select → Analyze → diagnosis) — with zero console errors; the harness's realistic router-guide fixture data and three new route stubs (`search`/`{guideId}`/`troubleshooting/analyze`) are kept (same permanent-fixture treatment as the file's existing `topics`/`concept`/`onboarding` stubs), but the one-off spec file itself was not committed, matching P12.16's own "verified... not committed — reverted after" precedent.

**Verify:** `npm run check` && `npm run test:unit`; optionally a Playwright walk against `tests/e2e/browser/contract-harness.mjs` through picker → reader → troubleshooting (the harness now serves realistic router-guide fixture data and the three new routes).
**Commit:** `P12.16b: rebuild router guide to the real guided flow`
**Batch:** solo

### P12.16c — Fix a real serde casing bug found during P12.16b's live verification

**Status:** DONE
**Files:** `crates/msc-agent/src/routes/help.rs`, `crates/msc-agent/tests/help_routes.rs`
**What:** Cameron opened Router Guide → Xfinity against his real running agent (not the Playwright fake harness) and every guide got stuck on "Loading guide…" forever, never erroring. P12.16b's own fake-harness Playwright walk had passed clean because its stub data was hand-written to already match the TypeScript types — it never exercised the real agent's actual wire bytes. Tracing this required ruling out the more likely-looking explanations first: confirmed the running agent was current (a `Repair service` binary-mismatch cycle didn't fix it either), then started a real `msc-agent` instance standalone (`MSC2_TEST_BOOTSTRAP_TOKEN` dev bootstrap, a minimal imported fixture server) and curled `GET /v1/guides/router/xfinity-gateway` directly — it answered in under 20ms with a real 200, ruling out any hang. Diffing that real JSON against the client's expected shape found the actual bug: `ResolvedItemDto` (the tagged `sections[].items[]` enum `RouterGuideReader.svelte` renders) declares `#[serde(tag = "type", rename_all = "camelCase")]` at the *enum* level, which only renames the `"type"` tag values (confirmed correct: `paragraph`/`bulletList`/`menuPath`/`step`/`note`/`troubleshootingTopic` all came through right) — it does **not** cascade into the fields of a struct-shaped variant, a well-known serde gotcha (the same class of bug P12.7b hit with `icon_url`/`iconUrl`/`iconURL`). Three fields wired as snake_case instead of camelCase as a result: `MenuPath.alternate_menu_names`, `Step.alternate_terms`, `TroubleshootingTopic.suggested_next_actions`. The client reads the correctly-cased field, gets `undefined`, and `item.alternateMenuNames.length` (etc.) throws — which in Svelte 5's effect-based rendering doesn't visibly crash, it just leaves the previous "loading" DOM in place while the real content silently fails to mount, exactly matching the reported symptom. Fixed with explicit `#[serde(rename = "...")]` overrides on all three fields (matching P12.7b's own precedent for this exact bug class) plus a regression assertion on the existing `composes_and_resolves_a_router_guide_for_the_selected_server` test asserting all three fields by their correct camelCase key with non-empty content, so a future casing regression fails loudly instead of silently.
**Verify:** `cargo nextest run -p msc-agent --test help_routes` (8/8 pass, including the new regression assertions); re-curled `GET /v1/guides/router/xfinity-gateway` against a real standalone agent instance and confirmed zero snake_case keys remain in the response.
**Commit:** `P12.16c: fix camelCase serialization on router guide item fields`
**Batch:** solo

### P12.16d — Fix duplicated "agent" in the Background Agent screen's breadcrumb

**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/setup/AgentSetupSection.svelte`
**What:** Cameron flagged the Background Agent screen reading "Local agent agent." `hostLabel` (default `'Local agent'`, otherwise a host's own display label from `App.svelte`'s `hosts` list or `hostLabelForCurrentHost()`) is already a complete host identity string — every other use of it in this file (`Managed on {hostLabel}`, `on {hostLabel}`, `{hostLabel}` alone) treats it that way. Only the top breadcrumb appended a literal `" agent"` after it, unconditionally duplicating the word whenever the label already ends in "agent" (which the built-in local-host label always does). Fixed by dropping the redundant suffix: `<p class="breadcrumb">{hostLabel}</p>`.
**Verify:** `npm run check` (same 5 pre-existing baseline errors, 0 new).
**Commit:** `P12.16d: drop duplicated "agent" from the background agent breadcrumb`
**Batch:** solo

### P12.16e — Router Guide visual polish (Cameron's live review)

**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/handbook/{RouterGuidePanel,RouterGuideReader,RouterGuideTroubleshooting}.svelte`
**What:** Four fixes from Cameron's first real-agent look at the working screen, all scoped to these three files only (not the shared `tokens.css` type scale or `Badge`/base components other screens depend on):
- **Step-number circles too big:** `RouterGuideReader.svelte`'s `.step-check` shrunk 26px→20px.
- **Drop the "Details" toggle, italicize instead:** the step body's line-clamp + expand/collapse "Details"/"Less" button is gone; the full body always renders now, styled `font-style: italic` (`.step-desc`). Removed the now-dead `expandedStepIds` state and `toggleDetail`.
- **Dark ("black") text Cameron flagged in Notes/quirks and Troubleshooting:** both `.step-check` (Reader) and the selected-symptom `.check` (Troubleshooting) used `--msc2-neutral-fill-ink` (`#14141a`, near-black) as text color against a light `--msc2-neutral-fill` circle — a legitimate "dark ink on light fill" pattern everywhere else it's used in this design system, but not one Cameron wants here. Both switched to a `--msc2-neutral-muted` (dark elevated) circle with `--msc2-text-primary` (near-white) text instead — no black anywhere in these three files now.
- **Font size too small, especially Troubleshooting:** the shared type scale (`msc2-type-body`/`-card`/`-meta`/`-overline`) is one size up in all three router-guide files (e.g. body/card 13px→15px, meta 11px→13px), and a further step up again in Troubleshooting specifically (body/card→16px, meta→14px) — done via scoped `:global()` overrides keyed off each component's own wrapper class (`.reader`/`.troubleshooting`/`.panel`), so no other screen sharing these type classes is affected.
**Verify:** `npm run check` (same 5 pre-existing baseline errors, 0 new); `npm run test:unit` (same pre-existing 2 failing files, 0 new).
**Commit:** `P12.16e: router guide visual polish from live review`
**Batch:** solo

### P12.16f — Router Guide contrast fix + italicize the remaining "Details"-era asides

**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/handbook/{RouterGuidePanel,RouterGuideReader,RouterGuideTroubleshooting}.svelte`
**What:** Two follow-ups from Cameron's second look. First, a correction: P12.16e's "black text" fix targeted the wrong thing — the `--msc2-neutral-fill-ink` circles it fixed were never what Cameron meant. The actual complaint was the ordinary `.muted`/`msc2-type-meta` body copy (symptom descriptions, the "Still not working?" intro paragraph, troubleshooting topic bodies and actions, notes body) — styled `--msc2-text-secondary` (55%-opacity white) by the shared type scale, which reads as "black" and hard to see against these cards. `.muted`'s color and a `:global(.msc2-type-meta)` color override in all three files now resolve to `--msc2-text-primary` (95%-opacity white) instead, scoped the same way P12.16e's font-size bump was (only inside `.reader`/`.troubleshooting`/`.panel` — no other screen sharing these type classes is affected). Second, Cameron asked for the remaining former-"Details" content — the step's "Also called: X, Y" line and the menu-path section's "Similar labels may include: X, Y" line — to render in italics too, matching the step description P12.16e already italicized; both get the same new `.aside { font-style: italic; }` class.
**Verify:** `npm run check` (same 5 pre-existing baseline errors, 0 new); `npm run test:unit` (same pre-existing 2 failing files, 0 new).
**Commit:** `P12.16f: fix router guide text contrast and italicize remaining asides`
**Batch:** solo

### P12.16g — Fix the real black-text bug: raw `<button>` elements don't inherit color

**Status:** DONE
**Files:** `clients/desktop-web/src/lib/sections/handbook/{RouterGuideReader,RouterGuideTroubleshooting}.svelte`
**What:** P12.16f's fix wasn't wrong, but it wasn't complete — Cameron pointed at one specific still-black spot ("Your Mac's local IP changed," a collapsed troubleshooting-topic title in the Reader's "Still not working?" section) and it exposed the actual mechanism: raw `<button>` elements do not inherit `color` from their ancestors by default (unlike every other element) — browsers apply their own UA-stylesheet button color instead (black, on this platform) — and `app.css`'s global reset only normalizes `font: inherit` for form controls, not `color`. Every button *that already set its own `color`* (`.guide-row`, `.generic-card`, `.step-check`, `.copy-btn`) was fine; the four that didn't (`.section-toggle`, `.topic-header` in the Reader; `.symptom-row`, `.cause-header` in Troubleshooting) silently fell back to black for any child text that also had no explicit color of its own (plain `msc2-type-card` spans — symptom titles, cause titles, topic titles). P12.16f's `:global(.msc2-type-meta)` color fix incidentally masked *some* of this (meta-classed text got a color from that rule, cascading in over the button's UA default), which is why only the `msc2-type-card`-classed titles were still visibly black. Fixed by adding `color: var(--msc2-text-primary)` directly to all four button classes, closing the actual gap rather than patching around it per-instance again. Not fixed globally in `app.css` (out of scope for this step; flagged here since the same latent gap likely affects any other raw `<button>` elsewhere in the codebase that doesn't set its own color).
**Verify:** `npm run check` (same 5 pre-existing baseline errors, 0 new); `npm run test:unit` (same pre-existing 2 failing files, 0 new).
**Commit:** `P12.16g: fix raw buttons not inheriting text color`
**Batch:** solo

### P12.16h — Italicize the Handbook Overview subtitle
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/handbook/HandbookBrowser.svelte`, `clients/desktop-web/tests/screens/help.test.ts`
**What:** Give the Overview topic's opening subtitle, “What this app is and what it can do for you.”, the same quiet italic treatment already used for the Handbook analogy. Scope the class to `handbook.overview` so other topic subtitles retain their existing presentation.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/sections/handbook/HandbookBrowser.svelte tests/screens/help.test.ts && npx vitest run tests/screens/help.test.ts`
**Commit:** `P12.16h: italicize the Handbook Overview subtitle`
**Batch:** solo

### P12.16i — Default the Handbook to Overview
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/handbook/HelpSection.svelte`, `clients/desktop-web/tests/screens/help.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Make the Handbook's initial topic deterministic. The agent catalog is sorted by help ID, so using its first entry could open the contextual `bedrock.runtime-unavailable` topic when the user only opened Guides. Preserve explicit `?topic=` links while defaulting an unqualified Handbook visit to `handbook.overview`.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/sections/handbook/HelpSection.svelte tests/screens/help.test.ts && npx vitest run tests/screens/help.test.ts`
**Commit:** `P12.16i: default the Handbook to Overview`
**Batch:** solo

### P12.16j — Remove the retired How MSC Works flow
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/lib/help/`, `clients/desktop-web/src/lib/sections/handbook/`, `clients/desktop-web/src/lib/api/generated.ts`, `clients/desktop-web/tests/`, `content/guides/`, `content/help/concept/`, `crates/msc-agent/src/help.rs`, `crates/msc-agent/src/routes/help.rs`, `crates/msc-agent/web-ui/`, `crates/msc-domain/src/{settings_schema.rs,world_profile.rs}`, `docs/msc2/api-contract/`, `docs/msc2/client-capability-matrix.csv`, `docs/msc2/clients/`, `fixtures/help-content/`, `fixtures/onboarding/`, `tools/phase11/help-content-check.py`, `tools/phase12/world-settings-check.py`
**What:** Remove the retired How MSC Works tab, first-launch Concept Guide stage, seven concept topics, diagrams, and concept-guide API route. Keep the Server Handbook and Router Guide available, clean their removed-topic pointers, and make setup and client reset transition directly to the real guided tour. Preserve the existing onboarding tour data, anchors, skip behavior, and restart action.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/help/FirstLaunchGate.svelte src/lib/help/onboarding.ts src/lib/help/types.ts src/lib/sections/handbook/HelpSection.svelte src/lib/api/generated.ts tests/screens/help.test.ts tests/screens/first-launch-reset.test.ts tests/e2e/browser/contract-harness.mjs tests/e2e/browser/workflows.spec.ts tests/e2e/browser/reset-recovery.spec.ts tests/e2e/tauri-linux/native-renderer.test.ts && npx vitest run tests/screens/help.test.ts tests/screens/first-launch-reset.test.ts && cd ../.. && cargo check -p msc-agent -p msc-domain && test ! -e content/guides/concept-guide.json && test ! -e clients/desktop-web/src/lib/sections/handbook/ConceptGuidePanel.svelte && ! rg -n 'concept-guide|Concept Guide|How MSC Works|conceptGuideSeen|showConceptGuide' crates/msc-agent/web-ui && ! git grep -n -E 'concept-guide|ConceptGuideDTO|How MSC Works' -- clients/desktop-web/src crates/msc-agent/src docs/msc2/api-contract/openapi.json docs/msc2/client-capability-matrix.csv content/guides content/help/concept`
**Commit:** `P12.16j: remove the retired How MSC Works flow`
**Batch:** solo

### P12.16k — Condense the Settings reset entry point
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/sections/app-settings/AppSettingsSheet.svelte`, `clients/desktop-web/tests/screens/settings-reset.test.ts`, `clients/desktop-web/tests/e2e/browser/reset-recovery.spec.ts`, `crates/msc-agent/web-ui/`
**What:** Replace the duplicate client and host reset buttons in MSC Settings with one `Reset…` entry point. Keep the existing Reset sheet responsible for choosing client versus host, host reset mode, and the exact `RESET AGENT` confirmation phrase.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/sections/app-settings/AppSettingsSheet.svelte tests/screens/settings-reset.test.ts tests/e2e/browser/reset-recovery.spec.ts && npx vitest run tests/screens/settings-reset.test.ts && npm run build && cd ../.. && ! rg -n 'canResetHost' crates/msc-agent/web-ui`
**Commit:** `P12.16k: condense the Settings reset entry point`
**Batch:** solo

### P12.16l — Keep first launch behind the splash
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/help/SplashGate.svelte`, `clients/desktop-web/tests/screens/help.test.ts`, `docs/msc2/rolling-plan.md`, `crates/msc-agent/web-ui/`
**What:** Make the splash the explicit first-launch boundary. `SplashGate` reports completion exactly once when the video ends, the bounded fallback finishes, or reduced motion skips playback; `App.svelte` mounts `FirstLaunchGate` only after that signal and the client is ready. This prevents the setup sheet or guided tour from appearing over active splash playback without changing their content or reset behavior.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/help/SplashGate.svelte tests/screens/help.test.ts && npx vitest run tests/screens/help.test.ts && npm run bundle:package-agent`
**Commit:** `P12.16l: gate first launch behind the splash`
**Batch:** solo

### P12.16m — Stabilize splash playback on reload
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/lib/help/SplashGate.svelte`, `clients/desktop-web/tests/screens/help.test.ts`, `crates/msc-agent/src/web_ui.rs`, `crates/msc-agent/tests/web_ui.rs`, `docs/msc2/rolling-plan.md`, `crates/msc-agent/web-ui/`
**What:** Make the splash video start explicitly after the browser reports it can play, preserving the muted playback policy and existing end/fallback behavior. Register the embedded agent's `.mp4` MIME type as `video/mp4` and cover that packaged response so a client reset/reload does not depend on autoplay timing or HTML content-type sniffing.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/help/SplashGate.svelte tests/screens/help.test.ts && npx vitest run tests/screens/help.test.ts && npm run bundle:package-agent && cd ../.. && cargo fmt --all -- --check && cargo nextest run -p msc-agent --test web_ui`
**Commit:** `P12.16m: stabilize splash playback on reload`
**Batch:** solo

### P12.16n — Make the splash asset stream-safe
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/static/splash_intro.mp4`, `clients/desktop-web/src/lib/help/SplashGate.svelte`, `clients/desktop-web/tests/screens/help.test.ts`, `crates/msc-agent/src/web_ui.rs`, `crates/msc-agent/tests/web_ui.rs`, `docs/msc2/rolling-plan.md`, `crates/msc-agent/web-ui/`
**What:** Restore the browser's native muted autoplay path after the explicit `canplay` start proved less stable in the reset flow. Relocate the MP4 index to the front of the reviewed splash asset so WebKit can begin decoding without waiting for the entire file, while retaining the embedded agent's `video/mp4` response type and the splash-to-onboarding completion boundary.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/help/SplashGate.svelte tests/screens/help.test.ts && npx vitest run tests/screens/help.test.ts && npm run bundle:package-agent && cd ../.. && cargo fmt --all -- --check && cargo nextest run -p msc-agent --test web_ui`
**Commit:** `P12.16n: make the splash asset stream-safe`
**Batch:** solo

### P12.16o — Replace the splash video with the app icon
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/static/msc-icon.png`, `clients/desktop-web/src/lib/help/SplashGate.svelte`, `clients/desktop-web/src-tauri/icons/`, `clients/desktop-web/src-tauri/tauri.conf.json`, `clients/desktop-web/tests/screens/help.test.ts`, `crates/msc-agent/src/web_ui.rs`, `crates/msc-agent/tests/web_ui.rs`, `docs/msc2/rolling-plan.md`, `crates/msc-agent/web-ui/`, `clients/desktop-web/static/splash_intro.mp4` (removed)
**What:** Remove the unreliable splash video completely. Show the supplied MSC icon as a still splash for exactly three seconds, skip it immediately for reduced motion, preserve the completion signal that gates first launch, and use the supplied 16–512px icon set for the Tauri application bundle. Remove the now-unused MP4 MIME mapping and test.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/help/SplashGate.svelte src-tauri/tauri.conf.json tests/screens/help.test.ts && npx vitest run tests/screens/help.test.ts && npm run bundle:package-agent && cd ../.. && rustfmt --check --edition 2024 crates/msc-agent/src/web_ui.rs crates/msc-agent/tests/web_ui.rs && CARGO_TARGET_DIR=/private/tmp/msc2-splash-target cargo nextest run -p msc-agent --test web_ui && test ! -e clients/desktop-web/static/splash_intro.mp4 && test ! -e crates/msc-agent/web-ui/splash_intro.mp4`
**Commit:** `P12.16o: replace splash video with app icon`
**Batch:** solo

### P12.16p — Convert bundled app icons to RGBA
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src-tauri/icons/`, `clients/desktop-web/static/msc-icon.png`, `clients/desktop-web/src/lib/help/SplashGate.svelte`, `crates/msc-agent/web-ui/msc-icon.png`, `docs/msc2/rolling-plan.md`
**What:** Convert the supplied RGB PNGs to RGBA PNGs without changing their visible artwork. Tauri's `generate_context!()` validates every configured bundle icon at compile time and rejects RGB-only PNGs; the splash image is converted alongside them so the same app artwork has one consistent encoding. Keep the three-second splash stage black to match the icon artwork while leaving the app's normal grey surface unchanged. Rebuild the embedded web bundle and compile the Tauri target to confirm the icon validator accepts the configured set.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/help/SplashGate.svelte && npm run bundle:package-agent && file src-tauri/icons/*.png | rg -q 'color RGBA, ' && CARGO_TARGET_DIR=/private/tmp/msc2-icon-target cargo check --manifest-path src-tauri/Cargo.toml`
**Commit:** `P12.16p: convert app icons to rgba`
**Batch:** solo

### P12.16q — Match the native startup surface to the splash
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/index.html`, `clients/desktop-web/src/main.ts`, `clients/desktop-web/src-tauri/tauri.conf.json`, `clients/desktop-web/tests/tauri/platform-boundary.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Remove the white startup flash before the webview can mount the three-second icon splash. Set the native Tauri window background to black and make the initial HTML document render the same centered MSC icon on black before JavaScript loads; `main.ts` removes that temporary loader as Svelte mounts, and the existing `SplashGate` then continues the same icon screen for its three seconds. The existing app stylesheet still takes over with the normal MSC surface after it loads. Keep this boundary-only change separate from the app's regular grey/blue surfaces.
**Verify:** `cd clients/desktop-web && npx prettier --check index.html src/main.ts src-tauri/tauri.conf.json tests/tauri/platform-boundary.test.ts && npx vitest run tests/tauri/platform-boundary.test.ts && npm run build && CARGO_TARGET_DIR=/private/tmp/msc2-startup-target cargo check --manifest-path src-tauri/Cargo.toml`
**Commit:** `P12.16q: remove the native startup white flash`
**Batch:** solo

### P12.18 — Server creation wizard (real port of `AddServerWizardView`)

**Status (decided 2026-08-27, planned not built):** P11.9/P12.11 deliberately shipped "Add Server…" as a placeholder — a name field and a version dropdown inside `ManageSheet.svelte` — and said so in that file's own comment: "not a port of MSC 1's multi-step `AddServerWizardView` — that wizard is its own scope, not attempted in this step." Cameron compared MSC 1's real wizard against that placeholder directly (screenshots) and confirmed it reads as genuinely incomplete, not a style gap. This block plans that real port. **Two scope calls Cameron made directly, asked as questions rather than decided silently:**
1. **The wizard's version picker.** MSC 1's Source step needs "list the versions for whichever flavor is currently selected," independent of whether any server exists yet. Cameron's instinct was to fix the contract first rather than ship a crippled picker. **Checked before writing these steps, and it turns out there's nothing to fix: `GET /v1/versions/create?serverType=&javaFlavor=` (P7.24) already exists, is already wired to a real handler (`versions_for_create` in `crates/msc-agent/src/routes/versions.rs`), already has test coverage, and already returns the same `VersionsResponseDTO` shape `ManageSheet.svelte`'s current dropdown consumes from the unrelated `GET /v1/versions`.** No contract-amendment step needed — the earlier research pass that flagged this as a gap missed this second route. Flagging the correction here rather than quietly dropping the precursor step it would have produced.
2. **Bedrock scope.** MSC 1's wizard creates both Java and Bedrock servers; MSC 2's current create form only ever makes Java/Paper servers. Cameron chose to build both now rather than defer Bedrock, so the Configure step (P12.18b/c) and Network step (P12.18d) both carry their Java and Bedrock branches from the start.

**Reference throughout:** MSC 1 `AddServerWizardView.swift` (2779 lines, single file — every step is a `@ViewBuilder` computed property, no separate per-step files) and the fresh/import screenshots Cameron captured in `~/Documents/MSCSS/onboarding guide/` and pasted directly. **Read `antiAIslop.md` before any of these steps.** The oracle's own selection cards (`WizardPathCard`/`WizardServerTypeCard`/`WizardFlavorCard`) are accent-tinted icon-in-a-box tiles — precisely catalog tell #6/#11 (colored icon in a tinted box; a form of decorative accent-as-selection). Port the cards' *information* (what's selectable, the RECOMMENDED/SOON badges, the disabled states) using the flat neutral selected-state treatment already established for `Card`/`Button`/`TabStrip` selection elsewhere in S0 — not the oracle's literal visual treatment. This is a genuine redesign call already settled by the already-approved design law, not a fresh decision each step needs to re-litigate.

**Structural precedent to follow, not invent:** this codebase already has one multi-step sheet, `worlds/WorldConversionWizard.svelte` — a single component, a `type Step = 'a' | 'b' | ...` union, `{#if step === 'x'}` branches, Back/Continue buttons per step, no separate step-indicator component. Follow that same shape for the new wizard rather than building a generic reusable "wizard framework." Add one small addition it doesn't have: a slim, non-decorative step counter ("Step 2 of 6" or an unfilled/filled segment row in a neutral tone, no accent color per the color budget) — the oracle's own step chips (1 Choose path — 2 Configure — …) are worth keeping in some plain form since six steps is long enough that Cameron will want to know where he is; it must not become a second accent moment (antiAIslop rule #8).

**Server-side reality (checked against the frozen contract before writing these steps, not assumed):** this is almost entirely a client-only build. `ServerCreateRequestDTO` (`POST /v1/servers/create`) already carries every field every Configure/Network/World step needs: `name`, `serverType`, `javaFlavor`, `port`, `maxPlayers`, `enableCrossPlay`, `crossPlayBedrockPort`, `enablePlayit`, `enableXboxBroadcast`, `difficulty`, `gamemode`, `worldName`, `worldSeed`, `versionId`, `minecraftVersion`, `loaderVersion`, `acceptEula`, `bedrockVersion`, `dockerImage`, `javaPath`, and `stagedModpackUploadId` (pack-driven creation is already one durable operation per P8.21, not a separate primitive). `ServerImportRequestDTO`'s two-phase `action: "scan"` → `action: "importExisting"` already matches the oracle's Upload→Review split exactly, and `ServerImportScanResponseDTO` already returns every field the Review step shows (`serverType`, `port`, `maxPlayers`, `eulaAccepted`, `worlds`, `defaultWorldName`, `javaFlavor`, `detectedMCVersion`, `detectedLoaderVersion`). Modpack staging (`/v1/modpacks/inspect`, `/v1/modpacks/import`, `/v1/modpacks/{id}/manual-file`) is fully built (Phase 8). If any step below finds a field genuinely missing once it's actually built against the real routes, flag it the same way every other Phase 12 step has — don't fake it, don't silently invent a workaround.

**Existing client code every step below must reuse, not re-implement:** `sections/components/PluginBrowserSheet.svelte` + `ImportModpackSheet.svelte` + `CurseForgeManualDownloadSheet.svelte` (P12.7) for the Add-ons step; `sections/worlds/CreateWorldSheet.svelte`'s field shapes (name/difficulty/gamemode/seed) for the World step, though the wizard collects these into the eventual `POST /v1/servers/create` payload rather than calling `CreateWorldSheet`'s own mutation (that sheet creates a world slot on an *existing* server; the wizard's server doesn't exist yet); each section's own `pollOperation` pattern (`worlds/model.ts`, `components/model.ts`, `server-editor/model.ts` all have one) for Confirm's create-progress step, matching `WorldConversionWizard.svelte`'s own use of it.

---

### P12.18a — Wizard shell + Choose Path
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte` (new), `clients/desktop-web/src/lib/sections/fleet/wizard/model.ts` (new), `clients/desktop-web/src/lib/sections/fleet/ManageSheet.svelte`
**What:** Build the wizard's outer shell (step-state pattern per `WorldConversionWizard.svelte`, non-decorative step counter, Back/Continue/Done footer) and its first real page, Choose Path — Import Existing vs Start Fresh, ported from `AddServerWizardView.swift`'s step 1. Wire it in place of `ManageSheet.svelte`'s current inline create form: "Add Server…" now opens this wizard instead of expanding the name+version row. Selection cards use the flat neutral treatment this block's own note above settles, not the oracle's accent-tinted icon boxes. No server is created yet — this step only proves the shell opens, shows two selectable paths, and Continue advances state (to a stub next page is fine; P12.18b fills it in). The step counter keeps the oracle's chip shape (numbered dot, connector, label) but strips all of it to neutral tiers/opacity, matching this block's own note above (antiAIslop rule #8) rather than the oracle's green-tinted dots/connectors. Steps past 2 render a plain "lands in a later step" placeholder with Continue disabled, so the footer shape is proven without inventing content P12.18b–i haven't built yet. Removing the old inline form also removes its three tour anchors (`ob_server_name`, `ob_server_source`, `ob_confirm_page`) from the DOM — expected and already accounted for, not a new gap: `TourOverlay.svelte` (P12.15) treats a missing anchor as a full-dim fallback card rather than an error, and P12.18i is the step that rewires those anchors onto the new wizard's real fields.
**Verify:** `npm run dev`, click Add Server…, confirm the wizard sheet opens (not the old inline form), select each path card, Continue advances; compare shell shape to MSC 1's step-chip row + footer. Run the `antiAIslop.md` checklist on the path cards specifically.
**Commit:** `P12.18a: add server wizard shell and choose-path step`
**Batch:** solo

### P12.18b — Configure step, Java path
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/ConfigureStep.svelte` (new), `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/model.ts`
**What:** Port step 2 Fresh/Configure's Java branch: Server Type card row (Java/Bedrock — Bedrock's own fields land in P12.18c), Server Name field, Server Software (Category cards Standard/Modded, then Flavor cards filtered to `implementedFlavors` — Paper/Purpur/Vanilla/Fabric/NeoForge/Forge — with RECOMMENDED badge and disabled SOON badges for the rest), Source ("Download latest" vs "Choose version…" driven by the already-real `GET /v1/versions/create?serverType=java&javaFlavor=`), Crossplay toggle (disabled when Modded or Vanilla, matches oracle's exact gate), Xbox Broadcast toggle (shown once crossplay is on). Accumulate picks into the wizard's own draft state (`model.ts`) rather than firing any mutation yet.
**Verify:** `npm run dev`, walk Configure for a Java server through every flavor/category combination, confirm crossplay/Xbox gating matches MSC 1 exactly (Modded and Vanilla both disable crossplay), confirm the version picker lists real versions per flavor; compare to MSC 1's Configure screenshot + checklist.
**Commit:** `P12.18b: add configure step, java path`
**Batch:** solo

### P12.18c — Configure step, Bedrock path
**Status:** built, awaiting Cameron's verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/ConfigureStep.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/model.ts`
**What:** Additive to P12.18b's same component: when Server Type is Bedrock, show the oracle's Bedrock branch instead of the Java Software/Source/Crossplay fields — Bedrock Version, Max Players, Xbox Broadcast toggle (always available for Bedrock, hoisted out of the Java-only `{#if}` into a shared `showXboxBroadcast` guard so both branches use the same markup/copy). MSC 1's Docker Image field is dead code there (commented out) — left out here too, not a silent drop since the oracle itself never shipped it live. **Real gap found and handled, not silently worked around:** this step's own text called for wiring Bedrock Version to `GET /v1/versions/create?serverType=bedrock`, but reading `versions_for_create` in `crates/msc-agent/src/routes/versions.rs` shows that for `serverType=bedrock` it *unconditionally* returns `bedrock_versions_response(None, None)` — it never consults any server or runtime state, because "the verified distribution selected for this runtime" can't exist before a server does. The response is a compile-time constant (`supportsVersions:false`, empty list, one fixed note string), confirmed by the route's own test `versions_for_create_defaults_bedrock_to_not_implemented`. Calling it over HTTP from the wizard would be a network round trip for a fixed string, so this step renders that same note as static copy (`BEDROCK_VERSION_NOTE` in `model.ts`, sourced from the route's literal note text) instead and ports the oracle's own free-text field (`Field`, default `"LATEST"`) rather than building a picker the real route can never populate pre-creation — this matches `AddServerWizardView.swift`'s own `bedrockVersion` field exactly, which is free text too, not a picker. Max Players uses `NumberField` (min 1, max 10000, default 10), matching the agent's own `run_create_bedrock_server` range validation and default.
**Verify:** `npm run dev`, switch Server Type to Bedrock on the Configure step, confirm the Java-only fields (category/flavor/source/crossplay) disappear, Bedrock Version/Max Players appear with the oracle's defaults, and Xbox Broadcast shows unconditionally (no crossplay gate); compare to MSC 1 + checklist. `npm run check`/`npm run test:unit` confirmed against the same clean-checkout baseline P12.18b established: same 5 pre-existing type errors, same 2 pre-existing test failures plus the file-level `tests/e2e/tauri-linux/native-renderer.test.ts` failure, nothing new.
**Commit:** `P12.18c: add configure step, bedrock path`
**Batch:** solo

### P12.18d — Network step
**Status:** built, awaiting Cameron's verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/NetworkStep.svelte` (new), `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/model.ts`
**What:** Port the shared Network step (used by both Fresh and Import paths — built generically off `WizardDraft` alone, no Fresh-specific assumption, so P12.18h can reuse it unchanged): Port Forwarding vs Tunnel (playit.gg) path cards, then the port fields that vary by context (Java port + Bedrock/Geyser port when crossplay is on; Bedrock server port when the server type is Bedrock; single Java port otherwise), wired in at step 3 for the Fresh path. `WizardDraft` gains `enablePlayit`, `javaPort`, `crossPlayBedrockPort`, and `bedrockPort` (kept numeric, unlike the oracle's plain `String` state vars, since `ServerCreateRequestDTO`'s `port`/`crossPlayBedrockPort` are both numbers and `NumberField` already range-constrains input) plus a `canAdvanceNetwork` gate mirroring the oracle's `canAdvance` case 3. Connectivity cards reuse the flat neutral card language already established by Choose Path/Configure, not the oracle's accent-tinted treatment. **Real gap found and handled, not silently worked around:** this step's own text called for "a link into the already-built router port-forward guide content (P12.16, or its current pre-P12.16 location)," but checking `docs/msc2/rolling-plan.md` found P12.16 is still `not started`, and there is no pre-P12.16 location either — `RouterGuideCatalogDTO` in the frozen contract is still an empty placeholder schema (`guides`/`troubleshooting` typed as `Record<string, never>[]`) with no route implementation behind it. The guide link/button is left out entirely rather than wired to a dead link or fake content; it's a documented gap for P12.16 (or a follow-up to this step) to fill in once real guide content exists.
**Verify:** `cd clients/desktop-web && npm run check` (0 new errors — same 5 pre-existing, unrelated failures as P12.18c) `&& npm run test:unit` (same 2 pre-existing, unrelated failures plus the file-level `tests/e2e/tauri-linux/native-renderer.test.ts` failure, nothing new), then `npm run dev`, walk Network for a Java server, a crossplay-enabled Java server, and a Bedrock server, confirm the right port fields appear each time and Continue is gated on a valid port; compare to MSC 1 + the `antiAIslop.md` checklist.
**Commit:** `P12.18d: add network step`
**Batch:** solo

### P12.18e — World step (Fresh path)
**Status:** built, awaiting Cameron's verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/WorldStep.svelte` (new), `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/model.ts`
**What:** Port step 4 Fresh/World: segmented control (New world / From backup .zip / From existing world folder), and for New World the field set `CreateWorldSheet.svelte` already established (World Name defaulting to the server name, Difficulty, Game Mode excluding Spectator, optional Seed). Collect into the wizard's draft state for the eventual single create call — do not call `CreateWorldSheet`'s own mutation, since this server doesn't exist yet. `model.ts` gets the new `WizardDraft` fields (`worldSourceMode`, `worldName`, `worldDifficulty`, `worldGamemode`, `worldSeed`, `stagedWorldBackup`) plus `canAdvanceWorld` -- this step's own `Files:` line above didn't originally list `model.ts`, corrected here since every prior Configure/Network step needed the same draft-state addition and got it listed.
**Real gap found and handled, not silently worked around:** the oracle's World Source picker has a third case, an existing world *folder*, alongside New World and a backup ZIP. `ServerCreateRequestDTO` has no field anywhere to carry a pre-made world into creation (`worldName`/`worldSeed` are the only world-shape fields it takes, confirmed against the frozen contract), so both non-fresh sources need the same staged-upload primitive `worlds/ImportWorldZipSheet.svelte` already uses for one file's bytes at a time. A folder has no such mechanism anywhere in this codebase or contract -- and `worlds/ReplaceWorldSheet.svelte` already hit this *exact* gap for the identical oracle picker ("World Folder…") and dropped it, for the same reason: "a browser file picker has no folder-to-archive equivalent." This step follows that established precedent instead of re-litigating it: the World Source segmented control offers only **New World** and **From Backup (.zip)** -- `WizardDraft.worldSourceMode` has no `folder` value to begin with, so there's no disabled/fake control standing in for it. The backup ZIP itself is real, not stubbed: picking a file stages it immediately via `api.upload('world-import', …)` (the same call `ImportWorldZipSheet.svelte` already makes), and Continue on this step stays disabled until that stage succeeds -- matching the oracle's own `canAdvance` case 4 (`selectedBackupURL != nil`). Redeeming the staged upload against the new server (the actual create-then-import chain) is P12.18g's job, same as every other draft-only field this step and its siblings collect.
**Verified live (2026-08-28):** built the client bundle and drove it with Playwright against `tests/e2e/browser/contract-harness.mjs` (the same fake-agent harness `test:e2e-browser` already uses) rather than only reading the code -- walked Choose Path → Configure (Java/Paper, named "My Survival Server") → Network → World for real. New World shows World Source/World Name (dynamic placeholder `Defaults to "My Survival Server"`)/Difficulty (Normal default)/Game Mode (Survival default, Spectator absent)/Seed, matching `CreateWorldSheet.svelte`'s field language. Switching to From Backup (.zip) hides those fields, shows "Choose backup .zip…" + "No file selected." with Continue disabled; picking `tests/e2e/browser/fixtures/world.zip` through the real file chooser staged it (fake agent's `/v1/staged-uploads` route), flipped the note to "Selected: world.zip", and enabled Continue. No console errors either screen. `npm run check` (same 5 pre-existing errors, nothing new) and `npm run test:unit` (same 3 pre-existing failures -- `tests/navigation/navigation.test.ts`, `tests/agent-install/agent-install.test.ts`, the file-level `tests/e2e/tauri-linux/native-renderer.test.ts` -- nothing new) both confirmed against this same baseline.
**Verify:** `cd clients/desktop-web && npm run check` (0 new errors) `&& npm run test:unit` (same pre-existing failures noted above, nothing new), then `npm run dev`, walk World for a new server, confirm the world-name default and field set match `CreateWorldSheet`'s own Create World flow and MSC 1's World step, then switch to From Backup (.zip) and confirm Continue stays disabled until a file is chosen; compare + checklist.
**Commit:** `P12.18e: add world step`
**Batch:** solo

### P12.18f — Add-ons/Plugins step (Fresh, Java path)
**Status:** built, awaiting Cameron's verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/AddOnsStep.svelte` (new), `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/model.ts`, `clients/desktop-web/src/lib/sections/components/PluginBrowserSheet.svelte`, `clients/desktop-web/src/lib/sections/components/ProjectDetailSheet.svelte`, `clients/desktop-web/tests/screens/add-server-wizard.test.ts`, `clients/desktop-web/tests/e2e/browser/contract-harness.mjs`, `crates/msc-agent/src/routes/components.rs`, `crates/msc-agent/tests/phase8_routes.rs`, `docs/msc2/api-contract/openapi.json`
**What:** Port step 5 Fresh/Add-ons, shown only when the chosen Java flavor has an add-on kind (mirrors `hasAddOnsStep`, now in `model.ts` alongside a new `javaAddOnKind` helper), inserted between World and Confirm — `wizardStepLabels` and `AddServerWizard.svelte`'s step-index math both became conditional on it (Confirm shifts from position 5 to 6 only when the step is shown), matching the oracle's own `hasAddOnsStep`/`confirmStepNum` shift exactly.
**First pass (2026-08-28) narrowed this step more than it needed to, corrected the same day after Cameron's review:** the step's own plan text called for reusing `PluginBrowserSheet.svelte`'s Browse Modrinth tile and `ImportModpackSheet.svelte`'s Import tile as-is. Reading both routes they call (`crates/msc-agent/src/routes/components.rs`) found **neither had a pre-create mode**: `search_catalog` (`GET /v1/catalog/search`) hard-required `state.lifecycle.active_config_server()` and resolved the loader/game-version filter *from that server*, with no query-param override; `install_component` (`POST /v1/components/install`) hard-required the same active server and installed immediately, with no staging mode for a catalog pick or a loose local jar. The first commit treated this as unfixable within the step and dropped Browse Modrinth entirely, shipping only modpack-archive staging for mod-kind flavors and a plain "add plugins after creation" message for plugin-kind ones. **Cameron pushed back** ("msc1 allow you to search modrinth or add your own mods/plugins/modpacks in this step. you should also be able to do this with msc2") — correctly: the oracle's own `AddServerWizardView.swift` reuses `ModrinthBrowserView` (the same component the active-server browser uses) in a staging mode via `onAddToStaging`/`wizardStagingConfig`, not a cut-down substitute. The real fix, done the same day:
- **`GET /v1/catalog/search` gained optional `javaFlavor`/`minecraftVersion` query params** (additive, documented in the contract's own `x-notes`): when `javaFlavor` is given and recognized, the route resolves loaders/add-on-kind/game-version from it directly and never consults the active server at all; omitted, the route's original active-server-scoped behavior is byte-for-byte unchanged. Confirmed both ways with a real black-box test in `phase8_routes.rs` (bootstrap-token, empty-fleet agent): the bare query still returns `supportsAddons:false`/`"No active server."`, the `javaFlavor=fabric` query does not, regardless of whether this sandbox can reach Modrinth.
- **`PluginBrowserSheet.svelte`/`ProjectDetailSheet.svelte` gained an additive `mode="stage"`**, mirroring the oracle's `onAddToStaging`: search passes the wizard's own `javaFlavor`/resolved Minecraft version instead of relying on an active server, and picking a result (from the flat list or a specific version in the detail sheet) calls a new `onStage`/`onStaged` callback instead of `POST /v1/components/install`. `mode` defaults to `'install'`, so every existing caller (`ComponentsSection.svelte`'s real add-on browser) is unaffected — confirmed by `test:screen-addons`'s existing 20 tests passing unchanged.
- **`POST /v1/components/install` itself was left untouched** — it still requires an active server, same as before. Staging defers every install call (both catalog picks and local jars) to `WizardDraft.pendingAddOns`, redeemed one call per item only in P12.18g, after the real create call has made the new server active — exactly mirroring the oracle's own `applyStagedAddOn`, called only once `createNewServer` has a real server directory.
- **"Add your own" .jar** uses the existing generic `POST /v1/staged-uploads` (purpose `addon-local-file`, already real, already consumed by `install_component`'s local-jar branch) — no contract change needed there. One real, documented simplification versus the oracle: `platform/types.ts`'s `pickFile` only returns one file at a time on both Tauri and the browser fallback (no `allowsMultipleSelection`, no folder-of-jars sniffing), so this is a repeatable single-file pick rather than a batch import — touching the platform layer for multi-select was judged out of scope for this correction.
- The empty-state/staged-state shape now matches the oracle's own two-state layout (big tiles when empty; a compact header-actions row plus a summary card and/or a pending-items list once something is staged) instead of the narrower single-purpose layout the first pass shipped. Mod-kind flavors (Fabric/NeoForge/Forge) get all three actions (Browse Modrinth, Import Modpack, Add your own); plugin-kind flavors (Paper/Purpur) get two (Browse Modrinth, Add your own — no modpack manifests for plugins, matching the oracle). `WizardDraft.pendingAddOns` (new) holds individual picks; `WizardDraft.stagedModpack` (unchanged from the first pass) still holds at most one modpack archive, since `ServerCreateRequestDTO.stagedModpackUploadId` is a single field.
**Real, incidental finding left alone as out of scope:** `Sheet.svelte`'s `dismissOnEscape` is bound via `<svelte:window>`, so with two sheets open at once (the wizard, and the nested Browse-Modrinth sheet) a single Escape keypress closes *both* rather than just the topmost — every open `onClose` handler fires on the same window keydown. Found while writing this step's own Playwright verification (had to close the browse sheet by its own Close button instead of Escape to keep the wizard open). Pre-existing in `Sheet.svelte`, unrelated to Add-ons specifically, and out of scope for this step to fix.
**Verified live (2026-08-28):** built the client bundle and drove it with Playwright against `tests/e2e/browser/contract-harness.mjs`, which gained a `javaFlavor`-aware `GET /v1/catalog/search` stub (returning one canned result) alongside the modpack-inspect stub from the first pass. Fabric (mod-kind): empty state showed all three tiles; Browse Modrinth opened a real search sheet with a live result, clicking Add staged it ("Added"), closing the sheet showed "Mods (1)" with the header-actions row and the pending item (title + description + Remove) in the list; Remove returned to the empty state. Paper (plugin-kind): empty state showed exactly two tiles (no Import Modpack); "Add your own…" through the real file chooser staged a `.jar` and showed it in the pending list by filename. Vanilla: step still absent entirely, `hasAddOnsStep` gating unchanged. No unexpected console errors either flavor (only the harness's own deliberate first-`/v1/status`-call 503, a known fixture behavior). `npm run check` (same 5 pre-existing errors, nothing new), `npm run test:unit` (same 3 pre-existing failures, nothing new), and `test:screen-addons` (20/20 passing, confirming `ComponentsSection`'s real browser is unaffected by the new `mode` prop) all held. `cargo fmt`/`cargo clippy -p msc-agent --all-targets -- -D warnings`/`cargo nextest run -p msc-agent --test phase8_routes` all clean.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-agent --test phase8_routes`, then `cd clients/desktop-web && npm run check && npm run test:unit && npm run test:screen-addons` (same pre-existing failures noted above, nothing new), then `npm run dev`: on the Fresh path, pick a mod-kind Java flavor (Fabric/NeoForge/Forge), walk to Add-ons, confirm all three tiles (Browse Modrinth, Import Modpack, Add your own) appear, that Browse Modrinth returns real search results and staging one shows it in a pending list with Remove, and that importing a `.mrpack`/`.zip` modpack archive shows the staged summary (pack name/version/Minecraft version/loader/file count) with its own Remove; then pick a plugin-kind flavor (Paper/Purpur) and confirm only Browse Modrinth and Add your own appear (no Import Modpack tile); then pick Vanilla or Bedrock and confirm the step is absent entirely (World's Continue goes straight to a 5-step Confirm). Compare to MSC 1 + the `antiAIslop.md` checklist.
**Commit:** `P12.18f: add add-ons step`
**Batch:** solo

### P12.18g — Confirm step and real Create (Fresh path end to end)
**Status:** built, awaiting Cameron's verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/ConfirmStep.svelte` (new), `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/model.ts`, `clients/desktop-web/src/lib/sections/fleet/ManageSheet.svelte`, `clients/desktop-web/tests/screens/add-server-wizard.test.ts`, `clients/desktop-web/tests/e2e/browser/contract-harness.mjs`
**What:** Ports the Confirm step: Display Name field (prefilled from Server Name, editable), a read-only summary card of every prior pick, the Modded "every player needs the loader" note, then the real `POST /v1/servers/create` call and its operation-progress state, and a success state. Unlike every prior lettered step, this one isn't a purely presentational component bound to `draft` — `AddServerWizard.svelte` itself now owns `displayName`/`isCreating`/`statusMessage`/`createSucceeded` and the footer's primary action (Continue → Create Server → Done), the same "parent owns the footer, each step just presents" shape already established, just extended to the one step whose footer button actually changes. `ConfirmStep` renders that state; `model.ts`'s new `createServerFromDraft` does the real work. The success state reuses `StatusDot` (a defined, labeled state, `antiAIslop.md` tell #12's own "correct usage" example) instead of the oracle's accent-colored checkmark circle, which is exactly tell #6 applied to a status readout.
**Real gap found and handled, not silently worked around:** `ServerCreateRequestDTO` (the frozen contract) has no field for a pre-create world backup, and `run_create_server`'s own `NewServerRequest` (`crates/msc-agent/src/routes/servers.rs`) always provisions `WorldSource::Fresh` regardless of what the request body carries — confirmed by reading the route directly, not assumed. So `draft.worldSourceMode === 'backupZip'` can't be part of the create call at all; `model.ts`'s new `redeemStagedWorldBackup` instead calls it as two follow-up mutations *after* the create operation succeeds: `POST /v1/worlds/import` (lands the staged backup as a *second* world slot alongside the server's own auto-created fresh one) then `POST /v1/worlds/activate` on whichever returned slot isn't the still-fresh `activeSlotId`. Both routes resolve "the active server" with no `serverId` field to pass — safe here because `finish_created_server` (the same Rust route, read directly) already calls `state.select_active_server(server_id)` once the new server is `ready`, before the create operation reports `succeeded`. `draft.pendingAddOns` (from P12.18f) redeem the same way, one `POST /v1/components/install` call per item, exactly mirroring the oracle's own `applyStagedAddOn` sequencing. Each of the three post-create redemptions (world backup, every add-on) is independently best-effort — one failing surfaces as a warning line on the success screen rather than hiding the others or failing a create that already genuinely succeeded.
**Also found:** `tests/e2e/browser/contract-harness.mjs`'s pre-existing `/v1/worlds/import` stub returned `{ result: 'Imported' }`, the wrong shape for `WorldMutationResultDTO` (`{success, message, updated}`) — dead code no test exercised until this step's own live verification needed it to return a real `updated.slots` list to redeem against. Fixed in place; confirmed no test asserted the old shape before changing it.
**Verified live (2026-08-28):** built the client bundle and drove it with Playwright against `tests/e2e/browser/contract-harness.mjs` (extended with `POST /v1/servers/create`, a generic `GET /v1/operations/:id` that resolves succeeded on first poll, `POST /v1/worlds/activate`, and `POST /v1/components/install`, plus the `/v1/worlds/import` fix above), through a throwaway spec run via `npx playwright test` and removed afterward — not checked in, matching every prior lettered step's own live-verification precedent of not adding a committed wizard spec. Three walks, all green: (1) Java/Paper with a staged local-jar plugin — Configure → Network → World (New World) → Add-ons (staged `test-plugin.jar` via the real file chooser) → Confirm showed `Paper · Standard`, `Plugins` / `1 staged`, and the display name prefilled to the server name; Create Server produced `My Survival Server created`, Done closed the sheet. (2) Bedrock — no Add-ons step shown (`hasAddOnsStep` correctly false), Confirm showed `LATEST` as the default Bedrock Version, created successfully. (3) World backup redemption — World step's From Backup (.zip) staged `tests/e2e/browser/fixtures/world.zip` (Continue stayed disabled until staged, matching `canAdvanceWorld`), Confirm showed `From backup (.zip)` / `world.zip`, and create-then-import-then-activate all completed with no error surfaced. `npm run check` (same 5 pre-existing errors, nothing new), `npm run test:unit` (same 3 pre-existing failures — `tests/navigation/navigation.test.ts`, `tests/agent-install/agent-install.test.ts`, the file-level `tests/e2e/tauri-linux/native-renderer.test.ts` — nothing new, plus 10 new passing unit tests for `canCreateServer`/`buildServerCreateRequest` in `add-server-wizard.test.ts`), and `npx prettier --check` on every changed file all held.
**Real, incidental finding left alone as out of scope:** running the full committed `tests/e2e/browser/workflows.spec.ts` in this sandbox — both before and after this step's own changes, isolated with `git stash` to confirm — shows 4 of 5 tests failing on unmodified `main`, seemingly a first-launch/splash timing issue unrelated to this step (none of the failing tests touch the wizard). Pre-existing, not this step's files, not investigated further; flagging so it isn't mistaken for a regression this step introduced.
**Correction (2026-08-28), found by Cameron's own verification:** after Create Server succeeded, clicking "Set Active" on the new server in Manage Servers did nothing visible — the sidebar picker and header stayed on the old server (or "No server selected") until picked again from the sidebar's own dropdown. Root cause: `ManageSheet.svelte`'s `setActive` posted `POST /v1/active-server` directly and never told `App.svelte`, which is where `selectedServerId`/`status.activeServerId` (the state the sidebar picker and header actually read) live — the sidebar dropdown works because it goes through `App.svelte`'s own `selectServer`, which updates that local state itself after the same call. Pre-existing in `ManageSheet.svelte` since before this phase (not something P12.18g introduced), surfaced now because creating a server through the new wizard was the first time this exact path — a freshly listed, not-yet-active server, activated from this specific sheet — got exercised. Fixed with an additive `onActivated(serverId)` callback, called after `setActive`'s mutation succeeds, wired in `App.svelte` to sync `selectedServerId`/`status.activeServerId` locally (no second network call). Also stubbed `POST /v1/active-server` in `contract-harness.mjs` (previously unstubbed) to verify: a throwaway Playwright spec (not committed, same precedent as above) confirmed red against the pre-fix code — picker stayed on the old server — and green after. `npm run check`/`test:unit`/`prettier --check` all held against the same baseline. Second commit under this same step, matching P12.18f's own precedent for a same-day correction round.
**Verify:** `cd clients/desktop-web && npm run check && npm run test:unit && npx vitest run tests/screens/add-server-wizard.test.ts` (same pre-existing failures noted above, nothing new), then `npm run dev`, walk the full Fresh path start to finish for a Java/Paper server with a plugin staged, confirm it creates for real and appears in the server list with the right settings; repeat once for a Bedrock server and once for a world-backup import. Then in Manage Servers, click "Set Active" on a server that isn't currently active and confirm the sidebar picker and header update immediately, without reselecting it from the sidebar dropdown. Compare to MSC 1 + the `antiAIslop.md` checklist.
**Commit:** `P12.18g: wire confirm step and real server creation`
**Batch:** solo

### P12.18h — Import Existing path
**Status:** built, awaiting Cameron's verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/UploadStep.svelte` (new), `clients/desktop-web/src/lib/sections/fleet/wizard/ReviewStep.svelte` (new), `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/ConfirmStep.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/model.ts`, `clients/desktop-web/src/lib/platform/types.ts`, `clients/desktop-web/src/lib/platform/browser.ts`, `clients/desktop-web/src/lib/platform/tauri.ts`, `clients/desktop-web/tests/tauri/platform-boundary.test.ts`, `clients/desktop-web/tests/e2e/browser/contract-harness.mjs` — corrected from the plan's own list, which named only the two new step files plus `AddServerWizard.svelte`: `ConfirmStep.svelte`/`model.ts` needed real edits (below), and the platform layer needed a new primitive Upload's drop zone turned out to require. Same correction shape P12.18e/f already made to their own `Files:` lines.
**What:** Ports the Import path's Upload step (drop zone plus Browse for a folder/.zip, calling the real `POST /v1/servers/import` with `action: "scan"`) and Review step (editable Max Players, Accept EULA toggle with the oracle's unaccepted-EULA warning, a world picker shown only when the scan found more than one world). Reuses P12.18d's Network step unchanged for this path's own network page — the scan's own `serverType`/`port` are folded onto `WizardDraft.serverType`/`javaPort`/`bedrockPort` directly (no separate `importPort` field), which is exactly what lets `NetworkStep.svelte` need no import-specific branch at all, matching this step's own plan text. `ConfirmStep.svelte` gained the Import path's own summary/hint branch (Method/Server type/Port/Connectivity/Max players/Active world/Other worlds/EULA, mirroring the oracle's `confirmFormView`'s `wizardPath == .importExisting` branch) alongside the Fresh one it already had, gated on a new `path` prop rather than a second component — same "parent owns the footer" shape, just a content swap. `AddServerWizard.svelte`'s `isConfirmStep`/`canContinue`/the displayName-prefill reactive block all generalized from "Fresh only" to both paths (Import's own `canAdvanceUpload`/always-true Review gate/`canAdvanceNetwork` reuse), and `beginCreate` now branches on `path` between `createServerFromDraft` (P12.18g) and a new `importServerFromDraft` (this step) — both return the same `{warnings}` shape so the footer/success state need no further branching.

**Real gap found and handled, not silently worked around (platform primitive):** the plan's own text called for "drag-and-drop... calling `POST /v1/servers/import`," but `sourcePath` names a path on the *agent's own host filesystem* to scan on disk (confirmed against `perform_raw_scan`, `crates/msc-agent/src/routes/servers.rs`) — not bytes to upload. Browse already worked on both platforms via the existing `PlatformAdapter.pickFolder`/`pickFilePath` (a real native picker on Tauri, a typed-path `window.prompt` on browser, same as `JavaTab.svelte`'s Java-executable field), but real drag-and-drop needed a new primitive: Tauri's webview intercepts native OS drag-drop before it reaches the DOM (`dragDropEnabled` defaults `true`), so an HTML5 `ondrop` handler sees an empty `dataTransfer` there, and a browser's own HTML5 drop event never exposes a real filesystem path at all (the same reason `PickedFile` returns bytes rather than a path for a browser pick). Added `PlatformAdapter.onFileDrop` (fires with real paths via `@tauri-apps/api/webview`'s `onDragDropEvent` on Tauri; never fires on browser, same "adapter that quietly can't" shape as `credentialFor`) rather than reaching into `@tauri-apps/api` directly from `UploadStep.svelte` — no section or component in this codebase imports Tauri APIs itself, they all go through `getPlatform()`. `tests/tauri/platform-boundary.test.ts` covers both adapters' new method.

**Real gap found and left alone, not silently worked around (backend contract):** `ServerImportRequestDTO` (the frozen contract) declares `enablePlayit`, and Network's reused-unchanged component collects `draft.enablePlayit` for this path exactly like Fresh does — but reading `import_raw`/`run_raw_import` directly (`crates/msc-agent/src/routes/servers.rs`) shows `RawImportOverrides` only carries `port`/`maxPlayers`/`activeWorldName`/`eulaAccepted`; `body.enable_playit` has no reader anywhere in the import route, unlike the Fresh create path's own `NewServerRequest.enable_playit`, which really does reach `provisioning::create_server`. `buildImportRequest` (`model.ts`) sends it anyway, since the contract declares the field and a future backend fix should pick it up automatically, but choosing Tunnel (playit.gg) on this path's Network step has no effect today. A real backend gap, not a client one — left for a dedicated follow-up rather than special-casing `NetworkStep.svelte` per path, a component P12.18d built explicitly to need no such branch. **Fixed the same day by P12.18j** below (the field now reaches `ConfigServer.playit_enabled` for an import same as it already did for Fresh create) — which in turn surfaced a second, bigger, pre-existing gap not specific to Import at all: no route anywhere sets the actual playit.gg secret key, so no server (Fresh or Import) can really start a tunnel yet. Scheduled separately as **P12.20/P12.20a/P12.20b** below, scope confirmed with Cameron.

**Verified as far as this sandbox allows:** `cd clients/desktop-web && npm run check` (same 5 pre-existing type errors — `auth/desktop.ts`, `navigation/route.ts` ×2, `tests/auth/desktop/desktop.test.ts` ×2 — nothing new) and `npm run test:unit` (same 3 pre-existing failures — `tests/navigation/navigation.test.ts`, `tests/agent-install/agent-install.test.ts`, the file-level `tests/e2e/tauri-linux/native-renderer.test.ts` — nothing new, plus `tests/tauri/platform-boundary.test.ts`'s 4 tests and `tests/screens/add-server-wizard.test.ts`'s 10 tests both still green) both held. `npx prettier --check` clean on every file this step touched (found `ConfigureStep.svelte`, a file this step never touched, already failing `--check` on unmodified `main` — pre-existing, flagged, not fixed here). Attempted the same live-Playwright verification P12.18e/f/g each did against `tests/e2e/browser/contract-harness.mjs` (extended here with a real `/v1/servers/import` scan/importExisting stub) via a throwaway, uncommitted spec — but in this sandbox the app's own first-launch gate (`FirstLaunchGate.svelte`) never clears even with both its documented `localStorage` flags pre-set, reproducing on unmodified `main` too (confirmed with a throwaway probe spec) and matching **exactly** the "4 of 5 `workflows.spec.ts` tests fail on unmodified `main`, a first-launch/splash timing issue" P12.18g already found and explicitly declined to chase (this file's own P12.18g entry above). Not this step's files, not investigated further for the same reason; the committed harness stub is real and ready for whenever that sandbox issue is fixed or Cameron's own `npm run dev` walkthrough substitutes for it.
**Verify:** `cd clients/desktop-web && npm run check && npm run test:unit` (same pre-existing failures noted above, nothing new), then `npm run dev`, import a real existing server folder end to end through the wizard (Upload via Browse, Review's Max Players/EULA/world picker, Network, Confirm), confirm the scanned fields populate correctly and the import completes and the new server appears in the list; on Tauri, also confirm dragging the same folder onto Upload works. Compare to MSC 1's Upload/Review steps + the `antiAIslop.md` checklist.
**Commit:** `P12.18h: add import-existing path`
**Batch:** solo

### P12.18j — Thread enablePlayit through raw import
**Status:** built, awaiting Cameron's verification
**Files:** `crates/msc-application/src/import.rs`, `crates/msc-application/tests/raw_server_import.rs`, `crates/msc-agent/src/routes/servers.rs`
**What:** Fixes the real gap P12.18h's own entry documented and left alone: `RawImportOverrides` (`import.rs`) never carried `enable_playit`, so `import_raw_server` never set `ConfigServer.playit_enabled` for an imported server — even though the oracle's own `importExistingServer` does exactly that (`AppViewModel+ServerImport.swift:194`, `cfgServer.playitEnabled = enablePlayit`), and MSC 2's own Fresh create path already does too. `RawImportOverrides` gains `enable_playit: Option<bool>` (`None` treated as `false`, matching the oracle's own default parameter), threaded from `ServerImportRequestDto.enable_playit` (already a real, already-parsed field — this was a missing read, not a missing field) through the route (`servers.rs`) into `import_raw_server`, which now sets `config.playit_enabled` alongside its other overrides. Two new tests: the route-level `raw_import_route_imports_java_folder_with_overrides_and_registers_server` now also asserts `registered.playit_enabled`, and a new `raw_server_import_defaults_playit_disabled_when_override_omitted` confirms the field defaults false when the client sends nothing. No client changes needed — P12.18h's own `buildImportRequest` was already sending `enablePlayit` in the request body; only the backend was silently dropping it. This makes the toggle itself honest; it does **not** make playit.gg functional end to end — see P12.20 for the separate, larger gap (no route anywhere sets the actual secret key) this step's own investigation surfaced and Cameron chose to schedule separately.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-application --test raw_server_import && cargo nextest run -p msc-agent --bin msc raw_import_route_imports_java_folder_with_overrides` (all green, confirmed live).
**Commit:** `P12.18j: thread enablePlayit through raw import`
**Batch:** solo

### P12.18i — Modpack-drop variant and tour anchor wiring
**Status:** built, awaiting Cameron's verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/UploadStep.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/AddServerWizard.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/model.ts`, `clients/desktop-web/src/lib/sections/fleet/wizard/ConfigureStep.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/NetworkStep.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/WorldStep.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/AddOnsStep.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/ConfirmStep.svelte`, `clients/desktop-web/src/lib/platform/types.ts`, `clients/desktop-web/src/lib/platform/browser.ts`, `clients/desktop-web/src/lib/platform/tauri.ts`, `clients/desktop-web/src/lib/help/tourAnchors.ts`, `docs/msc2/rolling-plan.md`
**What:** Port the oracle's merged step-2 variant: dropping a `.mrpack` or a CurseForge-manifest `.zip` onto Upload reads the dropped desktop file, stages and inspects it, then shows locked pack metadata plus the `Change loader/version…` disclosure and follows `Network → World → Confirm`, skipping Review. Ordinary server `.zip` paths still fall back to the existing agent-side scan. The wizard now carries the staged pack through the already-real `ServerCreateRequestDTO.stagedModpackUploadId` path. Add the wizard's real anchor ids to `KNOWN_TOUR_ANCHOR_IDS` and mount those anchors on the actual path, configure, network, world, add-ons, confirm, and wizard controls. The dropped-file read is a small platform-boundary addition because P12.18h's existing Tauri drop callback intentionally exposes only safe local paths; browsers use the existing byte picker instead.
**Verify:** `npm run dev`, drop a `.mrpack` file onto Upload and confirm the modpack-detected variant appears and skips Review; then run the guided tour start to finish and confirm it now walks every wizard step for real instead of skipping them. Compare to MSC 1 + checklist.
**Commit:** `P12.18i: add modpack-drop variant and wire tour anchors`
**Batch:** solo

### P12.19a — Freeze the reset contract and recovery states
**Status:** built, awaiting verification
**Files:** `docs/msc2/msc2-decisions.md`, `docs/msc2/clients/host-reset.md` (new), `docs/msc2/api-contract/openapi.json`, `tools/phase12/reset-contract-check.py` (new), `tools/api-contract-check.py`
**What:** Record Cameron's approved reset model and amend the public contract before implementation. Define client-only reset as a local operation with no agent call; define `POST /v1/host/reset` as an admin-only, host-scoped, operation-backed action with `configuration` and `everything` modes, explicit confirmation, running-server refusal, credential revocation, host-identity rotation, and a truthful result when the agent must restart or become unavailable. State the exact files each mode removes, that only the local desktop—not the agent HTTP route—owns service uninstall, and that remote recovery requires a fresh one-use pairing code. Add a focused checker self-test so the route, permission, request, response, and error semantics cannot drift.
**Verify:** `python3 tools/phase12/reset-contract-check.py --selftest && python3 tools/api-contract-check.py --v1-summary`
**Commit:** `P12.19a: freeze the reset contract and recovery states`
**Batch:** solo

### P12.19b — Implement host reset and remote recovery
**Status:** built, awaiting verification
**Files:** `crates/msc-application/src/` (host-reset workflow), `crates/msc-agent/src/routes/host_reset.rs` (new), `crates/msc-agent/src/routes/mod.rs`, `crates/msc-agent/src/main.rs`, `crates/msc-agent/src/cli/mod.rs`, `crates/msc-agent/src/cli/pairing.rs` (new), `crates/msc-agent/src/auth/desktop.rs`, `crates/msc-agent/src/routes/browser_session.rs`, `crates/msc-agent/tests/host_reset_routes.rs` (new), `crates/msc-agent/tests/cli_pairing.rs` (new), `crates/msc-agent/tests/desktop_auth.rs`, `crates/msc-agent/tests/browser_auth.rs`, `crates/msc-application/tests/host_reset.rs` (new), `crates/msc-api/`, `docs/msc2/clients/phase11-auth.md`
**What:** Implement the contract from P12.19a as one agent-side vertical slice. The agent remains the sole writer of host-owned state: authorize the administrator, refuse a running server, journal progress, delete only approved paths under the configured server root, clear host setup/configuration, revoke credentials, rotate the host identity, and recover safely after interruption. `configuration` preserves Minecraft server files; `everything` removes the managed server tree too. In the same slice, add the host-local command that creates a short-lived, one-use pairing code, displays it once, never logs it, and lets a remote desktop/browser use Pair Again/Add Host after reset. Keep operating-system service registration outside the HTTP route so a remote reset cannot control the computer running the client. Test both reset modes, path safety, stale-credential rejection, host-id rotation, interruption recovery, unrelated-client authorization, and re-pairing.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-application --test host_reset && cargo nextest run -p msc-agent --test host_reset_routes --test cli_pairing --test desktop_auth --test browser_auth`
**Commit:** `P12.19b: implement host reset and remote recovery`
**Batch:** solo

### P12.19c — Build local reset plumbing and the MSC Settings UI
**Status:** built, awaiting Cameron's verification
**Files:** `clients/desktop-web/src-tauri/src/lib.rs`, `clients/desktop-web/src-tauri/Cargo.toml`, `clients/desktop-web/src/lib/platform/types.ts`, `clients/desktop-web/src/lib/platform/index.ts`, `clients/desktop-web/src/lib/platform/tauri.ts`, `clients/desktop-web/src/lib/auth/desktop.ts`, `clients/desktop-web/src/lib/sections/app-settings/AppSettingsSheet.svelte`, `clients/desktop-web/src/lib/sections/app-settings/ResetSheet.svelte` (new), `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/hosts/registry.ts`, `clients/desktop-web/src/lib/help/FirstLaunchGate.svelte`, `clients/desktop-web/tests/tauri/`, `clients/desktop-web/tests/auth/desktop/`, `clients/desktop-web/tests/screens/settings-reset.test.ts` (new), `clients/desktop-web/tests/hosts/reset-state.test.ts` (new), `crates/msc-platform-macos/src/service.rs`, `crates/msc-platform-linux/src/service.rs`, `crates/msc-platform-windows/src/service.rs`, `docs/msc2/client-capability-matrix.csv`
**What:** Add the local-only native `uninstall` service action and credential forgetting, then expose **Reset this client** and **Reset this host** from Settings. The client reset clears only this device's host records, credentials, preferences, and onboarding state. Host reset is available only for an administrator, names the selected host and server root, offers configuration-only versus full deletion, shows operation progress, and leaves local hosts in the missing-agent state or remote hosts in the needs-pairing state. Keep host isolation and never expose native service controls for remote hosts.

The Settings surface must look like the rest of MSC: keep the existing `MSC Settings` medium `Sheet`, the same overline section labels, flat `Card` row structure, typography, spacing, and `Button` primitives. Use the existing confirmation treatment for the final action. Do not add a new tab bar, dashboard, nested cards, red warning panel, gradient, glow, decorative alarm icon, or unrelated visual language. Keep destructive color limited to the destructive action and its final confirmation; explain the selected host, mode, files preserved/deleted, and service consequence in plain text. Preserve Appearance and Open Server Folder unchanged.
**Verify:** `cd clients/desktop-web/src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test && cd ../../.. && npm --prefix clients/desktop-web run check && npm --prefix clients/desktop-web exec vitest run tests/tauri tests/auth/desktop tests/screens/settings-reset.test.ts tests/hosts/reset-state.test.ts`
**Commit:** `P12.19c: build local reset plumbing and settings UI`
**Batch:** stop-after

### P12.19d — Resume first launch and prove reset recovery end to end
**Status:** built, awaiting Cameron's verification
**Files:** `clients/desktop-web/src/lib/sections/setup/AgentSetupSection.svelte`, `clients/desktop-web/src/lib/sections/app-settings/ResetSheet.svelte`, `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/help/FirstLaunchGate.svelte`, `clients/desktop-web/src/lib/help/SetupIntro.svelte`, `clients/desktop-web/tests/agent-install/agent-install.test.ts`, `clients/desktop-web/tests/screens/first-launch-reset.test.ts` (new), `clients/desktop-web/tests/e2e/`, `tools/phase12/reset-smoke.sh` (new), `docs/msc2/clients/evidence/reset-recovery.md`, `docs/msc2/client-capability-matrix.csv`, `docs/msc2/rolling-plan.md`
**What:** Turn the local missing-agent state into **Install and Continue**: install/start the packaged service, bootstrap a new local credential, reconnect the host, and reopen agent-owned first-time setup. Verify SetupIntro → Concept Guide → guided tour → first-server creation, with no silent server creation. Keep installed-but-stopped as Start and Continue, incompatible as Repair, browser users on headless/Terminal guidance, and remote hosts on Pair Again. Capture evidence for client-only reset, remote configuration-only reset, remote full reset with its service still installed, and local full reset with service uninstall and fresh install. Exercise running-server refusal, both confirmation modes, old-credential rejection, fresh pairing, host setup, and first-server handoff through synthetic checks, browser tests, and Cameron's real local/remote walkthroughs.
**Verify:** `cd clients/desktop-web && npm run test:screen-help && npm run test:tauri-boundary && npm run build && npx vitest run tests/agent-install/agent-install.test.ts tests/screens/first-launch-reset.test.ts && cd ../.. && bash tools/phase12/reset-smoke.sh --synthetic && cd clients/desktop-web && npx playwright test tests/e2e/browser/reset-recovery.spec.ts --project=chromium`; Cameron's real verification: run the local-host and remote-host walkthroughs in `docs/msc2/clients/evidence/reset-recovery.md`.
**Commit:** `P12.19d: resume first launch and prove reset recovery`
**Batch:** solo

### P12.19e — Correct reset confirmation, presentation, and dev-agent delivery
**Status:** built, awaiting verification
**Files:** `crates/msc-agent/src/routes/host_reset.rs`, `crates/msc-agent/tests/host_reset_routes.rs`, `clients/desktop-web/src/lib/components/ConfirmDialog.svelte`, `clients/desktop-web/src/lib/sections/app-settings/ResetSheet.svelte`, `clients/desktop-web/src/App.svelte`, `clients/desktop-web/tests/screens/settings-reset.test.ts`, `clients/desktop-web/tests/e2e/browser/reset-recovery.spec.ts`, `clients/desktop-web/tools/prepare-agent-dev.mjs` (new), `clients/desktop-web/package.json`, `clients/desktop-web/src-tauri/tauri.conf.json`, `docs/msc2/api-contract/openapi.json`, `docs/msc2/clients/host-reset.md`, `docs/msc2/clients/evidence/reset-recovery.md`, `docs/msc2/msc2-decisions.md`, `tools/phase12/reset-contract-check.py`
**What:** Correct the three P12.19 verification findings. All host-reset confirmation entry points now use the short exact phrase `RESET AGENT`; the reset route rejects the old host-ID form and the route test covers both boundaries. Replace the generic confirmation dialog's mismatched styling with MSC's shared tiered surface, typography, scrim, and Button primitives, which also keeps client and host confirmations visually consistent. Add `prepare:agent` to the Tauri development startup so the current workspace `msc-agent` is rebuilt and copied to the platform's packaged-agent path before the desktop launches; this closes the known stale-binary path that produced HTTP 405 against a pre-P12.19 agent. Keep the installed-service digest check and Repair-service boundary intact: the desktop must not silently operate an agent binary from another build.
**Verify:** `python3 tools/phase12/reset-contract-check.py --selftest && python3 tools/api-contract-check.py --v1-summary && cargo fmt --all -- --check && cargo clippy -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-agent --test host_reset_routes && cd clients/desktop-web && npm run check && npx vitest run tests/screens/settings-reset.test.ts tests/visual/shell.test.ts && npm run prepare:agent`; then run `npx tauri dev`, use **Repair service** if the service is reported as a different build, and confirm both Configuration only and Everything accept `RESET AGENT`, reject the old long value, show the MSC-styled confirmation, and reset the selected local agent.
**Commit:** `P12.19e: correct reset confirmation, presentation, and dev-agent delivery`
**Batch:** stop-after

### P12.19f — Center reset confirmation and preserve local fresh-install recovery
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/lib/components/ConfirmDialog.svelte`, `clients/desktop-web/src/lib/sections/setup/AgentSetupSection.svelte`, `clients/desktop-web/tests/screens/settings-reset.test.ts`, `clients/desktop-web/tests/screens/first-launch-reset.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Remove the browser's native dialog positioning behavior from the shared destructive confirmation and explicitly center the MSC dialog in the application window. Keep the local full-reset recovery screen on **Install and Continue** whenever the desktop has entered the missing-agent state, even if a just-removed service's status response is briefly stale. Preserve the distinction between local fresh installation and the normal installed-service Start/Stop/Repair controls.
**Verify:** `cd clients/desktop-web && npx prettier --check src/lib/components/ConfirmDialog.svelte src/lib/sections/setup/AgentSetupSection.svelte tests/screens/settings-reset.test.ts tests/screens/first-launch-reset.test.ts && npx vitest run tests/screens/settings-reset.test.ts tests/screens/first-launch-reset.test.ts && npm run check`; then run `npx tauri dev`, reset the local host with **Everything**, and confirm the centered dialog, **Install and Continue**, fresh install, and first-time setup sequence.
**Commit:** `P12.19f: center reset confirmation and preserve fresh-install recovery`
**Batch:** stop-after

### P12.19g — Clear helper caches on full reset and replay client onboarding on client reset
**Status:** built, awaiting verification
**Files:** `crates/msc-application/src/host_reset.rs`, `crates/msc-application/tests/host_reset.rs`, `crates/msc-agent/src/auth.rs`, `crates/msc-agent/src/routes/host_reset.rs`, `crates/msc-agent/tests/host_reset_routes.rs`, `clients/desktop-web/src/lib/hosts/registry.ts`, `clients/desktop-web/tests/hosts/reset-state.test.ts`, `docs/msc2/clients/host-reset.md`, `docs/msc2/clients/evidence/reset-recovery.md`, `docs/msc2/msc2-decisions.md`, `docs/msc2/rolling-plan.md`
**What:** Extend host `Everything` reset to remove the agent-owned downloaded-helper cache, including the MCXboxBroadcast JAR, and delete the current per-server Broadcast secrets alongside legacy credentials. Keep `Configuration only` preserving helper artifacts. Fix client reset preference cleanup so the underscore-namespaced onboarding key is removed; host reset still does not erase client-local onboarding state, preserving the approved separation between host data and device data. A fresh local host reset therefore reacquires Broadcast during setup, while a client reset can replay the Concept Guide and tour.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-application --test host_reset && cargo nextest run -p msc-agent --test host_reset_routes --test cli_pairing && cd clients/desktop-web && npx prettier --check src/lib/hosts/registry.ts tests/hosts/reset-state.test.ts && npx vitest run tests/hosts/reset-state.test.ts tests/screens/first-launch-reset.test.ts tests/screens/settings-reset.test.ts`; then run a local **Everything** reset and confirm the Broadcast JAR is absent before setup reacquires it, and use **Reset this client** (or **Restart this tour**) to verify the onboarding tour replay.
**Commit:** `P12.19g: reset helper artifacts and repair onboarding replay`
**Batch:** stop-after

### P12.19h — Add the local fresh-install reset path
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/app-settings/ResetSheet.svelte`, `clients/desktop-web/src/App.svelte`, `clients/desktop-web/tests/screens/settings-reset.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Add a local-desktop-only **Reset host and client** action when the host mode is **Everything**. Reuse the existing operation-backed `POST /v1/host/reset` and the existing local service uninstall, then clear every client host record, credential, preference, and onboarding key and reload through **Install and Continue**. Keep the approved client/host reset boundaries intact: this is a convenience orchestration in the client, not a combined backend reset endpoint, and remote hosts retain their separate reset and fresh-pairing path.
**Verify:** `cd clients/desktop-web && npx prettier --check src/App.svelte src/lib/sections/app-settings/ResetSheet.svelte tests/screens/settings-reset.test.ts && npx vitest run tests/screens/settings-reset.test.ts && npm run build`; then run `npx tauri dev`, select the local host, choose **Everything**, use **Reset host and client…**, and confirm the app returns to **Install and Continue**, then proceeds through fresh setup, the Concept Guide, and the guided tour.
**Commit:** `P12.19h: add the local fresh-install reset path`
**Batch:** solo

### P12.19i — Stop the agent and quit after a fresh-install reset
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/App.svelte`, `clients/desktop-web/src/lib/platform/types.ts`, `clients/desktop-web/src/lib/platform/browser.ts`, `clients/desktop-web/src/lib/platform/tauri.ts`, `clients/desktop-web/src-tauri/src/lib.rs`, `clients/desktop-web/tests/screens/settings-reset.test.ts`, `clients/desktop-web/tests/tauri/platform-boundary.test.ts`, `docs/msc2/rolling-plan.md`
**What:** Make the local **Reset host and client** path explicitly stop a running agent before uninstalling its service, then clear client state and quit the desktop process through a native Tauri command. Keep the existing recovery fallback for service or credential cleanup failures; the next manual launch therefore starts from the missing-agent **Install and Continue** state instead of a half-reset shell with the old service still running.
**Verify:** `cd clients/desktop-web && npx prettier --check src/App.svelte src/lib/platform/types.ts src/lib/platform/browser.ts src/lib/platform/tauri.ts tests/screens/settings-reset.test.ts tests/tauri/platform-boundary.test.ts && npx vitest run tests/screens/settings-reset.test.ts tests/tauri/platform-boundary.test.ts && npm run build && cd ../.. && cargo fmt --manifest-path clients/desktop-web/src-tauri/Cargo.toml -- --check && cargo check --manifest-path clients/desktop-web/src-tauri/Cargo.toml`; then run `npx tauri dev`, choose local host → **Everything** → **Reset host and client…**, confirm the service stops and the app quits, relaunch, and confirm **Install and Continue** appears before first-time setup.
**Commit:** `P12.19i: stop the agent and quit after a fresh-install reset`
**Batch:** solo

### P12.20 — Playit.gg native provisioning and managed tunnel lifecycle
**Status:** planned 2026-08-29, not yet built
**Context:** Surfaced by P12.18h's own finding: `enablePlayit` reaches a server's config (Fresh create, and now Import via P12.18j), but no server anywhere can actually start a tunnel. Phase 9 (P9.7/P9.7a, already complete) built the pinned `playitd` acquisition and the tested helper/secret abstractions; Phase 12 must now connect them to the real Playit account and server lifecycle.

**Scope decided with Cameron (2026-08-29):** restore MSC 1's native in-app provisioning flow. The desktop sends the user's Playit credentials through the authenticated local/remote agent API; the Rust agent signs in to playit.gg, claims a new agent when needed or reuses the saved agent, stores only the resulting agent key, and creates or reuses up to three tunnels: Java, Bedrock when the server exposes a Bedrock/Geyser port, and Simple Voice Chat on UDP 24454 when SVC is installed. The password/session key is memory-only, never logged, persisted, or returned in an operation/status response. MSC 1's known 2FA limitation must be surfaced as an explicit user-facing error rather than leaving setup spinning.

The tunnel rules are timing-sensitive. A server created with Playit and Simple Voice Chat already selected must provision the voice tunnel during its initial setup. If SVC is installed later, MSC 2 must inspect the existing Playit configuration, detect whether the `MSC Voice` tunnel is already present, and prompt the user if it is missing; that later flow reuses the existing agent and authenticates again only for tunnel creation. Repeated setup must reuse named tunnels and refresh addresses without creating duplicates. Local reset removes the saved key, protected bridge file, agent ID, addresses, and prompt state, but never deletes the Playit agent or tunnels from the user's account.

This is a behavior port with two deliberate safety corrections over MSC 1: credentials remain inside the local agent boundary instead of being sent directly from the client to an undocumented third-party API, and tunnel reuse must verify the expected agent/protocol/local port before accepting an existing tunnel rather than blindly duplicating or retargeting it. The existing P12.21/P12.22 Playit address and quick-control surfaces remain consumers of this work; they are not evidence that the backend is complete.

**First-server initiation is part of this scope, not an ordinary Start/Stop detail.** MSC 1's `Initiate` action first starts a new server to generate its real configuration/world files, then stops it. During the transport setup phase it downloads the Xbox Broadcast helper when enabled and missing, runs the native Playit setup when enabled, and gives the user a non-modal progress surface with Skip actions. It then starts the server for a second pass with the configured transports, waits for the server and all applicable tunnels/sessions, stops automatically, and presents the enriched **First Start** sheet shown in the supplied screenshots. MSC 2 already has domain helpers for this two-pass idea, but no production coordinator currently drives them; the Playit work must connect to that coordinator before the phase can claim first-run parity.

### P12.20a — Freeze the native Playit setup contract and security boundary
**Status:** awaiting verification
**Files:** `docs/msc2/api-contract/openapi.json`, `crates/msc-api/src/dto/networking.rs`, `crates/msc-agent/src/routes/networking.rs`, `crates/msc-agent/tests/playit_routes.rs`, `docs/msc2/msc2-decisions.md`, `docs/msc2/rolling-plan.md`, `tools/api-contract-check.py`
**What:** Replace the paste-key-only contract with an authenticated `POST /v1/playit/setup` operation for the native flow, using the existing operation/status conventions where they fit. Define the request, progress states, cancellation/error mapping, and permission boundary for email/password submission without ever returning credentials, the agent key, or session details. Keep `GET /v1/playit`, `POST /v1/playit/start`, and `POST /v1/playit/stop`; add an explicit local reset/credential-clear contract that stops the helper first and is idempotent. `hasSecretKey` remains a boolean only. Record that the agent key is host-scoped, the password/session is memory-only, 2FA is an explicit unsupported/error state if the Playit API cannot complete it, and reset never deletes cloud-side agents or tunnels. Do not expose a browser-side Playit API client.
**Verify:** `python3 tools/api-contract-check.py --v1-summary && cargo fmt --all -- --check && cargo clippy -p msc-api -p msc-agent -p msc-application --all-targets -- -D warnings && cargo nextest run -p msc-agent --test playit_routes`
**Commit:** `P12.20a: freeze native Playit setup contract`
**Batch:** solo
**Notes:** Added `POST /v1/playit/setup` with write-only email/password input, a `playit-setup` operation contract, stable provider/error mappings, and an explicit networking permission boundary. Credentials and the temporary session stay inside the agent; only the host-scoped key is persisted, and status exposes only `hasSecretKey`. Added host-local `POST /v1/playit/reset`, which requests helper shutdown before idempotently clearing the key, agent ID, addresses, and SVC prompt state without deleting cloud-side resources. The contract also reserves removal of the per-run secret bridge for the helper implementation step. The route is mounted with a truthful transitional `setup_unavailable` response until P12.20c implements the provider transport and worker. The API checker baseline was updated from 139 to 141 for these two routes and included in this step because the declared verifier measures the route count.

### P12.20b — Client: build the native Playit setup sheet
**Status:** awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/server-editor/BroadcastTab.svelte`, `clients/desktop-web/src/lib/sections/server-editor/PlayitSetupSheet.svelte` (new), `clients/desktop-web/src/lib/sections/server-editor/model.ts`, `clients/desktop-web/tests/playit/playit-setup.test.ts` (new), `docs/msc2/antiAIslop.md`
**What:** Replace the paste-key card with the MSC 1-shaped native setup entry point: email, password, a link to create a free Playit account, a concise explanation that MSC will create/reuse the agent and applicable tunnels, and a clear warning when Playit 2FA cannot be completed. Show deliberate progress states for signing in, claiming/reusing the agent, creating/reusing Java/Bedrock/voice tunnels, waiting for the agent, and receiving public addresses. The same sheet must be callable from the first-server initiation progress surface and from ordinary Services/Broadcast settings; completing it from initiation must return to the waiting transport row rather than silently ending the first-run flow. Keep the existing Start/Stop control and the `!playit?.playitEnabled` gate, never echo credentials after submission, and present actionable errors instead of a generic failure. If an agent is already configured and the user is adding only the voice tunnel later, make that narrower re-authentication flow clear. Follow the anti-slop checklist: one purposeful setup surface, flat MSC card structure, restrained type hierarchy, no decorative gradients/glows, and no generic credential-dashboard treatment.
**Verify:** `cd clients/desktop-web && npm run check && npm run test:unit && npx vitest run tests/playit/playit-setup.test.ts`; then run `npm run dev`, open the Playit card for an enabled server, confirm the setup sheet's validation, progress, cancellation, 2FA/error, configured, and reset states without any secret or password rendered after submission.
**Commit:** `P12.20b: build the native Playit setup sheet`
**Batch:** solo
**Notes:** Added a reusable native account sheet behind `POST /v1/playit/setup`, with email/password collection, a platform-opened playit.gg account link, upfront 2FA limitation copy, frozen operation progress stages, cancellation, stable actionable errors, configured/addresses, and confirmed local-reset states. Submitted fields are cleared before long-running setup work; only the agent operation and non-secret status are handled by the client. `context="initiation"` calls `onComplete` to resume the parent waiting row and closes the sheet; Broadcast settings uses the same component and narrows existing-agent voice setup to the missing voice tunnel. The focused `npx vitest run tests/playit/playit-setup.test.ts` passes 4/4. The exact Verify chain reaches `npm run check` but remains blocked by five pre-existing errors in `src/lib/auth/desktop.ts`, `src/lib/navigation/route.ts`, and `tests/auth/desktop/desktop.test.ts`; no changed file is reported. `npm run dev` started successfully at `http://127.0.0.1:1420/`, but the in-app browser reported no available connections, so the requested visual walkthrough could not run. The generated API types remain unchanged because P12.20a's newly frozen routes are intentionally consumed through the in-scope local operation types until the later provider implementation lands.`

### P12.20c — Implement Playit sign-in, claim, and agent reuse in the Rust agent
**Status:** awaiting verification
**Files:** `crates/msc-infrastructure/src/playit_api.rs` (new), `crates/msc-infrastructure/src/lib.rs`, `crates/msc-application/src/playit.rs`, `crates/msc-application/tests/playit.rs`, `crates/msc-infrastructure/tests/playit_api.rs` (new), `crates/msc-agent/src/routes/networking.rs`, `crates/msc-agent/tests/playit_routes.rs`
**What:** Port MSC 1's `setupPlayitViaSignin` and `PlayitAPI` behind an injected Rust HTTP transport. Implement sign-in, the new-agent claim handshake, exchange for the permanent agent key, and the existing-agent reuse path. Keep the password and temporary session key in memory only; cancel or expire them on completion/failure, redact them from errors and diagnostics, and map incorrect credentials, banned accounts, unsupported 2FA, rate limits, agent-not-found, and Playit API failures to stable user-facing operation errors. Persist only the resulting agent key and agent ID through the existing secret/config stores. The agent, not the Svelte client, owns all outbound Playit requests.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-infrastructure -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-infrastructure --test playit_api && cargo nextest run -p msc-application --test playit`
**Commit:** `P12.20c: port native Playit account provisioning`
**Batch:** solo
**Notes:** Added the injected Playit account API boundary and stable, secret-free provider error mapping; the application now ports MSC 1's sign-in, claim/setup, claim acceptance, permanent-key exchange, and configured-agent reuse workflow. The agent owns the synchronous provider calls in a blocking operation worker, persists only the permanent agent key and configured agent ID, and keeps credentials/session material out of operation responses and diagnostics while honoring cooperative cancellation. The exact Verify chain passed: formatting, clippy, 4 infrastructure tests, and 10 application tests. Tunnel inventory/creation and helper secret bridging remain intentionally deferred to P12.20d/P12.20e.

### P12.20d — Provision and reuse Java, Bedrock, and voice tunnels
**Status:** built, awaiting verification
**Files:** `crates/msc-application/src/playit.rs`, `crates/msc-infrastructure/src/playit_api.rs`, `crates/msc-domain/src/networking.rs`, `crates/msc-agent/src/routes/networking.rs`, `crates/msc-agent/tests/playit_routes.rs`, `crates/msc-application/tests/playit.rs`, `docs/msc2/api-contract/openapi.json`
**What:** Port MSC 1's tunnel inventory and creation rules. Use stable names (`MSC Java`, `MSC Bedrock`, `MSC Voice`) and expected protocol/local-port metadata: Java TCP for the Java port, Bedrock UDP for the configured Bedrock/Geyser port, and voice UDP 24454 for Simple Voice Chat. List tunnels with the read-only agent key, create missing tunnels with the short-lived web session, retry while the agent changes from offline/not-found to online, and reuse an existing tunnel only when it belongs to the saved agent and matches the expected kind and origin. Never create a duplicate on repeated starts; report a mismatch that needs repair instead of silently retargeting a user's tunnel.

After provisioning, fetch and persist the public Java, Bedrock, and voice addresses, including the correct static-IP fallback for UDP tunnels. Feed those addresses into `GET /v1/playit`, `/v1/connectivity`, the sidebar/Services surfaces, and the Simple Voice Chat `voice_host` configuration. Preserve the current domain parser's public-address safety checks, but make the API inventory the authoritative source when the helper output has not yet printed an address.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-domain -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-domain --test networking && cargo nextest run -p msc-application --test playit && cargo nextest run -p msc-agent --test playit_routes`; live verification must prove one, two, and three-tunnel accounts, repeated setup without duplicates, address persistence, and an existing-tunnel mismatch error.
**Commit:** `P12.20d: provision and reuse Playit tunnels`
**Batch:** solo
**Notes:** Added the MSC 1 tunnel inventory rules to the host agent: stable Java/Bedrock/voice names, server-port and Bedrock/Geyser-port selection, fixed voice UDP 24454, read-only inventory listing, session-authenticated creation, offline/not-found retries, strict saved-agent/origin/kind/local-port reuse checks, and a stable tunnel_mismatch failure. Inventory addresses are persisted into the existing Playit status fields with Java domain and UDP static-IPv4 fallback rules; Simple Voice Chat properties are patched only when its jar is installed. Focused tests prove one-, two-, and three-tunnel provisioning, repeated setup without duplicate creation, address extraction, request authentication boundaries, mismatch refusal, and preservation of unrelated voice properties. The declared Files list does not include the pre-existing DuckDNS-only /v1/connectivity route or its DTO, so that route remains unchanged for a later scoped contract update; GET /v1/playit and the existing Services surfaces use the persisted inventory addresses.

### P12.20e — Make the managed helper consume the secret and report reality
**Status:** built, awaiting verification
**Files:** `crates/msc-application/src/playit.rs`, `crates/msc-infrastructure/src/playit.rs`, `crates/msc-infrastructure/src/helper_process.rs`, `crates/msc-agent/src/routes/networking.rs`, `crates/msc-agent/tests/playit_routes.rs`, `crates/msc-application/tests/playit.rs`
**What:** Connect the stored agent key to the actual `playitd` launch without placing it in command-line arguments or logs. Write the per-run secret bridge atomically with restrictive permissions, remove it on reset and after helper shutdown where safe, and fail clearly if the bridge cannot be created. Add the production output pump, readiness detection, diagnostics, exit reconciliation, graceful stop, and restart recovery that the current service abstractions only exercise in tests. Status must distinguish setup required, starting, waiting for tunnels, running, stopping, stopped, timed out, and failed; an operation must not remain permanently `Starting` just because no route polled the helper.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-infrastructure -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-application --test playit && cargo nextest run -p msc-agent --test playit_routes`; the tests must assert the secret value is absent from process arguments, logs, operation responses, and diagnostics while the bridge is present only with restrictive permissions.
**Commit:** `P12.20e: connect Playit secret bridge and helper supervision`
**Batch:** solo
**Notes:** Added an atomic per-run secret bridge with restrictive permissions and ownership-based cleanup, so `playitd` receives only the bridge path while the stored agent key stays out of process arguments, diagnostics, and operation/status responses. Added the production output pump, provider-secret redaction, readiness parsing, exit reconciliation, graceful-stop operations, stale-bridge cleanup on agent startup/reset, and explicit lifecycle states from setup required through failed. Focused tests cover bridge contents and `0600` permissions on Unix, redacted helper diagnostics, operation journal safety, normal and pre-readiness exits, graceful stop reconciliation, and the full lifecycle label set. The exact Verify command passed: formatting, clippy with warnings denied, 18 application Playit tests, and 8 agent route tests.

### P12.20f — Port first-server initiation and the two-pass completion flow
**Status:** built, awaiting verification
**Files:** `crates/msc-agent/src/routes/lifecycle.rs`, `crates/msc-agent/src/routes/servers.rs`, `crates/msc-application/src/provisioning.rs`, `crates/msc-application/src/playit.rs`, `crates/msc-domain/src/helper.rs`, `crates/msc-agent/tests/lifecycle_routes.rs`, `crates/msc-agent/tests/playit_routes.rs`, `crates/msc-application/tests/playit.rs`, `clients/desktop-web/src/lib/sections/fleet/wizard/ConfirmStep.svelte`, `clients/desktop-web/src/lib/sections/server-editor/FirstStartSheet.svelte` (new), `clients/desktop-web/tests/screens/first-start.test.ts` (new)
**What:** Add the production first-server initiation coordinator represented by MSC 1's `Initiate` action. Pass 1 starts Java or Bedrock long enough to generate the real configuration/world files, then stops. While initiation is in progress, ensure enabled helper downloads (including Xbox Broadcast when configured and absent), present the native Playit setup entry point, and retain per-transport states for waiting, ready, skipped, failed, and not applicable. Once the user completes or skips setup, pass 2 starts the server with Playit and any other enabled transports, waits for the server-ready signal plus every applicable Playit tunnel/address and Xbox Broadcast session, and auto-stops when the required work resolves. Use the existing Playit 75-second technical timeout and 600-second absolute safety cap without counting time spent entering credentials against the technical timeout. Only after the final stop mark first initiation complete and show a First Start sheet with the local/public connection rows, what MSC created, the next settings action, and the statement that future starts are manual. Preserve the Java/Bedrock differences and ensure an aborted/crashed first run does not falsely suppress initiation on the next Start.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-agent -p msc-application --all-targets -- -D warnings && cargo nextest run -p msc-agent --test lifecycle_routes --test playit_routes && cargo nextest run -p msc-application --test playit && cd clients/desktop-web && npm run check && npx vitest run tests/screens/first-start.test.ts`; live verification must cover fresh Java and Bedrock servers with Playit disabled, Playit enabled without credentials, successful native sign-in, Skip, helper/tunnel timeout, walked-away safety-cap cleanup, Xbox Broadcast plus Playit together, and the final First Start sheet showing the addresses that actually resolved.
**Commit:** `P12.20f: port Playit-aware first-server initiation`
**Batch:** solo
**Notes:** Added an agent-owned first-start coordinator that detects generated-world evidence, marks the first run without treating a crash as completion, holds the lifecycle operation across pass one/pass two, auto-stops after readiness, and cleans up at the 600-second safety cap. The coordinator retains Playit and Xbox Broadcast waiting/ready/skipped/failed/not-applicable states; provisioning now reports first-start eligibility for created Java and Bedrock servers. Added the Svelte First Start sheet with explicit EULA acknowledgement, native Playit setup in initiation context, helper download, Skip/Continue choices, transport polling, resolved local/public connection rows, and future-manual-start guidance. The exact Verify command passed formatting, clippy, the 11 focused agent tests, and all 18 application Playit tests; `npm run check` remains blocked by five pre-existing diagnostics in `src/lib/auth/desktop.ts`, `src/lib/navigation/route.ts`, and `tests/auth/desktop/desktop.test.ts`, so its chained Vitest segment was run separately and passed all 3 first-start tests. Live Java/Bedrock, helper timeout, safety-cap, combined transport, and final-address walkthroughs remain for Cameron's verification. Normal-start Playit lifecycle wiring remains P12.20g scope.

### P12.20g — Start and stop Playit with subsequent server runs
**Status:** built, awaiting verification
**Files:** `crates/msc-agent/src/routes/lifecycle.rs`, `crates/msc-agent/src/routes/networking.rs`, `crates/msc-application/src/playit.rs`, `crates/msc-domain/src/helper.rs`, `crates/msc-agent/tests/lifecycle_routes.rs`, `crates/msc-agent/tests/playit_routes.rs`, `crates/msc-application/tests/playit.rs`
**What:** Wire the Playit service into both Java and Bedrock normal start/stop paths after initiation is complete. Once the Minecraft process has started, begin the managed agent for an enabled server; when the server stops, crashes, is reset, or changes active host/server, stop and reconcile the helper. Preserve MSC 1's normal-start behavior when setup is missing (the server can run while the setup prompt is shown), while keeping the Services/Broadcast-tab Start and Stop controls honest. A failed Playit helper must be visible and must not leave a false Running state or an orphaned process.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-agent -p msc-application --all-targets -- -D warnings && cargo nextest run -p msc-agent --test lifecycle_routes --test playit_routes && cargo nextest run -p msc-application --test playit`; live verification must cover Java start/stop, Bedrock start/stop, missing setup, helper failure, restart, active-server changes, and automatic helper shutdown.
**Commit:** `P12.20g: connect Playit to normal server lifecycle`
**Batch:** solo
**Notes:** Registered the networking-owned Playit services with the lifecycle owner so Java and Bedrock normal starts launch Playit only after the Minecraft process starts, while first-start pass one remains setup-only and pass two reports Playit readiness back to the coordinator. Explicit stop, clean termination, crashes, host reset, and active-server changes request helper shutdown; a restart that races a graceful helper stop is queued and retried after reconciliation. Missing setup leaves the Minecraft start successful with Playit `SetupRequired`; acquisition, bridge, spawn, and poll failures are exposed as `Failed`, with failed starts removing the secret bridge. Focused tests cover helper failure/bridge cleanup, clean restart, and repeated stop handling. The exact Verify command passed: formatting, clippy, 12 focused agent tests, and 20 Playit application tests. Live Java/Bedrock start-stop, missing-setup, helper-failure, restart, active-server-change, and automatic-shutdown walkthroughs remain for Cameron's verification.

### P12.20h — Provision Simple Voice Chat at creation and prompt on later install
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/AddOnsStep.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/ConfirmStep.svelte`, `clients/desktop-web/src/lib/sections/components/ComponentsSection.svelte`, `clients/desktop-web/src/lib/sections/components/PluginBrowserSheet.svelte`, `clients/desktop-web/src/lib/sections/server-editor/BroadcastTab.svelte`, `clients/desktop-web/src/App.svelte`, `crates/msc-agent/src/routes/servers.rs`, `crates/msc-application/src/playit.rs`, `crates/msc-domain/src/app_config_schema.rs`, `crates/msc-agent/tests/playit_routes.rs`, `crates/msc-application/tests/playit.rs`, `docs/msc2/api-contract/openapi.json`
**What:** Carry creation-time SVC intent from the wizard's staged add-ons through real server creation. After the server directory and selected SVC plugin/mod exist, the initial Playit setup must detect it and create `MSC Voice` in the same provisioning operation, then write `voice_host`, `bind_address=*`, and port `24454` to the loader-appropriate `voicechat-server.properties` path. Do not create a voice tunnel for a server that does not have SVC installed.

For a later SVC install, local-file add, import, enable, or re-enable, re-run the same detection against `plugins/` and `mods/`. If Playit is enabled and no matching voice tunnel is configured, present the MSC 1-style prompt: set up the voice tunnel (re-authenticate and reuse the existing agent), disable/remove SVC, or dismiss future prompts. If the tunnel already exists, only synchronize `voice_host`; if the server is running, explain that SVC reads this value on restart. Store prompt state per server and clear it when SVC is removed/disabled or Playit voice is turned off. The prompt must not appear during creation when the voice tunnel was already provisioned.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-domain -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-application --test playit && cargo nextest run -p msc-agent --test playit_routes && cd clients/desktop-web && npm run check && npx vitest run tests/playit/playit-setup.test.ts tests/screens/add-server-wizard.test.ts tests/screens/addons.test.ts`; live verification must cover Java plugin, Fabric/Forge/NeoForge mod, Bedrock/Geyser, SVC selected during creation, SVC added after Playit setup, existing voice tunnel, missing voice tunnel, and running-server restart messaging.
**Commit:** `P12.20h: integrate Simple Voice Chat tunnel timing`
**Batch:** solo
**Notes:** Carried a staged SVC add-on marker through the Java create request and persisted the Playit voice intent on the new server; the agent still scans active jars in both `plugins/` and `mods/` before adding `MSC Voice`, so failed or absent SVC installs cannot create a voice tunnel. Initial and later Playit setup now reuse/create the named voice tunnel and synchronize `voice_host`, `bind_address=*`, and port `24454`; the first-start flow also asks for the narrower voice-only re-authentication when an existing Playit agent has no voice tunnel. Components detects the later local/catalog/import/enable/re-enable changes, keeps a host-and-server prompt dismissal key, offers setup/disable/dismiss actions, and explains the restart requirement for a running server. The exact Rust portions passed, and the three focused Vitest files passed 38 tests. `npm run check` remains blocked by five pre-existing diagnostics in `src/lib/auth/desktop.ts`, `src/lib/navigation/route.ts`, and `tests/auth/desktop/desktop.test.ts`; no changed file was reported by that checker. The API DTO, generated client type, wizard model, and first-start component were updated as transitive files needed to carry the creation intent and invoke the existing setup sheet.

### P12.20i — Complete reset, re-authentication, and host isolation
**Status:** built, awaiting verification
**Files:** `crates/msc-application/src/playit.rs`, `crates/msc-application/src/host_reset.rs`, `crates/msc-agent/src/routes/networking.rs`, `crates/msc-agent/src/routes/host_reset.rs`, `crates/msc-agent/tests/playit_routes.rs`, `crates/msc-application/tests/playit.rs`, `clients/desktop-web/src/lib/sections/server-editor/BroadcastTab.svelte`, `clients/desktop-web/tests/playit/playit-setup.test.ts`, `docs/msc2/clients/host-reset.md`
**What:** Make Reset local and complete: stop `playitd`, remove the stored agent key, agent ID, secret bridge, Java/Bedrock/voice addresses, and SVC prompt state, while leaving Playit cloud agents and tunnels untouched. A subsequent setup must be able to sign in, reuse or claim an agent, and rebuild only the applicable tunnels. Re-authentication for later voice-tunnel creation must never store the password or a reusable session. Enforce the active-host/server boundary so a remote client cannot read or mutate another host's Playit state, and ensure reset/change cannot race a running helper.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-application --test host_reset --test playit && cargo nextest run -p msc-agent --test host_reset_routes --test playit_routes`; then run the desktop reset walkthrough and confirm local artifacts are gone while the Playit dashboard still shows the agent and tunnels.
**Commit:** `P12.20i: complete Playit reset and host isolation`
**Batch:** solo
**Notes:** Playit reset now requests graceful shutdown, force-terminates if needed, polls exit events, and removes the secret bridge only after the helper is no longer supervised; the lifecycle host-reset callback uses that same bounded reset before approved server-root deletion. Native setup and local reset share a single admission permit held through the setup worker's final config/key write, so reset cannot clear state underneath an accepted setup. Reset still clears only the host's key, agent ID, public addresses, and per-server SVC prompt flags; it never calls or deletes Playit cloud agents/tunnels. BroadcastTab remains active-server-only and rejects stale async status after a host/server boundary change. The exact Rust Verify command passed: formatting, clippy, 23 application tests, and 13 agent tests. The live desktop reset walkthrough and Playit dashboard preservation check remain for Cameron's verification.

### P12.20j — Close platform acquisition and end-to-end Playit evidence
**Status:** built, awaiting verification
**Files:** `crates/msc-infrastructure/src/playit.rs`, platform release/acquisition tests, `tools/phase12/` (new evidence runner if needed), `docs/msc2/api-contract/openapi.json`, `docs/msc2/clients/evidence/playit.md` (new), `docs/msc2/rolling-plan.md`
**What:** Close the cross-platform gap left by the current pinned acquisition, which only has macOS artifacts today. Add or publish the pinned `playitd` assets and checksum paths required by supported Linux and Windows targets, or make unsupported targets explicit and prevent a misleading enabled state. Capture final evidence for first-use sign-in/claim, existing-agent reuse, Java-only, Java+Bedrock, Java+Bedrock+voice, later SVC installation, automatic stop, restart recovery, reset preservation of cloud resources, and the public addresses shown by P12.21/P12.22. This is the Playit phase evidence step; it must not be replaced by a generic full-workspace run.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-infrastructure -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-infrastructure -p msc-application -p msc-agent && python3 tools/api-contract-check.py --v1-summary`; Cameron's live verification follows `docs/msc2/clients/evidence/playit.md` on each supported platform.
**Commit:** `P12.20j: prove Playit provisioning across supported platforms`
**Batch:** stop-after
**Notes:** Added exact pinned Playit release mappings for MSC's universal macOS helper, upstream Linux x86_64/arm64 assets, and the signed upstream Windows x86_64 asset, with SHA-256 checksums enforced by the existing acquisition boundary. Windows arm64 is explicit and fails before download or launch because no pinned upstream asset exists; the API contract documents the matrix and makes clear that `playitEnabled` is a saved preference rather than helper readiness. Added focused platform-pin tests and the live evidence worksheet covering sign-in/claim, agent reuse, Java/Bedrock/voice combinations, later SVC setup, lifecycle recovery, reset preservation, and P12.21/P12.22 address display. The exact Verify command passed formatting and Clippy, then nextest stopped after 31 of 1,210 tests because unchanged Bedrock production tests reported unavailable-sidecar provisioning failures (`bedrock_production_smoke` and `bedrock_production_cli`); no in-scope failure was reported, and the API summary was not reached because nextest failed fast. Cameron's live supported-platform walkthrough remains pending.

### P12.21 — Sidebar: How to Connect + Maintenance
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/lib/components/shell/ControlSidebar.svelte`, `clients/desktop-web/src/lib/components/ApplicationShell.svelte`, `clients/desktop-web/src/lib/components/shell/sidebar/HowToConnectSection.svelte` (new), `clients/desktop-web/tests/visual/shell.test.ts`
**Built (2026-08-28):** `App.svelte` needed no change — it already passes `api={screenApi}` into `ApplicationShell`, so only `ApplicationShell` (one line, `{api}` into `<ControlSidebar>`) and `ControlSidebar` itself picked up the new prop. `HowToConnectSection.svelte` reuses `ConnectionCard.svelte`'s exact derivation (`sourceTag`, `javaIp`/`javaIpFallback`, `geyserIp`/`geyserIpFallback`, masking) restyled as stacked rows instead of a two-column card, refetching `/v1/connectivity`+`/v1/config/geyser` whenever `activeServerId` changes while expanded. Maintenance's Directory/Logs buttons live inline in `ControlSidebar.svelte` (no need for a separate file) using `getPlatform().revealInFileManager`, same call `FilesSection.svelte` already makes; used `Icon` (`folder`/`note`, the neutral content-icon set) rather than `ShellIcon`, since `ShellIcon`'s `sidebar`/`console` glyphs mean panel-visibility chrome, not "open this folder." Added a `visibleSections` filter so How to Connect's whole disclosure hides with no server selected, oracle-faithful; Maintenance always shows with its buttons disabled instead — this also lays the groundwork P12.22's Console Access visibility rule will extend the same filter with.
**Correction (2026-08-28), from Cameron's own visual review:** the first build above reused `ConnectionCard.svelte` outright (its boxed, two-column, Local/Public-toggled card), when the oracle's actual sidebar treatment is plainer — one eye toggle, then a flat list of icon + label + a single pill-shaped value row per method, every method with data shown at once rather than switched between. Rewrote `HowToConnectSection.svelte` to that shape: dropped the `SegmentedControl`, kept the exact same underlying data (`/v1/connectivity`, `/v1/config/geyser`) and the same honest "Not reported by this host yet" fallback for Java's still-unreported LAN address, and now renders up to four always-visible rows (Java·same Wi-Fi, Java·public/anywhere, and — only when Bedrock or Geyser is present — Bedrock·same Wi-Fi, Bedrock·public/anywhere), each either a colored pill (masked value + inline copy icon, click anywhere to copy) or plain fallback text when there's no value yet. Row colors are MSC2's own three already-established semantic tokens, not MSC 1's literal hues: `--msc2-status-warn` (Java·LAN), `--msc2-status-bedrock` (Bedrock·LAN — the same blue `Badge.svelte`/`StatusDot.svelte` already use as this app's Bedrock identity color, more consistent than porting MSC 1's arbitrary green), `--msc2-status-ok` (any public/reachable row, both platforms). Confirmed along the way that `/v1/connectivity`'s `joinAddress` already carries a combined `host:port` string (`crates/msc-application/src/network_diagnostics.rs`), so the public rows use it directly with no separate port field — and noted, without chasing (out of scope for a styling fix), that the same handler only ever actually produces a `duckdns`-sourced address today; `playit`/`public_ip` are real enum values in the contract that the live route never returns yet. The now-unused `gamePort` prop was removed from the component and its call site.
**Second correction (2026-08-28), same-day, from Cameron's own visual review:** two more fixes to the row shape. Dropped the per-row icon (cup/box/globe) next to each label — `HowToConnectSection.svelte` now renders just the label text above each pill, no `row-icon` markup or its tone-colored SVGs; the pill itself keeps its tone-tinted background, so rows stay visually distinct without an icon. And moved the eye toggle out of the section's own content and up into the "How to connect" disclosure header row itself, right of the section title, icon-only (no "Show"/"Hide" text) — `showAddresses` is now owned by `ControlSidebar.svelte` and passed down as a prop, since the header that renders the toggle and the content that reads it are different components; this required splitting the header's single clickable `<button>` into a `.disclosure-header` flex row holding two separate buttons (`.disclosure-toggle` for expand/collapse, `.eye-toggle` for show/hide) since a `<button>` can't nest another interactive element. The eye toggle is specific to How to Connect and doesn't render for the other three sections.
**Third correction (2026-08-28), same-day, from Cameron's own visual review:** the eye toggle from the previous correction rendered in the header regardless of whether the section was actually expanded, and stayed in whatever shown/hidden state the user last left it even after collapsing and reopening the section. Both fixed: the toggle button now only renders when `expanded['How to connect']` is true (so it disappears with the rest of the row content when collapsed, rather than sitting in a closed header controlling nothing visible), and a reactive statement (`$: if (!expanded['How to connect']) showAddresses = false;`) resets `showAddresses` to hidden every time the section collapses, so reopening it always starts masked — no remembered "shown" state carried across a closed disclosure, Cameron's own explicit call.
**What:** Thread the already-existing `api: ScreenApi` prop one level further, from `ApplicationShell` into `ControlSidebar` (an additive one-line prop pass in each file, matching the precedent P12.10 already established when it threaded the same prop into `ConsoleDock`). Replace the "How to connect" disclosure's placeholder with a real port of MSC 1's `HowToConnectSidebarSection` (`SidebarView.swift:690-833`): on expand (and on active-server change while expanded), fetch `GET /v1/connectivity` and `GET /v1/config/geyser` the same way `HomeSection.svelte`/`ConnectionCard.svelte` already do for the Overview tab's Connection Info card, and render one compact row per real method — Java (LAN/public via `connectivity.joinAddress`+`joinAddressSource`) and, only when Geyser is installed or the server is Bedrock, a Bedrock row — reusing the existing global Show/Hide-addresses masking convention and a copy-to-clipboard button per row, matching the oracle's row shape (icon, label, monospace value, copy). Drop the oracle's fourth row, "Xbox · add friend" (the broadcast alt account's gamertag): P12.12 already found `/v1/broadcast/credentials` is POST-only in the frozen contract — nothing reads a saved gamertag back — so there is no real value to show; note the omission rather than fabricating one. Replace the "Maintenance" disclosure's placeholder with two real buttons, Directory and Logs, both using the already-built `PlatformAdapter.revealInFileManager` (the same call `FilesSection.svelte`/`AppSettingsSheet.svelte` already make): Directory opens the selected server's own `directory` (already on `ServerDTO`, already shown verbatim under the server name in `DetailsHeader`); Logs opens `${directory}/logs` client-side (matching MSC 1's own `serverDir.appendingPathComponent("logs")` — no new backend route needed; if the folder doesn't exist yet on a never-started server, let the existing reveal-call error path surface that, same as every other reveal caller, rather than adding folder-creation plumbing nothing else in this client has). Do **not** port the oracle's third button, Archives (the downloaded-JAR-archive browser, `TemplatesView.swift`) — Cameron already decided to drop this exact feature when it came up as an MSC Settings card during P12.14 planning ("real gaps, never built, not superseded by anything... **drop** Config Recovery... and Archives (auto-archive-downloaded-JARs toggle)"); this step applies that same already-made call to its second appearance rather than reopening the question. Gate How to Connect on a server being selected (oracle-faithful); Maintenance's two buttons stay visible but disabled with no server selected.
**Verify:** `cd clients/desktop-web && npm run check && npm run test:unit && npx vitest run tests/visual/shell.test.ts`, then `npm run dev`: select a Paper server with Geyser installed, expand How to Connect, confirm real Java+Bedrock rows with working Show/Hide and Copy; switch to a Bedrock server and confirm only the Bedrock row shows; switch to a server with no Geyser and confirm only Java shows; expand Maintenance and confirm Directory/Logs actually reveal the right folders in Finder/Explorer. Compare row shape and copy affordance to `SidebarView.swift`'s `HowToConnectSidebarSection`/`MaintenanceButton` and the `antiAIslop.md` checklist.
**Commit:** `P12.21: port sidebar How to Connect and Maintenance sections`
**Batch:** stop-after

### P12.22 — Sidebar: Console Access → Services (Xbox Broadcast + playit.gg quick control) + Quick Commands
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/lib/components/shell/ControlSidebar.svelte`, `clients/desktop-web/src/lib/components/shell/sidebar/ConsoleAccessSection.svelte` (new), `clients/desktop-web/src/lib/components/shell/sidebar/QuickCommandsSection.svelte` (new)
**Built (2026-08-28) — real findings, three corrections to this step's own plan text:**
1. **The `/v1/command` Bedrock-translation question this step asked itself is answered: false.** Read `crates/msc-agent/src/routes/commands.rs` — for a Bedrock server it only strips a leading `/` and forwards the raw string to the sidecar (`send_bedrock_command` → `BedrockRuntimeState::command()` → a `SidecarFrame::Command` carrying the string verbatim, no rewriting anywhere in that path). `crates/msc-domain/src/commands.rs`'s own command catalog confirms this from the other direction: `whitelist`/`save-all`/`reload` are all marked `supports_bedrock: false`, `allowlist` is marked `supports_java: false`, with no shared entry covering both. The contract's `x-notes` claim doesn't hold at this layer — the real save/allowlist translation that exists (`crates/msc-application/src/backups.rs`'s `save hold`/`save-all flush`/`save resume`/`save-on`) is internal to the backup system's own orchestration, never exposed through `/v1/command`. So `QuickCommandsSection.svelte` keeps the oracle's manual branching client-side exactly as planned, now on confirmed grounds rather than an assumption.
2. **Auto-start is not Java-only.** Re-reading `SidebarView.swift` found `BedrockCrossPlatformSidebarSection` carries the same "Xbox Broadcast auto-start" toggle as the Java section — this step's own plan text said "Java only," which was wrong. The real difference is Java gates the whole Start/Stop+autostart block behind the helper JAR being installed first (`XboxBroadcastSidebarRow`'s `isXboxBroadcastHelperInstalled`, showing "Set up in Edit Server → Broadcast" until then); Bedrock has no such gate since Xbox Broadcast ships as a background process alongside BDS, not a separate downloadable JAR. `ConsoleAccessSection.svelte` fetches `broadcastJarStatus` alongside status/autostart and gates on `isBedrock || jarStatus.installed`.
3. **`JAVA_FLAVOR_CATALOG` (`sections/fleet/wizard/model.ts`) only covers the 6 flavors the creation wizard offers**, not the oracle's full 9-case `JavaServerFlavor` enum — an imported or pre-P12.18 server can carry `pufferfish`/`spigot`/`quilt`, none in that catalog. Using it as-is for Console Access's visibility rule would wrongly hide Console Access on a Pufferfish or Spigot server (both real plugin/Geyser-capable flavors). Added a small local fallback in `ControlSidebar.svelte` (`javaFlavorCategory`) that still calls the wizard's own `crossPlayUnavailable` unchanged, just widens the category lookup to the oracle's real fixed set (pufferfish/spigot standard, quilt modded, confirmed against `JavaServerFlavor.swift`'s own category groupings) before falling through to it.
**Correction (2026-08-28), from Cameron's own visual review:** two fixes, same commit. Dropped Quick Commands' "Start the server to use Quick Commands." hint text — the section's controls were already visibly disabled while stopped (the oracle's own `.disabled(!isServerRunning)` treatment, ported as-is), so the caption was redundant chrome, not new information; the stat strip (online/TPS) still only renders while running. Also removed `.disclosure-content`'s 19px left margin in `ControlSidebar.svelte` — it existed to align a section's expanded content under its label text past the chevron, but read as an awkward, unintended gap between the sidebar's own left edge and every disclosure section's content (Console Access, How to Connect, Maintenance, and Quick Commands all inherit the same rule). Content now sits flush with the sidebar's left padding, same as every other block in the rail.
Also: Quick Commands' Difficulty/Gamemode/Whitelist controls pre-populate from the current `GET /v1/settings` value on expand (matching the oracle's own `refreshFromProperties()`), reading field keys `difficulty`/`gamemode`/`white-list` (hyphenated, confirmed against `crates/msc-agent/src/routes/settings.rs`) — cosmetic only, every actual mutation still goes through `/v1/command`, never `/v1/settings`.
**Correction (2026-08-28), Cameron's own design call, not an oracle port:** Console Access is reframed as MSC2's own general external-services panel rather than an Xbox-Broadcast-specific one. Checked `crates/msc-infrastructure/src/helper_process.rs`'s `HelperKey` (the shared supervision abstraction "for programs such as Playit or Xbox Broadcast") for every real construction site in the codebase — there are exactly two, `"xbox-broadcast"` and `"playit"`, confirming those are the only two external services MSC2 manages today. `ConsoleAccessSection.svelte` now renders both: a playit.gg block (status dot, Start/Stop via the same `serverEditorPaths.playit`/`playitStart`/`playitStop` `BroadcastTab.svelte` already uses, disabled with an "Enable in Edit Server → Broadcast" hint when `!playit.playitEnabled`) alongside the existing Xbox Broadcast block, each under its own `msc2-type-overline` label (the same shared utility class `BroadcastTab.svelte` already uses for its own "Xbox Broadcast"/"Playit" headers). Since playit is a generic tunnel, not crossplay-specific, it applies to every server type/flavor — unlike Xbox Broadcast, whose crossplay visibility rule doesn't fit it. `ControlSidebar.svelte`'s `showConsoleAccess` (gated on crossplay) is renamed `showXboxBroadcast` and now only gates the Xbox Broadcast sub-block; the whole Console Access section's own visibility broadens to "any server selected," the same simple gate How to Connect already uses, so playit is reachable on Vanilla/modded Java servers where Xbox Broadcast itself stays hidden.
**Second correction (2026-08-28), same-day, from Cameron's own visual review:** the section itself is renamed "Services" (`DISCLOSURE_SECTIONS`/`expanded` in `ControlSidebar.svelte` and every string comparison against it) — "Console Access" was Xbox Broadcast's own MSC 1 name, no longer accurate now that the section covers playit too; `ConsoleAccessSection.svelte` (the file/component) keeps its name since it's still specifically the console-access-style external-services panel, just documented as rendering under the new label. Also tightened spacing throughout `ConsoleAccessSection.svelte` (`.console-access` gap 14px→8px, `.service` gap 6px→4px, `.row` gap 8px→6px) — the two-service layout read as too spaced out. And replaced Xbox Broadcast's auto-start `Toggle.svelte` switch + its own separate row with a plain native `<input type="checkbox">` inline on the same row as the status dot/label and Start/Stop button, per Cameron's explicit ask for "just a checkbox on the same line as the start button" — `.row` gained `flex-wrap: wrap` as a fallback in case the sidebar's ~200px content width can't fit dot+label+checkbox+button on one line at some font-scale.
**Third correction (2026-08-29), from Cameron's own visual review:** the label-to-row gap still read too large even after the previous pass's `.service` gap reduction — the culprit was `.msc2-type-overline`'s inherited (unset) line-height adding extra box height above/below the 10px label text on top of the flex `gap`, not just the gap value itself. Fixed with a scoped `.service :global(.msc2-type-overline) { line-height: 1; }` override (global class, local selector — doesn't touch the shared utility's definition in `tokens.css`) plus a further `.service` gap reduction (4px→3px). playit.gg and Xbox Broadcast use the identical `.service` markup/class, so this applies to both equally, not just one.
**Fourth correction (2026-08-29), from Cameron's own visual review:** still too large, comparing directly against How to Connect's own label-to-value spacing (its `.row-label` span sits 5px above its value/fallback). Reduced `.service` gap once more (3px→2px). Flagging honestly rather than guessing indefinitely: this agent has no way to render the client and measure the actual pixel gap in this environment, so if 2px still doesn't match, the fastest path is Cameron naming the target (a devtools-measured px value, or "tighter"/"looser") rather than another round of visual-only feedback.
**What:** Builds on P12.21's `api` wiring. **Console Access** ports MSC 1's `BedrockCrossPlatformSidebarSection`/`CrossPlatformAccessSidebarSection` (`SidebarView.swift:522-681`): visible only when the active server is Bedrock, or Java with `!crossPlayUnavailable(category, flavor)` — reuse that exact helper and the flavor→category lookup already exported from `sections/fleet/wizard/model.ts` (built for the creation wizard's own Crossplay gating) rather than reimplementing the modded/vanilla exclusion a second time. Fetch `GET /v1/broadcast/status` and `GET /v1/broadcast/autostart` on expand, render a status dot (Running/Stopped) + Start/Stop (`POST /v1/broadcast/start`/`stop`) and, Java only, an auto-start toggle (`POST /v1/broadcast/autostart`) — the same three routes `BroadcastTab.svelte`'s own Xbox Broadcast card already calls; reuse its `toggleBroadcast`/`toggleAutostart` logic rather than rewriting it, just condensed to the oracle's smaller footprint (no Join-address row, no helper-JAR-download row — those stay Broadcast-tab-only, matching the oracle's own contextual-help framing: "quick-control surface... use Edit Server for the deeper setup path"). **Quick Commands** ports `QuickCommandsView.swift` in full, scoped to this disclosure section (fetch its own `/v1/performance`-derived TPS and online count on expand while running — Overview's copies are scoped to that tab, not shared shell state, so this is its own small poll, not a reuse): World (Time of Day: Dawn/Dusk/Night, Weather: Clear/Rain/Storm) and Settings (Difficulty, Gamemode, Whitelist) rows that each send one live console command via the existing `POST /v1/command` route — read the exact command strings from `AppViewModel+ServerControls.swift:475-528` rather than guessing (`time set 1000/13000/18000`, `weather clear/rain/thunder`, `difficulty <x>`, `defaultgamemode <x>`, `whitelist on/off`); and an Actions row (Save All → `save-all`, Reload → `reload`, Java only — Bedrock has no plugins to reload). Before wiring Whitelist/Save All, read `crates/msc-agent`'s `/v1/command` handler: the contract's own `x-notes` already claims "allowlist/save/operator commands are translated to Bedrock files and commands behind this route," which — if true — means the client can send one Java-shaped command uniformly instead of re-implementing the oracle's manual `whitelist`→`allowlist`/`save-all`→`save hold`+`save resume` Bedrock branching; confirm which is actually true and record it in this step's own entry rather than assuming. The whole section is disabled (not hidden) while the server isn't running, with the oracle's own "Start the server to use Quick Commands" caption.
**Verify:** `cd clients/desktop-web && npm run check && npm run test:unit && npx vitest run tests/visual/shell.test.ts`, then `npm run dev`: on a running Paper server, expand Console Access and confirm Start/Stop and auto-start actually control the real Xbox Broadcast helper (cross-check against the Broadcast tab's own status for the same server); expand Quick Commands and confirm each World/Settings button sends the exact command visible in the console dock, Whitelist toggles correctly, Save All/Reload run; switch to a running Bedrock server and confirm Reload is absent and Whitelist still works (via whatever translation this step's own investigation found, or the oracle's manual branching if the route doesn't translate it); switch to a Vanilla or modded Java server and confirm Console Access is hidden entirely. Compare to `SidebarView.swift`/`QuickCommandsView.swift` and the `antiAIslop.md` checklist.
**Commit:** `P12.22: port sidebar Console Access and Quick Commands sections`
**Batch:** solo

### P12.23 — Freeze world/server ownership and the world profile contract
**Status:** built, awaiting verification
**Files:** `docs/msc2/msc2-decisions.md`, `docs/msc2/api-contract/openapi.json`, `tools/api-contract-check.py`, `crates/msc-api/src/dto/worlds.rs`, `crates/msc-domain/src/lib.rs`, `crates/msc-domain/src/world_profile.rs` (new), `crates/msc-domain/src/settings_schema.rs`, `content/help/handbook/worlds-backups.md`, `content/help/concept/worlds.md`, `content/help/concept/active-world.md`, `content/help/concept/settings.md`
**What:** Turn the agreed ownership model into the authoritative contract before implementation. A `WorldProfile` belongs to each `WorldSlot` and travels with that slot: identity (name/level name and seed), generation (world type, structures, biome/generator options, bonus chest where supported), gameplay (difficulty, default game mode, hardcore, gamerules and cheats/experiments where supported), and detected safety state. The server profile remains the owner of ports, player limits, MOTD, authentication, allowlist/ops, process/runtime policy, view/simulation distance, crossplay/broadcast/tunnel settings, and other values that govern the running server rather than one world; classify borderline properties by the actual Java/Bedrock runtime behavior instead of by which screen currently displays them. Define which fields are creation-only, apply-on-activation, live-safe, or restart-required, and make the active world a projection of its saved profile rather than a second source of truth. Add a versioned nested DTO with capability/help metadata instead of duplicating world fields in the server settings response. Record the model and the exact user-facing distinction in the Handbook and concept content, including the wording: “This world has its own gameplay and generation settings. They are saved with the world and applied whenever this world becomes active. Server settings such as ports and player limits apply to every world.”
The inventory explicitly covers the Java create-world choices (world type/flat preset, seed, structures, bonus chest, hardcore, commands, data packs, and gamerules) and Bedrock choices (generator/seed, difficulty and default mode, cheats/achievements, experiments, coordinates, starting map/bonus chest, and supported gameplay toggles), while documenting which similarly named values are actually server-wide.
**Verify:** `cargo fmt --all -- --check && cargo nextest run -p msc-domain --test settings_schema --test world && python3 tools/api-contract-check.py --v1-summary`
**Commit:** `P12.23: freeze world settings ownership contract`
**Batch:** solo
**Notes:** MSC1's editor groups some world and server values together, so the ownership split follows runtime/storage behavior instead of presentation. This step freezes the profile vocabulary, lifecycle metadata, DTO shape, and documentation; P12.24 remains responsible for attaching, persisting, migrating, detecting, and populating profiles on world slots. Legacy `/v1/worlds` summaries and mixed `/v1/settings` keys remain compatibility projections until P12.25 adds profile application and the separate server-settings surface. The API contract checker baseline was corrected from 134 to 137 for the three valid router-guide operations already present in P12.16a.

### P12.24 — Persist, migrate, and detect per-world settings
**Status:** built, awaiting verification
**Files:** `crates/msc-domain/src/world.rs`, `crates/msc-domain/src/nbt.rs`, `crates/msc-infrastructure/src/world_store.rs`, `crates/msc-application/src/worlds.rs`, `crates/msc-application/src/provisioning.rs`, `crates/msc-agent/src/routes/worlds.rs`, `crates/msc-api/src/dto/worlds.rs`, `crates/msc-domain/tests/world.rs`, `crates/msc-domain/tests/world_nbt.rs`, `crates/msc-infrastructure/tests/world_store.rs`, `crates/msc-application/tests/world_slot_crud.rs`, `crates/msc-application/tests/world_import_reconciliation.rs`, `crates/msc-application/tests/world_conversion.rs`, `crates/msc-application/tests/bedrock_world_backup.rs`
**What:** Store a schema-versioned `WorldProfile` in each slot's metadata and migrate old slots that only have name/seed without destroying or inventing values. Fresh server creation, Create World, duplicate, backup restore-as-new-slot, raw-directory import, and Java↔Bedrock conversion must all preserve an explicit profile when one exists. When adopting an existing world, read the real Java or Bedrock world data and mark values as detected, unknown, unsupported, or achievement-disabled; do not change server-wide settings just to make an imported world look uniform. Preserve unknown properties and unsupported values for round-trip safety, but never report an unverified value as applied. Make the slot summary/API carry enough information for the client to show the active world, saved defaults, and detected safety state without opening raw files.
**Verify:** `cargo fmt --all -- --check && cargo nextest run -p msc-domain --test world --test world_nbt && cargo nextest run -p msc-infrastructure --test world_store && cargo nextest run -p msc-application --test world_slot_crud --test world_import_reconciliation --test world_conversion --test bedrock_world_backup`
**Commit:** `P12.24: persist and detect per-world settings`
**Batch:** solo
**Notes:** Stores the versioned profile as a nested `profile` object in `slot.json`, keeping the pre-P12.24 `WorldSlot` and legacy API summary shapes compatible. Legacy slots migrate to identity-only profiles without inventing values; imported Java/Bedrock NBT populates only values actually read, and unknown safety remains explicit when data cannot be verified. Fresh creation records configured difficulty/gamemode, while create, duplicate, import, reconciliation, rename, restore, and conversion preserve or refresh profiles; raw JSON copying retains unknown future profile properties. P12.25 still owns profile application and the separate server-settings surface, so the existing legacy `/v1/worlds` summary remains unchanged per P12.23.

### P12.25 — Apply the saved profile and expose server settings separately
**Status:** built, awaiting verification
**Files:** `crates/msc-application/src/worlds.rs`, `crates/msc-application/src/provisioning.rs`, `crates/msc-agent/src/routes/worlds.rs`, `crates/msc-agent/src/routes/settings.rs`, `crates/msc-domain/src/settings_schema.rs`, `crates/msc-domain/src/properties.rs`, `crates/msc-infrastructure/src/world_store.rs`, `docs/msc2/api-contract/openapi.json`, `crates/msc-application/tests/world_activation.rs`, `crates/msc-agent/tests/bedrock_routes.rs`, `crates/msc-agent/tests/bedrock_production_surfaces.rs`
**What:** Make initial creation and world activation use the same application path: stop/refuse conflicting operations as required, apply that slot's saved world profile, preserve server-owned properties, and report whether a change is live, pending restart, or blocked while running. Add the world-profile read/update surface needed by both clients while keeping `/v1/settings` focused on server-owned settings. Remove the current presentation-only mixing of difficulty/gamemode and server controls; leave values such as ports, max players, allowlist, MOTD, runtime, and network helpers in the server profile. Keep the server-wide gamemode policy/force-gamemode override disabled by default; when deliberately enabled, label it as a server-wide policy that can supersede every world's saved default rather than silently rewriting the slots. Activation, duplicate, restore, import, and restart must read back what the runtime actually accepted.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-application --test world_activation && cargo nextest run -p msc-agent --test bedrock_routes --test bedrock_production_surfaces`
**Commit:** `P12.25: apply world profiles and split server settings`
**Batch:** solo
**Notes:** Initial creation and activation now share `apply_world_profile`, which writes the slot's supported projection fields, preserves unrelated server properties, and verifies the resulting file by reading it back. Added slot profile GET/POST routes with sparse JSON updates and per-field live/pending-restart/blocked status; `/v1/settings` now rejects world-owned keys and no longer advertises them. The current toolchain's Clippy baseline also flagged an unchanged `world_store::load_profile` let-and-return pattern in HEAD; its behavior-preserving simplification is included solely to make the declared verifier clean. The Bedrock production smoke expectation for `/v1/players` was corrected to match its existing response shape; no player-route behavior changed.

### P12.26 — Centralize creative, cheats, and achievement safety checks
**Status:** built, awaiting Cameron's verification
**Files:** `crates/msc-application/src/world_safety.rs` (new), `crates/msc-agent/src/routes/commands.rs`, `crates/msc-agent/src/routes/settings.rs`, `crates/msc-agent/src/routes/worlds.rs`, `crates/msc-agent/src/routes/servers.rs`, `crates/msc-agent/src/cli/mod.rs`, `crates/msc-agent/tests/bedrock_cli.rs`, `crates/msc-agent/tests/bedrock_routes.rs`, `crates/msc-application/tests/command_input.rs`, `clients/desktop-web/src/lib/sections/fleet/wizard/WorldStep.svelte`, `clients/desktop-web/src/lib/sections/worlds/CreateWorldSheet.svelte`, `clients/desktop-web/src/lib/components/shell/sidebar/QuickCommandsSection.svelte`, `clients/desktop-web/src/lib/sections/settings/SettingsSection.svelte`, `docs/msc2/api-contract/openapi.json`
**What:** Put one agent-side guard in front of every MSC-controlled path that can change a world toward Creative or enable cheats: server creation, world creation/editing, activation, settings, the in-app console, Quick Commands, CLI commands, and the equivalent API calls used by other clients. Creative/cheat changes for Bedrock must show a deliberate confirmation explaining that achievements may be permanently disabled for that world, even if cheats are later turned off; the default remains off. Enabling the server-wide gamemode override must require its own confirmation stating that it applies to every world on the server and can supersede each slot's saved default; it is off by default and must never be enabled as a side effect of choosing a world's Creative mode. The warning must be returned by the API/CLI contract as a structured confirmation requirement, so a client cannot bypass it by using a different surface. Java should explain the different advancement/command semantics without claiming the Bedrock Xbox-achievement consequence. Treat commands MSC cannot inspect (for example, an external console or a mod's custom command) as externally changed state: detect it on readback and report it, but do not pretend MSC can intercept another process. Test the server-wide override separately from a world default so accidentally enabling Creative does not turn every slot into an irreversible-looking change.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-application --test command_input && cargo nextest run -p msc-agent --test bedrock_cli --test bedrock_routes`
**Commit:** `P12.26: centralize world safety confirmations`
**Batch:** solo
**Notes:** Added one application-level confirmation vocabulary and guard for Bedrock achievement risk, Java Creative/command semantics, and the separate server-wide force-gamemode policy. The command, settings, server-create, world-profile update, and world-activation routes reject unsafe changes before side effects and return a structured acknowledgement token; existing world readback continues to expose externally changed Bedrock cheats as achievement-disabled. Added CLI retry tokens, neutral desktop confirmation surfaces, and OpenAPI coverage. `crates/msc-application/src/lib.rs` registers the new module as required by the new application file. The declared verifier passed; nextest reported the existing Bedrock API integration test as passing but leaky.

### P12.27 — Build the shared Create Server and Worlds-tab world-settings experience
**Status:** built, awaiting verification
**Files:** `clients/desktop-web/src/lib/sections/fleet/wizard/WorldStep.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/ConfirmStep.svelte`, `clients/desktop-web/src/lib/sections/worlds/CreateWorldSheet.svelte`, `clients/desktop-web/src/lib/sections/worlds/WorldSettingsForm.svelte` (new), `clients/desktop-web/src/lib/sections/worlds/WorldsSection.svelte`, `clients/desktop-web/src/lib/sections/worlds/WorldSlotCard.svelte`, `clients/desktop-web/src/lib/sections/settings/SettingsSection.svelte`, `clients/desktop-web/src/lib/sections/worlds/model.ts`, `clients/desktop-web/tests/screens/add-server-wizard.test.ts`, `clients/desktop-web/tests/screens/worlds-backups.test.ts`, `docs/msc2/antiAIslop.md`
**What:** Use one `WorldSettingsForm` in both flows so the first world created with a server and a later world created in the Worlds tab have the same ownership, labels, defaults, warnings, and save behavior. Start with a calm Essentials section (name, seed, difficulty, default game mode), then use collapsed World Generation and Gameplay Rules sections for the complete supported native options. Show version/edition/flavor-specific sections only when the agent advertises them; unsupported settings are explained as unavailable, never silently ignored. The Create Server wizard's review separates “Server settings — apply to every world” from “First world settings — saved with this world.” The Worlds tab repeats the ownership sentence, shows a concise saved-profile summary on each slot, and opens the same form for editing; creation-only values become read-only after generation with a clear replace/create-new-world path where required. Settings and sidebar controls show the server profile only, with an explicit link back to the active world's settings. Keep the server-wide gamemode override off by default and use the central Bedrock safety confirmation from P12.26. Follow `antiAIslop.md`: one-card depth, existing MSC field/button/sheet primitives, neutral disclosure rows, deliberate spacing, and no decorative color or generic dashboard chrome.
**Verify:** `cd clients/desktop-web && npm run check && npx vitest run tests/screens/add-server-wizard.test.ts tests/screens/worlds-backups.test.ts`
**Commit:** `P12.27: build shared world settings flows`
**Batch:** solo
**Notes:** Added one shared world-profile form for the fresh-server wizard and Worlds-tab create/edit flows, with Essentials visible first, neutral collapsed disclosures for generation/gameplay, lifecycle read-only messaging, edition-aware unsupported explanations, and the P12.26 structured safety confirmation path. The wizard review and Settings surface now state server-versus-world ownership explicitly; slot cards show saved difficulty/mode summaries. Profile writes filter Java-only and Bedrock-only keys before sending. The declared verifier's `npm run check` is currently blocked by five unchanged HEAD diagnostics in the auth desktop transport, route descriptor typing, and its desktop auth test; the exact Vitest targets were run separately and passed (34 tests).

### P12.28 — Add version, edition, and Java-flavor capability handling
**Status:** built, awaiting verification
**Files:** `crates/msc-domain/src/capability.rs`, `crates/msc-agent/src/routes/capabilities.rs`, `crates/msc-agent/src/routes/versions.rs`, `crates/msc-api/src/dto/capabilities.rs`, `docs/msc2/api-contract/openapi.json`, `docs/msc2/client-capability-matrix.csv`, `clients/desktop-web/src/lib/sections/worlds/WorldSettingsForm.svelte`, `clients/desktop-web/src/lib/sections/fleet/wizard/WorldStep.svelte`, `clients/desktop-web/src/lib/sections/worlds/model.ts`, `crates/msc-domain/tests/capability.rs`, `crates/msc-agent/tests/provisioning_routes.rs`, `content/help/handbook/vanilla.md`, `content/help/handbook/paper.md`, `content/help/handbook/bedrock.md`, `content/help/handbook/standard-vs-modded.md`
**What:** Advertise the world-settings capabilities for the actual server type, Minecraft version, Java flavor, loader, and installed runtime instead of maintaining one static list. Cover the common 1.20+ Java and Bedrock profile, then the native generation/gameplay options that differ across Vanilla, Paper/Purpur, Fabric/Quilt, Forge/NeoForge, and Bedrock. Include the version in the advanced-settings context and re-evaluate capabilities after a version or flavor change. A setting the runtime does not advertise is disabled/hidden with a reason and is never sent as if it applied; existing unknown properties remain preserved and visibly unknown. For arbitrary third-party mod configuration, provide an explicit “provided by this server/mod” boundary and a safe handoff to the server's own configuration path rather than inventing a universal UI or silently dropping the setting. Update the capability matrix for Desktop/Web, iOS, and CLI so shared API support is not mistaken for complete UI parity.
**Verify:** `cargo fmt --all -- --check && cargo clippy -p msc-domain -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-domain --test capability && cargo nextest run -p msc-agent --test provisioning_routes && cd clients/desktop-web && npm run check`
**Commit:** `P12.28: add version-aware world setting capabilities`
**Batch:** solo
**Notes:** Added an additive `worldSettings` capability response with create-time query context, version-entry normalization, Java runtime probing, per-field availability/reasons, native flavor labels, and an explicit third-party server/mod configuration handoff. The shared Desktop/Web world form displays the selected context, preserves unknown properties, and carries the advertised field set through profile-change filtering; the wizard re-requests capabilities when edition, flavor, or version changes. Domain and provisioning-route checks pass. The exact verifier reaches `npm run check`, which still reports the five unchanged P12.27 baseline diagnostics in `auth/desktop.ts`, `navigation/route.ts`, and `tests/auth/desktop/desktop.test.ts`; no new diagnostics were reported in this step's files.

### P12.29 — World-settings completion evidence and Handbook parity gate
**Status:** built, awaiting verification
**Files:** `content/help/handbook/worlds-backups.md`, `content/help/handbook/bedrock.md`, `content/help/concept/worlds.md`, `content/help/concept/active-world.md`, `content/help/concept/settings.md`, `content/help/contextual/settings-difficulty.md`, `content/help/contextual/settings-gamemode.md` (new), `docs/msc2/clients/world-settings.md` (new), `docs/msc2/client-capability-matrix.csv`, `tools/phase12/world-settings-check.py` (new), `clients/desktop-web/tests/screens/world-settings.test.ts` (new), `crates/msc-application/tests/world_activation.rs`, `crates/msc-agent/tests/cli_worlds_backups.rs`
**What:** Close this work as a bounded world-settings release, not as an open-ended Minecraft encyclopedia. The native settings supported by MSC 2's 1.20+ floor, Java/Bedrock families, and the offered Java flavors must be reachable through the shared form, persisted per slot, applied on activation, capability-gated by version/flavor, and explained in the Handbook. The completion evidence must cover: fresh server creation and later Worlds-tab creation; two slots with different difficulty/gamemode profiles; duplicate/import/restore migration; activation and restart status; server-only settings remaining common to every world; the default-off server gamemode override; Creative/cheat confirmation through settings, Quick Commands, in-app console, CLI, and API; Bedrock achievement-disabled readback; Java versus Bedrock messaging; and an unsupported-version/flavor setting that is clearly not applied. Add a small fixture/checker rather than a broad workspace test sweep, and have Cameron perform the two live UI walks. Explicitly record that arbitrary mod-defined settings are outside the native core contract and are handed off safely; that is the support boundary for this release, not a hidden unfinished world-settings task. When this gate passes, there should be no remaining world-settings implementation or UX work in the current scope.
**Verify:** `python3 tools/phase12/world-settings-check.py --selftest && cargo nextest run -p msc-application --test world_activation && cargo nextest run -p msc-agent --test cli_worlds_backups && cd clients/desktop-web && npx vitest run tests/screens/world-settings.test.ts && npm run check`; Cameron's live verification: create one Java and one Bedrock server, create two worlds with different profiles, switch between them, and confirm the server/world summaries, safety confirmation, and Handbook explanations.
**Commit:** `P12.29: close the world settings release gate`
**Batch:** stop-after
**Notes:** Added the bounded world-settings release note, Handbook/Concept/contextual help parity, and a stdlib-only evidence checker. Added targeted UI source/model coverage for shared creation/editing, capability omission, migration routes, ownership, and safety messaging; activation coverage now proves two distinct Java slot profiles, server-only values, default-off `force-gamemode`, and Bedrock restart-required reporting; CLI coverage proves profile changes expose `--change` and `--confirm`. The exact verifier passed through the checker, both targeted Rust suites, and the five new UI tests. `npm run check` still reports the five unchanged HEAD diagnostics in `auth/desktop.ts`, `navigation/route.ts`, and `tests/auth/desktop/desktop.test.ts`, as already recorded by P12.27/P12.28. Cameron's two live Java/Bedrock UI walks remain required; no world-settings work is intentionally deferred after those walks.

### P12.30 — Connect verified Bedrock provisioning to the production lifecycle
**Status:** awaiting verification
**Files:** `crates/msc-application/src/bedrock_provisioning.rs`, `crates/msc-application/src/bedrock_runtime.rs`, `crates/msc-application/src/bedrock_linux.rs`, `crates/msc-application/src/bedrock_windows.rs`, `crates/msc-application/src/bedrock_macos.rs`, `crates/msc-infrastructure/src/bedrock_distribution.rs`, `crates/msc-agent/src/routes/servers.rs`, `crates/msc-agent/src/routes/lifecycle.rs`, `crates/msc-agent/src/routes/bedrock_runtime.rs`, `crates/msc-application/tests/bedrock_provisioning.rs`, `crates/msc-agent/tests/bedrock_production_lifecycle.rs`, `content/help/handbook/worlds-backups.md`, `crates/msc-application/tests/world_activation.rs`
**What:** Wire the existing verified archive downloader into the real Bedrock create/start/version path; it currently exists but has no production caller. Provision during the operation that creates or first starts a Bedrock server, preserve the world and user settings during updates, and report download/staging/checksum failures through the existing operation result. Select the correct archive explicitly: Linux uses the Linux BDS package, Windows uses the Windows package, and the macOS VM sidecar consumes the Linux guest package. Re-evaluate the selected server's eligibility after provisioning instead of keeping the startup snapshot that still says `provisioning_required`, and keep Java lifecycle behavior unchanged.
**Verify:** `cargo fmt --all -- --check && cargo nextest run -p msc-application --test bedrock_provisioning && cargo nextest run -p msc-agent --test bedrock_production_lifecycle`
**Commit:** `P12.30: wire Bedrock provisioning into production lifecycle`
**Batch:** solo
**Notes:** Production create and first-start now invoke verified Bedrock archive provisioning, preserve existing server state during promotion, select Linux/Windows/native or Linux-guest/macOS packages explicitly, and refresh eligibility after provenance is written. The P12.29 handbook additions used headings unsupported by the embedded help parser, and its new Rust test was not rustfmt-clean; both stale HEAD baselines were repaired so this step's declared verifier can run.

### P12.31 — Package the three Bedrock runtime variants
**Status:** awaiting verification
**Files:** `clients/desktop-web/tools/prepare-agent-dev.mjs`, `clients/desktop-web/src-tauri/src/lib.rs`, `clients/desktop-web/src-tauri/tauri.conf.json`, `sidecar/bedrock/BedrockSidecar.xcodeproj/project.pbxproj`, `sidecar/bedrock/Resources/README.md`, `packaging/agent-service-layout.json`, `tools/phase12/bedrock-package-check.py` (new)
**What:** Make the distributable layouts match the runtime lookup paths. Linux and Windows packages carry the native agent that launches their native BDS executable. macOS GUI and headless packages build and ship `BedrockSidecar` beside the agent together with the Intel `vmlinuz-kata` and `appliance-initramfs.gz` pair; the build must fail clearly when those binary resources are not supplied, rather than producing a package that can never boot Bedrock. Use an explicit build input and recorded checksums for the appliance pair (the pair remains a distribution artifact, not source), and do not add an Apple Silicon runtime. Keep the runtime BDS archive as a verified first-run download, not a GUI-package payload.
**Verify:** `python3 tools/phase12/bedrock-package-check.py --selftest`
**Commit:** `P12.31: package Bedrock runtimes for all hosts`
**Batch:** solo
**Notes:** Added one `agent/` Tauri resource payload and aligned the macOS, Windows, and Linux service paths with the desktop runtime lookup. Linux and Windows stage the native agent and keep their native BDS archives as verified first-run downloads; macOS validates the recorded Intel appliance checksums, builds `BedrockSidecar` as x86_64, and stages the sidecar plus kernel/initramfs pair for GUI and headless layouts. The macOS installer passes the sibling sidecar directory to launchd. Apple Silicon remains intentionally unsupported. The exact package self-test passed; appliance binaries and live cross-platform package builds remain Cameron's verification work. P12.31a makes the checked-in pair the default development input while retaining the environment variable as an override.

### P12.31a — Make the macOS Bedrock appliance self-contained
**Status:** built, awaiting verification
**Files:** `sidecar/bedrock/Resources/vmlinuz-kata` (new), `sidecar/bedrock/Resources/appliance-initramfs.gz` (new), `clients/desktop-web/tools/prepare-agent-dev.mjs`, `sidecar/bedrock/Resources/README.md`, `docs/msc2/rolling-plan.md`
**What:** Copy the verified Intel Bedrock kernel/initramfs pair into MSC 2's own sidecar resource directory and make that directory the default input for macOS development staging. Keep `MSC2_BEDROCK_APPLIANCE_DIR` as an explicit override for a deliberate replacement/release input. The macOS-only staging branch validates the same recorded checksums and copies the pair beside `BedrockSidecar`; Linux and Windows never read this directory, package this VM appliance, or change their native BDS paths. This removes the build-time dependency on the MSC 1 checkout. Before public distribution, confirm the pair's redistribution/license terms separately from the code port.
**Verify:** `cd clients/desktop-web && env -u MSC2_BEDROCK_APPLIANCE_DIR npm run prepare:agent && cd ../.. && python3 tools/phase12/bedrock-package-check.py --selftest`
**Commit:** `P12.31a: bundle the macOS Bedrock appliance`
**Batch:** solo

### P12.32 — Make Bedrock creation and capability state honest after provisioning
**Status:** awaiting verification
**Files:** `crates/msc-agent/src/routes/servers.rs`, `crates/msc-agent/src/routes/capabilities.rs`, `crates/msc-agent/src/routes/bedrock_runtime.rs`, `crates/msc-agent/src/routes/lifecycle.rs`, `crates/msc-api/src/dto/`, `docs/msc2/bedrock/compatibility-matrix.csv`, `docs/msc2/bedrock/evidence/`
**What:** Ensure a newly created or imported Bedrock record transitions cleanly from `provisioning_required` to `available` once its verified distribution and host prerequisites exist, and that create, start, stop, command, status, console, settings, worlds, backups, CLI, and the existing client surfaces all use that same selected runtime state. Remove any remaining path that reports a Bedrock server as ready while its executable, provenance, or Intel macOS sidecar resources are absent. Keep Apple Silicon explicitly unavailable under D-028. Record this phase's result as implementation-ready but leave the separate compatibility-matrix promotion and live runtime evidence for Cameron's later cross-platform verification phase.
**Verify:** `cargo clippy -p msc-application -p msc-agent --all-targets -- -D warnings && cargo nextest run -p msc-agent --test bedrock_runtime_selection --test bedrock_production_surfaces`
**Commit:** `P12.32: reconcile Bedrock readiness and capability state`
**Batch:** solo
**Notes:** Rebound production Bedrock selection to the persisted active record and refresh its eligibility on every Bedrock activation. Creation provisions and records the adapter binding so its first start can reuse it; imports verify distributions without binding the shared adapter. Cross-server selection is refused while that adapter is bound or running. The active-server response and server list expose the selected runtime state, and import/create operation results no longer claim `ready` without runtime availability. Compatibility-matrix cells and live evidence remain unchanged for Cameron's later cross-platform verification.

### P12.33 — Bedrock implementation-readiness gate
**Status:** awaiting verification
**Files:** `tools/phase12/bedrock-package-check.py`, `docs/msc2/bedrock/phase12-readiness.md` (new), `docs/msc2/rolling-plan.md`
**What:** Check the implementation boundary without pretending to have live server evidence: native Linux and Windows runtime selection, macOS Intel sidecar/resource lookup, verified BDS provisioning, service/headless package inclusion, and the shared public lifecycle path. Record the exact handoff for Cameron's later phase: run one disposable Bedrock server on each available OS, confirm UDP reachability and lifecycle recovery, then promote only the matching matrix cells. This is the end of the implementation work; no real server, VM boot, Windows run, or macOS run is required here.
**Verify:** `python3 tools/phase12/bedrock-package-check.py --readiness`
**Commit:** `P12.33: close Bedrock implementation readiness`
**Batch:** stop-after
**Notes:** Added a readiness mode to the existing package checker and recorded the implementation boundary. The checker validates native Linux/Windows selection, the Intel-macOS Swift sidecar and guest-package mapping, verified archive/provenance promotion with user-state preservation, the shared public lifecycle routes, and GUI/headless service layouts. It explicitly verifies that the compatibility matrix remains unavailable and leaves the live handoff — one disposable server per available host, UDP reachability, lifecycle recovery, and matching-cell promotion — for Cameron; no real server, VM boot, Windows run, or macOS run is claimed here.

### P12.LAST — Consistency sweep + parity gate
**Status:** not started
**Files:** `docs/msc2/clients/`, `docs/msc2/client-capability-matrix.csv`, `docs/msc2/renderings/`
**What:** Run after P12.0–P12.16, P12.18a–i, P12.19a–d, P12.20a–b, P12.21–P12.22, the world-settings completion block P12.23–P12.29, and the Bedrock implementation block P12.30–P12.33. Sweep every rebuilt screen for design-system consistency (uniform sheet sizes, spacing, one card language, no drift), run the `antiAIslop.md` checklist across all screens, and do the screen-by-screen MSC 1 parity comparison (shape + behavior). Confirm the D-003 corollary (no desktop-only screen) and that the kept data/agent layer's contract is unchanged. **This step also absorbs the deferred Phase 11 gate (P11.28k + P11.29), now evaluated against the redesigned client:** reconcile the capability matrix; prove D-003 bundle/screen identity, generated DTO drift, D-013 host isolation, capability/permission routing, D-026 served-content use, browser/Tauri auth, browser responsive behavior, native Linux WebKitGTK rendering, tri-platform packaging, and headless independence; and satisfy the first-launch preservation contract (real-agent/auth path, fresh-profile setup → Concept Guide → guided tour → Handbook, step/anchor coverage, skip/reopen, splash/reduced-motion, and the agent-owned vs client-owned initiation boundary). Also verify the reset contract: client-only reset never mutates a host, local full reset returns through Install and Continue, remote full reset requires fresh pairing, and neither path silently creates a server. Record evidence. The other agent decides in REVIEW whether the combined Phase 12 gate holds.
**Verify:** `cd clients/desktop-web && npm run test:unit && npm run check`, then `python3 tools/phase11/phase11-check.py --gate && python3 tools/phase6/capability-matrix-check.py docs/msc2/client-capability-matrix.csv && cargo nextest run --workspace`, then Cameron's full visual parity + anti-slop pass across every screen against `~/Documents/MSCSS/` and MSC 1. (This is Phase 12's only full-workspace run.)
**Commit:** `P12.LAST: consistency sweep and parity gate`
**Batch:** solo
