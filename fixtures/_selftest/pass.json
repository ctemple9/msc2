{
  "domain": "_selftest",
  "case": "pass",
  "source": {
    "file": "N/A - synthetic fixture, not extracted from MSC 1",
    "test": "selftest-pass",
    "line": 0
  },
  "input": { "value": 42 },
  "expected": { "value": 42 },
  "notes": "Proves the runner's full-compare path exits 0 on a match. The _selftest domain is computed as the identity function (actual = input), so input and expected are set equal on purpose."
}
