{
  "domain": "pack-managed-guard",
  "case": "dependency-auto-install-transitively-blocked-when-parent-mutation-refused",
  "source": {
    "file": "docs/msc2/addons/phase8-scope.md",
    "test": "Pack-managed guard section -- dependency-installer interaction",
    "line": 89
  },
  "input": {
    "server_packManaged": true,
    "operation": "installModrinthAddon for a new mod with required Modrinth dependencies"
  },
  "expected": {
    "parent_install_refused": true,
    "dependency_installer_invoked": false,
    "reason": "Since the pack-managed guard refuses the parent single-add-on install BEFORE any download begins, P8.15's dependency-aware installer is never reached at all for a refused parent operation -- there's no separate 'dependencies bypass the guard' case to design against, because the guard sits upstream of dependency resolution entirely."
  }
}
