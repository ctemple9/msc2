{
  "domain": "router-troubleshooting",
  "case": "duplicate-symptoms-normalized-preserving-first-occurrence-order",
  "source": {
    "file": "RouterPortForwardTroubleshootingEngine.swift",
    "test": "analyze(symptoms:fallbackState:runtimeContext:)",
    "line": 118
  },
  "input": {
    "symptoms": [
      "cannot_connect_externally",
      "mac_ip_address_changed",
      "cannot_connect_externally",
      "router_rule_points_to_old_ip",
      "mac_ip_address_changed"
    ],
    "rules": "standard",
    "topics": "full",
    "fallbackState": null,
    "detectedGatewayIpAddress": null
  },
  "expected": {
    "escalationBullets": [
      "If the router UI lists multiple copies of the same device, delete stale target entries before re-testing."
    ],
    "fallbackResolutionKind": null,
    "likelyCauses": [
      {
        "confidence": "strong",
        "id": "local_ip_changed",
        "matchedSymptoms": [
          "cannot_connect_externally",
          "router_rule_points_to_old_ip",
          "mac_ip_address_changed"
        ],
        "score": 11,
        "severity": "high"
      }
    ],
    "recommendedActions": [
      "Check the Mac's current local IP in the app.",
      "Update the router rule so it points to the current local IP.",
      "Reserve the Mac's IP in the router later so it stops changing.",
      "Re-check the Mac's current local IP in Minecraft Server Controller.",
      "Update the router rule so it points to the current host Mac address.",
      "Reserve the Mac's DHCP address later so it stops changing."
    ],
    "summary": "Most likely cause: Your Mac's local IP changed.",
    "symptoms": [
      "cannot_connect_externally",
      "mac_ip_address_changed",
      "router_rule_points_to_old_ip"
    ]
  },
  "notes": "Input repeats two symptoms and orders macIPAddressChanged before routerRulePointsToOldIP. `report.symptoms` dedups preserving that first-occurrence input order (mac before router-rule), but the resulting cause's `matchedSymptoms` still orders anyOf by the rule's own declared order (routerRulePointsToOldIP before macIPAddressChanged) \u2014 proof `matchedSymptoms` order comes from rule declaration, not from input/normalized order, since the two orders disagree here."
}
